---
name: 娱乐圈模拟器独立模式
overview: 新增首页第三入口「娱乐圈模拟器」(gameMode='entSim')：玩家身份锁定=队友的妹妹+圈内艺人，职业4选1(练习生/爱豆/solo歌手/演员)，世界观锁定娱乐圈，分3章节(练习生期8回合/出道期15回合/巅峰期无限，章节内独立计数)。复用oneHeart恋爱引擎，娱乐圈特化逻辑集中到src/ent-sim/。含每日可点日程表+事业数值面板(人气0-100+4档等级)+4职业专属行程/事件池+哥哥递进考验+女主秘密+心动/告白+情敌假戏真做+5种掩护+曝光分档(安全0-25/注意26-50/危险51-75/爆表76-100)+10+细分结局。全部isSisterSetting放宽门控，零回归。
design:
  architecture:
    framework: html
  styleKeywords:
    - Glassmorphism
    - 深色玻璃
    - 紫调渐变
    - 圆角按钮
    - 微交互hover抬升
    - 娱乐圈质感
  fontSystem:
    fontFamily: PingFang SC
    heading:
      size: 14px
      weight: 600
    subheading:
      size: 13px
      weight: 500
    body:
      size: 11px
      weight: 400
  colorSystem:
    primary:
      - "#7C6FF0"
      - "#9D8BFF"
    background:
      - "#1A1626"
      - rgba(255,255,255,0.06)
    text:
      - "#F2EEFF"
      - rgba(242,238,255,0.6)
    functional:
      - "#5BD6A0"
      - "#FF8A8A"
      - "#FFD479"
todos:
  - id: entSim-skeleton
    content: 建entSim模式骨架：首页第三按钮+gameMode='entSim'+isEntSimMode/isSisterSetting/isIdolProfession放宽+17文件gameMode===oneHeart门控全量放宽+女团爱豆判断改isIdolProfession+旧entertainment世界观移除(文件保留)+存档迁移。用[code-explorer]全量搜索确保无遗漏
    status: pending
  - id: entSim-setup
    content: 建entSim专属setup4步：身份锁定/职业4选1UI/世界观锁定/男主哥哥情敌选择/Step4生成种子+小动作+爱豆女团背景(isIdolProfession)+女主秘密+按职业设entSimChapter+人气初始值+state新字段
    status: pending
    dependencies:
      - entSim-skeleton
  - id: entSim-prompts
    content: 注入entSim专属prompt：4职业规则差异化(恋爱禁令/回归节奏/同行共鸣/粉丝经济)+男主称呼演进+第3轮情敌改再次出现+日程encounterHint引导+章节进度注入
    status: pending
    dependencies:
      - entSim-setup
  - id: entSim-career
    content: 实现事业系统：人气0-100+等级派生+章节切换(章节内计数entSimChapterRound练习生8/出道15/巅峰无限)+切章动画+过渡剧情+事业面板弹窗UI+大事件涨跌+曝光分档边界(安全0-25/注意26-50/危险51-75/爆表76-100)
    status: pending
    dependencies:
      - entSim-prompts
  - id: daily-agenda
    content: 实现每日可点日程表：4职业专属行程池+entSimTodayAgenda生成+UI日程卡片按钮+doAgendaActivity(复用事件卡)+encounterHint分支+概率成败影响人气+轻量副作用+已执行置灰
    status: pending
    dependencies:
      - entSim-career
  - id: brother-secret
    content: 实现哥哥递进考验2-3次+三重身份察觉+团队支线 + 女主秘密发现/撞破/调侃三路径
    status: pending
    dependencies:
      - entSim-prompts
  - id: romance-exposure
    content: 实现心动时刻高光+男主告白(接受/拒绝/拖延可再告白) + 情敌假戏真做被拍+5种掩护自动应用+曝光分档+彻底曝光抉择(爆表>=76)
    status: pending
    dependencies:
      - brother-secret
  - id: social-endings
    content: 实现朋友圈(日常AI+事业节点自动发)暗斗+聊天输入动效+剧场全开5种 + 10+细分结局+情敌隐藏结局优先检测+事业巅峰/塌房判定
    status: pending
    dependencies:
      - romance-exposure
---

## 产品概述

新增首页第三入口「娱乐圈模拟器」(gameMode='entSim')，与换乘恋爱/只为你心动并列的独立模式。玩家身份锁定=队友的妹妹(亲哥是SEVENTEEN成员)+圈内艺人，职业4选1(练习生/爱豆/solo歌手/演员)。世界观锁定娱乐圈，分3章节推进(练习生期8回合/出道期15回合/巅峰期无限，章节内独立计数)。恋爱为主、事业为辅，复用oneHeart恋爱引擎，娱乐圈特化逻辑集中到src/ent-sim/独立模块。旧oneHeart的entertainment世界观移除并入新模式，旧存档迁移。

## 核心功能

**模式入口与Setup**

- 首页第三按钮「娱乐圈模拟器」设gameMode='entSim'；专属4步setup(选男主+职业4选1+女主信息 / 选哥哥情敌系统随机推荐+写作风格 / 确认+生成种子事件小动作女团背景秘密 / 开始)；身份世界观锁定不选；女主必须比全团13人都小(忙内妹妹)；不开放自定义职业；setup按职业设entSimChapter初始值(练习生=1，其余=2)与人气初始值(练习生10/爱豆40/solo35/演员30)

**分章节事业线**

- 三章节(章节内独立计数entSimChapterRound，跨章重置)：练习生期(0-7共8回合，仅练习生职业经历，出道评估达标转爱豆)→出道期(0-14共15回合，爱豆/solo/演员直接从此章开始)→巅峰期(无限，到达成结局)；章节间时间跳跃+过渡剧情；切章流程=检测达标→弹切章全屏动画→注入过渡剧情prompt→AI生成过渡段→进入新章重置entSimChapterRound
- 事业数值：人气0-100+事业等级派生分档(<20新人/20-50上升/50-80当红/80+顶流)；晋升保档、塌房(人气<10)强制降级；独立事业面板按钮点开看详情；涨跌来源=日程活动成败+大事件(回归/获奖)+曝光塌房-

**每日可点日程表**

- 每天生成女主1-3个活动按钮(随机数量)；日程非必做(做有加成+偶遇剧情，不做视作做了无加成)；一天日程1件+约会/探班1件(互斥沿用现有)；活动有概率成败影响人气
- 4职业专属行程池：练习生(练习室/月末评价/出道组竞争)、爱豆(回归/打歌/签售/Vlive/合宿)、solo(专辑/demo/音源/独立舞台)、演员(片场/试镜/拍摄/杀青)
- 真实娱乐圈分支：录节目遇哥哥团员(team)、练习刷手机看新闻(solo)、签售被粉丝问恋爱(粉丝)、拍画报撞见某人(拍摄)、休息日独处触发秘密/朋友圈/剧场(休息)
- 分支选项=预设框架+AI填充文案；影响=好感+曝光+人气+剧情分支；已执行置灰
- 曝光加成：录节目+2/签售+1/拍摄+3/公开活动+2；exposureRisk分档(安全0-25/注意26-50/危险51-75/爆表76-100)，爆表(>=76)触发彻底曝光抉择

**4职业行业事件池(全选)**

- 练习生：月末评价排名/出道组公布/前辈探班/宿舍夜话/淘汰危机/评估被批
- 爱豆：回归撞男主巡演/直拍被扒同款/签售被问恋爱/退团危机/part争议/同台打歌避嫌/ANTI攻击/海外签售异地
- solo：音源反响/创作合作给男主写歌/颁奖提名/音乐节/feat邀请/创作瓶颈
- 演员：试镜结果/剧组八卦/同剧演员暧昧传闻/收视率/演技争议/杀青感言
- 曝光风险差异：爱豆最高/演员低/solo中/练习生最低

**男主与感情推进**

- 上心种子事件(setup生成200字固定存档，类型现有4类+新增，音乐类硬约束女主主动哼唱→男主旁听)
- 专属小动作(开局选1主+1备，随好感解锁更多)
- 心动时刻(好感破20进暧昧期stage0→1，300字高光，仅一次)
- 男主告白(好感>=60且哥哥非protective，接受/拒绝/拖延；被拒拖延后看玩家后续行动可再告白)
- 肢体接触门槛：牵手stage1/拥抱stage1/接吻stage2/同床stage3；欧巴称呼stage2明确期解锁
- 剧场分阶段解锁：暧昧期解锁男主视角，明确期解锁情敌/哥哥视角，结局后全开；5种各800字

**哥哥系统**

- 开局参谋观望中立；递进考验2-3次(中局试探/后局设局安排妹妹情敌独处/结局前把关)；失败转testing后可逆转回supportive
- 三重身份(亲哥+情敌队友+男主队友)察觉情敌异常看支持度(高=告诉男主/低=纠结)；哥哥支线每5-8回合1次(创作瓶颈/黑粉/队友矛盾，绝不SOLO不分开)

**女主秘密**

- 固定2-3个全围绕EXO追星；男主发现=好感契机/被撞破看类型(可消除/永久把柄)/哥哥知道=调侃

**曝光与地下恋**

- 情敌假戏真做(固定stage>=3触发1-2次，营业CP被拍→危机公关→澄清/不回应/借机公开)
- 5种掩护手段(假装不熟/借哥打掩护/避嫌约会/营业掩护/公司掩护)自动应用最合适，累计3次粉丝起疑曝光+
- 曝光阶梯后果+热度衰减(低调日自动减+手动公关2次用完后仅低调日减)
- 彻底曝光公开抉择=结局高潮(爆表档>=76触发)

**社交与剧场**

- 朋友圈=日常AI生成+事业节点(回归/获奖/杀青)自动发庆祝动态；暗斗彩蛋男主+情敌都评论(随机50%)
- 聊天=自由输入+快捷话题；"正在输入"时长随好感(低3-5秒/高0.5-1秒)；"开会中晚点回"看好感(高好感不出现)
- 男主/哥哥生日触发特殊剧情；节日=沿用HOLIDAYS_1V1+艺人专属(出道纪念日/回归日/杀青日)

**结局**

- 玩家主动选「走向大结局」触发；10+细分(HE公开婚礼/地下永久/带球跑、NE和平分手/远距离/暧昧未明、BE曝光塌房/情敌上位/哥哥反对、隐藏和情敌在一起)
- HE公开婚礼额外条件=哥哥supportive+公开恋爱+事业稳；事业巅峰+恋爱稳定=HE加成，事业崩塌=强制BE，其余事业状态仅影响文案
- 情敌隐藏结局优先检测(aff<40+情敌倾向>=50+接受情敌告白→替换男主情敌变新男主继续，旧男主变情敌走真诚追求路线非营业CP型)

## 技术栈

- 语言/运行时：JavaScript(ES Modules)，Vite 5.4构建，浏览器原生运行(vanilla JS，无组件库)
- 复用现有约定：var不用let/const、字符串+拼接、AI文本escHtml()转义、禁alert/confirm用showToast/showConfirmModal、GS引用不可替换、新增状态字段在state.js:defaultGameState()定义+migrateSave()兜底
- 不引入新依赖，不引入Tailwind(项目CSS变量体系style.css)

## 实现方案

采用「独立模式+引擎复用+逻辑抽模块」策略。entSim在核心调度上等价于oneHeart(复用renderOneHeartGameScreen/generateOneHeartRound/buildOneHeartSystemPrompt/聊天/朋友圈/剧场/约会/探班/曝光/事件卡/好感度引擎)，但setup走专属流程、娱乐圈特化逻辑集中到src/ent-sim/。

### 门控放宽策略（核心，零回归关键）

经探索确认gameMode === 'oneHeart'散布在17个文件中，分两类处理：

**第一类：核心调度点(改===为=== || ===)** — entSim必须走oneHeart引擎路径：

- ui-renderer.js: renderGameScreen(:1408)、bindGameEvents(:1821)、renderAll CSS(:76)、setup step2/3/4分流(:314/431/491/917/960/1062)、模式按钮绑定(:556)、行程条生成(:1281)
- game-engine.js: generatePhaseNarrative(:58)、generateOneHeartRound、handleOptionChoice、handleFreeAction(:977)、handleRegenerate、advancePhase、scheduleReturnGift/checkPendingReturnGifts、applyOneHeartEventEffects、validateOneHeartNarrative、话题频率追踪
- prompts.js: buildSystemPrompt(:88)、buildUserMessage(:302)、感情进度门控(:1513)、行程约束(:1627/1356)、哥哥立场块(:1802)、偶像行为引擎(:1857)、队友妹妹总原则(:1879)
- validator.js: checkOneHeartPacing(:36)、checkOneHeartAddressing(:89)
- 其他: parser.js、ui/narrative-box.js、ui/header-bar.js、modals/

**第二类：娱乐圈特化函数(已检查worldSetting==='entertainment'，isSisterSetting放宽后自动生效)**：

- game-engine.js: generateOneHeartSchedule(:2316)、pickOneHeartBrotherEvent、updateBrotherStance、applyDateEffects、accumulateScandalHeat、applyScandalHeatDailyDecay、handleBrotherChat、哥哥递进考验、营业CP曝光等约15处

**关键：isSisterSetting()放宽 + isIdolProfession()统一**

### 审计修正6项（已纳入）

**P0-1 章节回合数改章节内独立计数**：新增entSimChapterRound字段，每章从0计，跨章重置。练习生期0-7(8回合)/出道期0-14(15回合)/巅峰期无限。爱豆/solo/演员开局直接出道期chapterRound从0计15回合进巅峰，解决"出道期15回合"矛盾。

**P0-2 entSimChapter按职业初始**：setup按profession的startChapter设entSimChapter(练习生=1，爱豆/solo/演员=2)，state默认1但setup覆盖，避免爱豆开局错误触发练习生出道剧情。

**P0-3 profession字段统一+isIdolProfession()**：entSim profession存"爱豆"(不带"女团")。新增isIdolProfession()辅助(匹配'爱豆'||'女团爱豆'兼容旧存档)。所有现有=== '女团爱豆'判断改为isIdolProfession()，确保buildHeroineGirlGroup等女团逻辑对entSim爱豆触发。

**P1-4 worlds/entertainment.js保留**：只从ONE_HEART_WORLDS选项和worlds/index.js的_all移除entertainment，文件本身保留(game-engine.js:14仍import ENT_SCHEDULE_POOL/BROTHER_EVENTS)。

**P1-5 章节切换注入时机**：检测回合达标→弹切章全屏动画→注入过渡剧情prompt→AI生成过渡段→进入新章重置entSimChapterRound。

**P1-6 曝光分档数值边界**：安全0-25/注意26-50/危险51-75/爆表76-100，爆表档(>=76)触发彻底曝光抉择。

**P2-7 事业人气初始值按职业**：练习生10/爱豆40/solo35/演员30。

### 关键技术决策

1. 首页第三入口：ui-renderer.js:298-303新增modeEntSim按钮；:221 modeSel三选一；:541-560 bindSetupEvents新增modeEntSim绑定设gameMode='entSim'。

2. entSim专属setup：分流到ent-sim/setup.js。Step2(选男主+职业4选1+女主信息)→Step3(选哥哥情敌系统随机+写作风格)→Step4(确认+生成种子/小动作/女团背景/秘密+按职业设entSimChapter+人气初始值)→Step5(游戏)。setup锁定worldSetting='entertainment'+role='哥哥'。

3. Prompt复用与扩展：buildOneHeartSystemPrompt已有isSisterSetting()块自动生效。entSim额外注入buildEntSimPromptSuffix(profession)：4职业规则差异化+男主称呼演进+日程encounterHint引导+章节进度注入。

4. 日程表：generateOneHeartSchedule(:2316)门控放宽+末尾扩展entSimTodayAgenda。4职业专属行程池按profession抽取，每项{id,task,place,cat,type,encounterHint}。doAgendaActivity复用generateEventStory生成150-200字短故事+按cat应用副作用+概率成败影响人气。

5. 事业系统：entSimPopularity(0-100)+entSimCareerRank派生+entSimChapter+entSimChapterRound(章节内计数)。章节切换检测entSimChapterRound达标触发切章动画+过渡剧情。事业面板独立弹窗显示人气条+等级徽章+章节进度。

6. 旧entertainment移除与迁移：data.js ONE_HEART_WORLDS移除entertainment项；worlds/index.js _all移除(entertainment.js文件保留)。migrateSave新增oneHeart+entertainment→entSim迁移。

7. 结局判定：evaluateEntSimEnding返回10+细分type，按哥哥立场×情敌倾向×曝光×是否公开×好感×事业组合。情敌隐藏结局优先检测。事业巅峰+恋爱=HE加成，事业崩塌=强制BE。

### 性能与可靠性

- 日程生成O(1)随机抽取，跨天随generateOneHeartSchedule一起生成
- 场景AI生成复用generateEventStory，失败走兜底文案不阻塞
- 事件复用_pendingEvents队列(上限2)不刷屏
- 结局判定纯同步函数无AI调用
- 存档迁移migrateSave一次性执行，旧存档无感知升级
- 章节内计数仅多一个字段自增，无额外开销

## 目录结构

```
src/
├── ent-sim/                    # [NEW] 娱乐圈模拟器独立模块
│   ├── index.js                # 模块入口导出所有entSim函数
│   ├── setup.js                # entSim专属setup4步(身份锁定/职业4选1/世界观锁定/按职业设chapter+人气初始值)
│   ├── prompts.js              # buildEntSimPromptSuffix(profession) 4职业规则差异化+称呼演进+第3轮情敌改再次出现+章节进度注入
│   ├── schedule.js             # 4职业专属行程池定义 + generateEntSimAgenda(复用generateOneHeartSchedule扩展)
│   ├── agenda.js               # doAgendaActivity 日程执行+encounterHint分支+复用generateEventStory+概率成败+轻量副作用
│   ├── events.js               # 娱乐圈事件池: 哥哥递进考验/情敌假戏真做/女主秘密/4职业行业事件
│   ├── career.js               # 事业数值(人气0-100+等级派生)+章节切换(entSimChapterRound章节内计数)+切章动画+过渡剧情
│   ├── endings.js              # evaluateEntSimEnding 10+细分type+情敌隐藏结局优先检测+事业巅峰/塌房判定
│   └── data.js                 # ENT_SIM_PROFESSIONS(4职业+startChapter)/4职业行程池/事件池/结局模板/encounterHint映射/曝光分档边界
├── ui-renderer.js              # [MODIFY] step1第三按钮modeEntSim; modeSel三选一; renderGameScreen/bindGameEvents/renderAll扩展含entSim;
│                               #   setup分流entSim; 行程条门控放宽; 新增事业面板弹窗+章节进度条+切章动画;
│                               #   :1078 buildHeroineGirlGroup触发条件改isIdolProfession()
├── utils.js                    # [MODIFY] 新增isEntSimMode()+isIdolProfession(); isSisterSetting()放宽含entSim
├── state.js                    # [MODIFY] defaultGameState新增: entSimTodayAgenda/entSimAgendaDone/entSimPopularity/
│                               #   entSimCareerRank/entSimChapter/entSimChapterRound/_mainConfessionDone/
│                               #   entSimSecret/_brotherTestPhase/entSimCoverUsed/entSimManualPRUsed/entSimExposurePublic;
│                               #   migrateSave新增oneHeart+entertainment转entSim迁移
├── game-engine.js              # [MODIFY] 核心调度点gameMode===oneHeart放宽含entSim(约15处);
│                               #   generateOneHeartSchedule门控放宽+扩展entSim agenda;
│                               #   娱乐圈特化函数改isSisterSetting()(约15处自动生效);
│                               #   === '女团爱豆'判断改isIdolProfession();
│                               #   generateOneHeartRound内章节切换检测(entSimChapterRound)+事业数值涨跌
├── prompts.js                  # [MODIFY] buildSystemPrompt/buildUserMessage门控放宽;
│                               #   :1037-1044女团背景注入改isIdolProfession();
│                               #   新增entSim职业规则+称呼演进+章节进度注入
├── validator.js                # [MODIFY] checkOneHeartPacing/checkOneHeartAddressing门控放宽含entSim
├── data.js                     # [MODIFY] ONE_HEART_WORLDS移除entertainment项; buildHeroineGirlGroup增强(队友具象化)
├── worlds/index.js             # [MODIFY] 移除entertainment导入和_all项(文件保留)
├── app.js                      # [MODIFY] import ent-sim/index.js
├── style.css                   # [MODIFY] 新增.entsim-mode样式+日程卡片+事业面板+章节进度条+切章动画
├── parser.js                   # [MODIFY] gameMode===oneHeart门控放宽
├── ui/header-bar.js            # [MODIFY] 门控放宽+章节进度条渲染
├── ui/narrative-box.js         # [MODIFY] 门控放宽
└── modals/                     # [MODIFY] news-modal/gift-panel/review-modal等门控放宽
```

## 关键代码结构

```javascript
// utils.js — 门控放宽 + isIdolProfession
export function isEntSimMode() {
  return GS.gameMode === 'entSim';
}
export function isSisterSetting() {
  if (GS.gameMode === 'entSim') return true;
  return GS.gameMode === 'oneHeart' && GS.worldSetting === 'entertainment'
    && GS.oneHeartRelationCharacter && GS.oneHeartRelationCharacter.role === '哥哥';
}
export function isIdolProfession() {
  var p = (GS.heroineProfile && GS.heroineProfile.profession) || '';
  return p === '爱豆' || p === '女团爱豆';
}

// state.js — 新增字段
entSimTodayAgenda: [],
entSimAgendaDone: {},
entSimPopularity: 30,         // setup按职业覆盖: 练习生10/爱豆40/solo35/演员30
entSimCareerRank: 'rookie',
entSimChapter: 1,             // setup按职业覆盖: 练习生=1, 其余=2
entSimChapterRound: 0,        // 章节内独立计数, 跨章重置
_mainConfessionDone: false,
entSimSecret: null,
_brotherTestPhase: 0,
entSimCoverUsed: 0,
entSimManualPRUsed: 0,
entSimExposurePublic: null,

// ent-sim/career.js — 章节切换(章节内计数, P0-1修正)
export function checkChapterSwitch() {
  var cr = GS.entSimChapterRound || 0;
  var chap = GS.entSimChapter || 1;
  if (chap === 1 && cr >= 8) { enterChapter(2); }       // 练习生期8回合→出道期
  else if (chap === 2 && cr >= 15) { enterChapter(3); }  // 出道期15回合→巅峰期
}
function enterChapter(newChap) {
  GS.entSimChapter = newChap;
  GS.entSimChapterRound = 0;  // 跨章重置
  triggerChapterTransition(newChap);  // 弹切章动画→注入过渡prompt→AI生成→进新章
}

// ent-sim/data.js — 曝光分档边界(P1-6)
export var EXPOSURE_TIERS = {
  safe: [0, 25],       // 安全
  caution: [26, 50],   // 注意
  danger: [51, 75],    // 危险
  explosive: [76, 100] // 爆表→触发彻底曝光抉择
};
```

entSim模式沿用项目现有玻璃拟态+深色紫调风格，主体复用oneHeart框架(底部4Tab剧情/聊天/朋友圈/剧场+行程条+操作区+快捷指令抽屉)，不整套重设计。通过.entsim-mode CSS标识做紫调微差异化向娱乐圈质感偏移。新增UI块：首页第三入口按钮、entSim专属setup4步、每日日程卡片、事业面板弹窗、章节进度条+切章动画。

页面区块(1v1操作区内新增)：

- 日程标题条：今日日程·Day N+天气点缀，与哥哥立场/曝光指示同行
- 活动按钮组：1-3个圆角玻璃按钮横排，图标(📺录节目/🎤练习/✍️签售/📷拍摄/🌙休息按cat映射)+活动名(13px)+地点小字(11px)；hover微抬升translateY(-2px)+边框高亮0.2s缓动
- 已执行态：变灰+图标加勾+禁用
- 休息日态：仅1个休息日·独处按钮
- 事业面板：独立弹窗，显示人气条(0-100)+事业等级徽章+章节进度
- 章节进度：Header常驻条+切章全屏动画
- 移动端窄屏按钮自动换行两列，可点区域>=44px

视觉规范：卡片底色半透明深色玻璃rgba(255,255,255,0.06)+模糊，边框1px半透白；按钮圆角12px内边距8-10px；主色#7C6FF0/#9D8BFF紫调渐变；背景#1A1626深色；字体PingFang SC。

## Agent Extensions

### SubAgent

- **code-explorer**
- Purpose: 在entSim-skeleton任务中全量搜索17个文件中所有gameMode === 'oneHeart'和gameMode !== 'oneHeart'调度点，以及所有=== '女团爱豆'判断点，输出精确行号清单
- Expected outcome: 完整的需放宽行号清单(核心调度点约30+处+娱乐圈特化函数约15处+女团爱豆判断点)，区分"改===为||"和"改isIdolProfession()"两类，确保门控放宽和职业字段统一无遗漏