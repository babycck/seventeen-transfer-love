# 代码审查报告（2026-07-08）

> 本文件为**独立新增**报告，与 `2026-07-07-代码审查修复计划.md` 并列，不覆盖、不修改旧文档。
> 审查目标：**① 回归验证** 2026-07-07 审查的 BUG-01~26 / IMP-01~21 是否真实落地、有无新回归；**② 全量排查** 此前未覆盖或新出现的 bug / 隐患。
> 审查方法：纯静态审查 + 精准 grep / 读取（4 个并行代码探索子代理 + 人工核对），未运行构建、未修改任何代码。

---

## 一、审查范围与基线说明

- **代码基线**：`src/`（约 85 个 JS 模块 + `style.css`）。相对 2026-07-07 已显著扩张，新增 `src/skeleton/`、`src/ui/`、`src/modals/`、`src/worlds/`、`src/memory/`、`src/parser/`、`src/prompts/` 子模块，以及 `core.js` / `schema.js` / `validator.js` / `story-cache.js` / `sms.js` / `screen-effect.js` 等新文件。
- **评判基准（AGENTS.md 规范）**：
  - AI 生成文本插入 DOM 前必须经 `escHtml()` 转义；
  - 用 `var` 而非 `let/const`，字符串用 `+` 拼接、禁用模板字符串；
  - `GS` 引用不可替换（用 `Object.assign`）；
  - 禁止 `alert()` / `confirm()`，统一 `showToast()` / `showConfirmModal()`。
- **严重度**：🔴 高危（崩溃 / 持久化 / 卡死）/ 🟡 中危（逻辑不严谨 / 语义偏差 / 规范偏离）/ 🟢 低危（健壮性 / 一致性 / 死代码）。

---

## 二、回归验证结果表

### 2.1 高危 BUG-01~08（全部 ✅ 已修复，无回归）

| 项 | 内容 | 结论 | 当前证据（file:line） |
|---|---|---|---|
| BUG-01 | `re_10` 随机事件永远不触发 | ✅ 已修复 | `src/data.js:536` cond 已为 `day>=4`；`utils.js:71-73` 未知字段 fail-closed |
| BUG-02 | 普通选项好感即时结算后未持久化 | ✅ 已修复 | `src/game-engine.js:761` 调用 + `:809` `saveGame()` |
| BUG-03 | fallback 到 pending 重复加分 | ✅ 已修复 | `src/game-engine.js:1142-1165` 即时结算后 `GS.pendingAffChanges.shift()` 消费 |
| BUG-04 | `rivalryIntensity` 稳态重复累加 | ✅ 已修复 | `src/skeleton/affection-engine.js:54-71` 上升沿逻辑 + `state.js:268-270/349-350/574-579` 三层兜底 |
| BUG-05 | `xItemsRevealRound` 跨天未重置 | ✅ 已修复 | `src/skeleton/scheduler.js:36-39` 重置 round，且 `xItemsRevealState` 显式跨天保留 |
| BUG-06 | Day11 深夜醉酒分支卡死 | ✅ 已修复 | `src/game-engine.js:465-467/498-500` 兜底「▶ 进入深夜」；`:711-715` 未命中统一路由 `generateDrunkConsequence`，永不触 phase 上限 |
| BUG-07 | `compressTodayForInjection` 重复 AI 压缩 | ✅ 已修复 | `src/memory.js:125-128` 缓存守卫；`scheduler.js:31` 跨天清空 |
| BUG-08 | `parsed.affChanges` 的 risk 扣分未结算 | ✅ 已修复 | `src/game-engine.js:762-769` 显式遍历 `riskMember/riskDelta` 结算 |

### 2.2 中低危 BUG-09~26（全部 ✅ 已落地，无回归）

| 项 | 内容 | 结论 | 证据 |
|---|---|---|---|
| BUG-09 | `showMissionCardModal` 死代码 | ✅ | 正式模块已删，仅 `app.js.backup` 残留文本 |
| BUG-10 | `re_13` 语义偏差 | ✅ | `utils.js:91-100` `memberDrinkCount` token；`data.js:539` cond 已改 |
| BUG-11 | `MEMBERS.find` 无容错 | ✅ | `event-triggers.js:32/78/95/129` 均加 `if(!x) return` / `filter` |
| BUG-12 | 疲劳值无上限 | ✅ | `affection-engine.js:40` `Math.min(5, fatigue+1)` |
| BUG-13 | 好感下限双标 | ✅ | `affection.js:9-15` 统一常量；`game-engine.js:1218-1221` 删二次 clamp |
| BUG-14 | 模板字符串误判 | ✅ 合规 | `prompts.js` 反引号/`${`/`let`/`const` 命中数 = 0 |
| BUG-15 | portrait 立绘空值防御 | ✅ | `portrait.js:5-6/11-12` 均有 `if(!memberId) return ''` |
| BUG-16 | `migrateSave` 仅 v22 兜底 | ✅ | `state.js:572-579` 移出版本判断外，遍历 `defaultGameState` 补 undefined 字段 |
| BUG-17 | `updateAffection` 无 `Number.isFinite` 防御 | ✅ | `affection.js:12` 已加 |
| BUG-18 | `evaluateCond` 未知字段静默 true | ✅ | `utils.js:70-74` 改 `console.warn` + `result=false` |
| BUG-19 | 思考泄漏拦截过严 | ✅ | `parser.js:276` 已移除易混淆词 |
| BUG-20 | `re_8` phase 字符串依赖 | ✅ | `utils.js:82-86` `phase` 兼容数字索引 |
| BUG-21 | `drinkCounts` 清零注释 | ✅ | `scheduler.js:43-45` 语义注释 |
| BUG-22 | AGENTS.md 运算符文档 | ✅ | `AGENTS.md:191` 已列出 `== >= <= > < &&` |
| BUG-23 | 秘密任务 6/8 死数据 | ✅ | `data.js:476-487` 仅 Day2/Day7；`utils.js:153-157` 移除 Day7 排除 |
| BUG-24 | 事件池计数/跳号 | ✅ | `data.js:520-542` 编号连续、四季变体齐 |
| BUG-25 | `re_2` 仅夏/冬 | ✅ | `data.js:524/526` 有 `re_2_spring` / `re_2_autumn` |
| BUG-26 | 随机/秘密任务互斥不对称 | ✅ | `utils.js:117-118` 与 `:151-152` 双向互斥 |

### 2.3 改进项 IMP-01~21（全部 ✅ 已落地，无回归）

| IMP | 内容 | 结论 | 关键证据 |
|---|---|---|---|
| IMP-01 | 相位事件仲裁 | ✅ | `event-triggers.js:201-225` + `prompts.js:334/337/345/432/439/463` 五处门控 |
| IMP-02 | 修罗场相对阈值 | ✅ | `affection-engine.js:61` 最高两位差距≤15 且都≥40 |
| IMP-03 | 吃醋观察者相对判定 | ✅ | `event-triggers.js:142-153` 阈值 40 + 非最低 |
| IMP-04 | 嫉妒任务收敛 D6-10 | ✅ | `event-triggers.js:7-15` 相位锁定互斥 |
| IMP-05 | 1v1 随机事件按阶段抽取 | ✅ | `game-engine.js:1863-1898` + `state.js:541` 兜底 |
| IMP-09 | 女主人格画像 | ✅ | `state.js:553-555` + `game-engine.js:1085-1107` + `prompts.js:451/1199` |
| IMP-10 | 脚踏两条船系统 | ✅ | `event-triggers.js:230-242` + `prompts.js:367` + 兜底 |
| IMP-11 | 成员关系网 | ✅ | `event-triggers.js:246-253` + `affection-engine.js:66-67` + `prompts.js:371-387` |
| IMP-12 | 抉择代价链 | ✅ | `event-triggers.js:257-274` + `prompts.js:389-403` + 兜底 |
| IMP-13 | 情绪状态链 mood | ✅ | `src/skeleton/mood-engine.js` + `state.js:354` + `game-engine.js:48/1130` + `prompts.js:405` |
| IMP-14 | 连续性硬事实锚 | ✅ | `prompts.js:902-917` 注入「不可推翻的事实」 |
| IMP-15 | 三人差异化反应 | ✅ | `prompts.js:409-429` |
| IMP-16 | 艺人行程/探班 | ✅ | `worlds/entertainment.js:32-73` + `game-engine.js:1981-2061` + `ui-renderer.js:2339-2384` + 兜底 |
| IMP-17 | 哥哥内人 | ✅ | `state.js:545-552` + `game-engine.js:1902-1918` + `prompts.js:1577-1602` |
| IMP-18 | 结局系统 | ✅ | `state.js:478-479` + `game-engine.js:1922-1962` + `prompts.js:1553` + `ui-renderer.js:1515/1553-1595` |
| IMP-19 | 节奏渐进 | ✅ | `game-engine.js:1966-1978` + `prompts.js:1291` |
| IMP-20 | 娱乐圈文案修订 | ✅ | `worlds/entertainment.js:4-7` 重写 |
| IMP-21 | 队友的妹妹 A~H | ✅ | `prompts.js:1604-1627` `role==='哥哥'` 门控注入 |

**回归结论：2026-07-07 审查列出的全部 26 个 BUG + 21 个 IMP 均真实落地，未发现修复被改坏、失效或引入新回归。**

---

## 三、新发现问题

### 🔴 高危（崩溃）

> 以下 3 项均为同一类根因：**1v1（只为你心动）模式下 `GS.selectedMembers` / `GS.secretX` 为空或未设定，而档案类弹窗仍按换乘逻辑读取 `xMember = MEMBERS.find(...)` 后直接访问 `.name` / `.stageName` / `.emoji`，导致 `TypeError` 崩溃。**

#### NEW-01：`x-archive-modal.js` 1v1 模式 `xMember` undefined 崩溃
- **位置**：`src/modals/x-archive-modal.js:30-40`
- **根因**：`var members = GS.selectedMembers.map(...)`；1v1 下 `selectedMembers` 为空 → `xMember = members.find(...)` 返回 `undefined` → `xMember.name` / `xMember.stageName` 抛错。同文件 `otherMembers` 遍历 `om.emoji` 亦无保护。
- **修复建议**：进入函数首行加 `if (GS.gameMode === 'oneHeart') { showToast('该功能仅换乘模式可用'); return; }`，或对 `xMember` 做 `if (!xMember) return;`。

#### NEW-02：`help-modal.js` 1v1 模式 `xMember` undefined 崩溃
- **位置**：`src/modals/help-modal.js:8,13`
- **根因**：`xMember = members.find(...)` 后 `xMember.emoji` / `xMember.name` 访问；1v1 下 `members` 为空崩溃。同逻辑的 `help-merged-modal.js` 已用 `xMember ? ... : '未设定'` 兜底，但本文件未加。
- **修复建议**：同 NEW-01，加 `gameMode` 早退或 `if (!xMember) return;`。

#### NEW-03：`x-items-modal.js` 1v1 模式 `xMember` undefined 崩溃
- **位置**：`src/modals/x-items-modal.js:5,23`
- **根因**：`var xMember = MEMBERS.find(function(m){ return m.id === GS.secretX; })` 后 `xMember.emoji` / `xMember.name`；1v1 下 `secretX` 为空/无匹配 → undefined 崩溃。
- **修复建议**：加 `gameMode` 早退或 `if (!xMember) { showToast(...); return; }`。

### 🟡 中危

#### NEW-04：`sms-modal.js` 发送按钮无防重，双击重复触发 `sendSms`
- **位置**：`src/modals/sms-modal.js:97-122`
- **根因**：`.sms-target-btn` 点击回调为 `async` 且未 `disabled`；快速双击同一目标会触发 `sendSms` 两次，造成好感重复加减（`-1` 给其他成员、`+5` 给目标两次）且 `smsHistory` 推两条重复记录。
- **修复建议**：点击后立即可视 `disabled` 或置 `GS.smsSentToday` 后立即禁用按钮，避免同一会话重复发送。

#### NEW-05：`review-modal.js` / `history-modal.js` `parsed.narrative` 缺失时 `.split` 崩溃
- **位置**：`src/modals/review-modal.js:294`、`src/modals/history-modal.js:61`
- **根因**：`parsed.narrative.split('\n')` 在异常存档 / 仅 `blocks` 结构的解析对象上 `narrative` 为 `undefined` 时抛 `TypeError`。`narrative-box.js` 已采用 `parsed.blocks` 优先分支更安全。
- **修复建议**：在使用前加 `if (!parsed.narrative) parsed.narrative = '';` 或优先用 `parsed.blocks`。

#### NEW-06：`chat-modal.js` `memberName` 未转义拼接（规范偏离）
- **位置**：`src/modals/chat-modal.js:32`
- **根因**：`messagesHtml = '...和 ' + memberName + ' 聊天吧...'`，`memberName` 为静态 `MEMBERS` 常量（非 AI/用户输入），实际 XSS 风险低，但违反"成员名插入 DOM 前统一 `escHtml`"规范（同文件 40 行已用 `escHtml(memberName)`）。
- **修复建议**：统一用 `escHtml(memberName)`。

#### NEW-07：`handleRegenerate` / `handleOptionChoice` / `handleFreeAction` 缺 1v1 防御性守卫（隐式耦合）
- **位置**：`src/game-engine.js:1320 / 670 / 972`
- **根因**：1v1 安全性当前完全依赖 `renderGameScreen`（ui-renderer.js:1295）开头的整体分流 `if (oneHeart) return renderOneHeartGameScreen()`。这 3 个 transfer 专用函数内部**没有** `if (GS.gameMode === 'oneHeart') return;` 守卫，若未来 1v1 界面误渲染通用 `.option-btn` / `btnRegenerate` 容器，会错走 transfer 逻辑访问 `GS.selectedMembers.some()` / `MEMBERS.find(...)` 而崩溃。属脆弱的隐式耦合。
- **修复建议**：在 3 个函数入口补 `if (GS.gameMode === 'oneHeart') return;`（防御性，不影响现有行为）。

### 🟢 低危 / 健壮性 / 死代码

| 项 | 位置 | 说明 |
|---|---|---|
| NEW-08 | `modals/archive-modal.js:96`、`modals/x-archive-modal.js:4` | `filterItemsByReveal()` 为死代码，全项目无调用方，可删除 |
| NEW-09 | `modals/help-modal.js`、`modals/help-manual.js`、`modals/index.js:8-9` | 疑似被 `help-merged-modal.js` 取代，导出 `showHelpModal`/`showHelpManual` 无运行期引用（叠加 NEW-02 的 1v1 崩溃，建议整体下线或加保护） |
| NEW-10 | `modals/dating-dice.js:55-65` | `others[1]` 在当前 3 人阵容恒安全；若未来改 2 人阵容会越界，标注潜在脆弱性 |
| NEW-11 | `modals/event-modal.js:64-123` | `window.showXxxEvent` 冗余挂载（game-engine 仅用 ES import），属方案 B 遗留，非 bug |
| NEW-12 | `ui/narrative-box.js:260-311` | 打字机 `setInterval` 有 `clearInterval` 清理兜底，基本安全 |
| NEW-13 | `src/app.js.backup` | 过期旧版副本（含旧 `compressTodayForInjection` / `showMissionCardModal`），不参与 ESM 构建、不影响运行；仅建议清理以免误导审查（**按 AGENTS.md 约定不得删除**） |

### XSS / escHtml / 持久化 / 卡死 专项确认（均合规，无新增隐患）
- AI 文本转义链路：`narrative-box.js`（observerOS / memberInterview）、`x-archive.js`、`x-items-modal.js`、`event-modal.js`、`sms.js`、`chat-modal.js`、`moments-modal.js` 的 AI 文本插入 DOM 前**均已 `escHtml`**，无 XSS 遗漏。
- 状态兜底：`mood` / `brotherStance` / `oneHeartSchedule` / `secretDating` / `memberBond` / `futureConsequences` / `heroinePersona` / `rivalryHighActive` / `oneHeartRandomUsed` / `oneHeartEnding` / `endingMeters` / `oneHeartBrother*` 等新增字段在 `defaultGameState` 与 `migrateSave`（含 BUG-16 通用兜底循环 `state.js:572-579`）**两处均完整兜底**，无漏底字段。
- 持久化：好感 / mood / brotherStance / schedule / visitLog / dispatch（300ms debounce 自动 save）等关键副作用后均有 `saveGame()`，刷新不丢状态。
- 跨天清理：符合 BUG-05（`xItemsRevealState` 跨天保留、`xItemsRevealRound`/`todayOptionTexts` 清零）；临时态 `exMessage`/`truthPunishment`/`midnightCall`/`questionBox`/`xGhostEvent`/`pendingJealousy`/`drunkTrigger` 均正确清理。
- 新文件（`core.js`/`schema.js`/`validator.js`/`story-cache.js`/`screen-effect.js`/`x-archive.js`/`promises.js`/`store.js`/`auth.js`/`api.js`/`init.js`）均被至少一个模块 import 接入主流程，无孤模块、无 escHtml 遗漏。

---

## 四、修复顺序建议

1. **🔴 优先（NEW-01 / NEW-02 / NEW-03）**：三处 1v1 档案类弹窗 undefined 崩溃，统一加 `gameMode === 'oneHeart'` 早退或 `xMember` 判空。影响 1v1 模式档案/规则速览/记忆物品功能可用性。
2. **🟡 其次（NEW-04 / NEW-05 / NEW-07）**：`sendSms` 双击防重、`parsed.narrative` 判空、`handleRegenerate` 等 3 函数补 1v1 防御守卫，消除崩溃与数据污染风险。
3. **🟡 规范（NEW-06）**：`chat-modal.js:32` 补 `escHtml(memberName)`，保持转义一致性。
4. **🟢 收尾（NEW-08~NEW-12）**：清理死代码 `filterItemsByReveal` / 失效的 help 导出、补 dating-dice 2 人阵容容错；`app.js.backup` 按约定保留不删。

---

## 五、回归验证清单（建议实测项）

- [ ] 1v1 模式下点击「X 档案 / 规则速览 / X 记忆物品」弹窗不崩溃（验证 NEW-01/02/03 修复）。
- [ ] 1v1 模式下连续点短信发送目标、确认 `smsHistory` 仅一条、`GS.smsSentToday` 即时禁用（NEW-04）。
- [ ] 用含异常 `blocks` 结构（无 `narrative`）的存档触发回顾/历史弹窗不崩（NEW-05）。
- [ ] 1v1 模式下确认通用「重新生成 / 选项按钮」容器未被误渲染（NEW-07 守卫生效）。
- [ ] 既有回归项复查：re_10 在 Day>=4 可触发；Day4/5 记忆物品不重复；Day11 深夜醉酒不卡死；选项好感刷新后保留；修罗场强度不异常爬升；风险选项扣分生效；Day2/Day7 上午弹出秘密任务；随机事件池无跳号、春/秋可抽 re_2。

---

*审查完成时间：2026-07-08。审查结论：旧项全部落地无回归；新发现 3 个 🔴（均为 1v1 档案类弹窗 undefined 崩溃）、4 个 🟡、6 个 🟢，建议按第四节顺序定点修复。*
