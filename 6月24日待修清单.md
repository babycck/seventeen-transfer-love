# 6月24日 待修清单

## 1. 打字机速度 BUG（已修复 ✅）
- **文件**：`src/ui/header-bar.js:38` / `src/ui/narrative-box.js:169` / `src/ui-renderer.js:74,76,963`
- **修复内容**：
  - 档位数值反转（慢=100/中=50/快=20/极快=10）
  - `|| 30` 改为 `== null ? 30 : x`，修复"即时"档失效
- **状态**：已完成

## 2. API 超时 AbortError（已修复 ✅）
- **文件**：`src/api.js:417`
- **修复内容**：超时从 30000ms 改为 60000ms
- **状态**：已完成

## 3. 叙事分段约束（已删除）
- **文件**：`src/prompts.js`
- **改动**：已删除 rule 9（每段不超过300字/换段）+ styleNote 变量及 5 处注入（每句话独立成一段）
- **状态**：已完成

## 4. Day 3 出现真心话抽取（已修复 ✅）
- **文件**：`src/prompts.js:103-104` + `src/prompts.js:643-650`
- **问题**：prompt 中 Day 3「每人说一个故事」可能被 AI 误解为真心话抽卡游戏
- **修复内容**：Day 3 环节描述中新增"这是自然讲故事环节，不是真心话游戏——没有抽卡、没有强制规则"
- **状态**：已完成

## 5. 好感度显示混乱，分不清选了什么
- **文件**：`src/affection.js` / `src/ui/option-panel.js`
- **问题**：玩家选了选项后，好感度变化飘字和日志可能与选项对应关系不清晰
- **排查方向**：
  - 选项的 `affName`/`affDelta` 是否正确传递和显示
  - `triggerAffectionFromChoice` 是否误匹配
  - 飘字动画防抖（300ms）是否导致连续选择时显示错乱
- **状态**：待排查（需用户提供截图或具体场景）

## 6. 采访间内容前言不搭后语
- **文件**：`src/prompts.js`（interview/memberInterview block 生成）
- **问题**：AI 生成的采访间段落与上文 narrative 衔接不上，逻辑断裂
- **可能原因**：
  - 删除分段约束后，AI 自由发挥可能导致 block 间衔接变差
  - 历史剧情注入（`getLayeredHistory`/`getTodayFullTextCapped`）可能不完整，AI 缺上下文
  - `noRepeatNote` 要求不复述，AI 可能过度回避导致跳跃
- **状态**：待排查（可能需要调整 prompt 或增加上下文衔接指令）

## 7. Day 3 上午跳下午无新剧情
- **文件**：`src/game-engine.js:967-978`（advancePhase）/ `src/ui-renderer.js:877-883`（跳过按钮）
- **问题**：点击"跳过时段"后 `advancePhase` 调用 `generatePhaseNarrative`，但没生成新剧情
- **排查方向**：
  - `generatePhaseNarrative` 是否被 `dayCompleted` 或其他条件拦截（line 968 `if (GS.dayCompleted) return`）
  - API 调用是否失败（结合 #2 的 30 秒超时 AbortError）
  - `resetPhaseState` 后 `phaseNarrative` 为空，渲染应显示加载状态
- **状态**：待排查（很可能与 #2 超时有关）

## 8. Day 4 记忆物品公开应每次只分享一个（已修复 ✅）
- **文件**：`src/prompts.js:435-446` / `src/state.js`
- **问题**：Day 4/5 prompt 把所有成员的 X 记忆物品一次性注入，AI 一次性全写出来
- **修复内容**：新增 `GS.xItemsRevealRound` 轮转索引，每次只注入 1 位成员的物品，轮转遍历所有成员
- **状态**：已完成

## 9. 时段刷新但自由输入次数未刷新（已修复 ✅）
- **文件**：`src/store.js:111`（RESET_PHASE_STATE）
- **问题**：`RESET_PHASE_STATE` 重置了 `phaseOptionCount` 但漏了 `phaseFreeCount`
- **修复内容**：在 `state.phaseOptionCount = 0;` 后加 `state.phaseFreeCount = 0;`
- **状态**：已完成

## 10. X 档案记忆物品格式不统一（已修复 ✅）
- **文件**：`src/x-archive.js:103-109` / `src/modals/x-items-modal.js`
- **问题**：`buildXArchiveItemsMemberPrompt`（成员↔场外X）输出格式与 `buildXArchiveItemsPrompt`（女主↔X）不同，渲染不兼容
- **修复内容**：统一为 `名称：xxx / 样式：xxx / 故事：xxx` 格式
- **状态**：已完成

## 11. 选项老是那几个一样的（已修复 ✅）
- **文件**：`src/prompts.js:596`（noRepeatNote）
- **问题**：AI 生成的选项重复度高，缺多样性约束
- **修复内容**：在 `noRepeatNote` 中增加 "选项必须场景相关，避免重复使用'主动搭话''保持距离''沉默不语'等通用模板，每个选项指向不同的行动方向"
- **状态**：已完成

## 11. 选项老是那几个一样的
- **文件**：`src/prompts.js`（options 生成）/ `src/ai-generator.js`（重试机制）
- **问题**：AI 生成的选项重复度高
- **可能原因**：
  - prompt 没有要求选项多样性
  - `regenerate` 时 `prevSummary` 注入要求"至少2个选项不同"，但普通生成没有多样性约束
  - temperature 0.7 可能偏低
- **方案**：在 prompt 中增加"选项必须场景相关、避免通用模板"的约束，或调高 temperature
- **状态**：待排查

---
*后续待修项继续追加*
