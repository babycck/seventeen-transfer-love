# 6月24日 待修清单

> ✅ 所有 4 项已全部修复（2026-06-24）
> 修改涉及 7 个文件：`data.js` `state.js` `game-engine.js` `option-panel.js` `affection.js` `prompts.js` `ai-generator.js`

---

## 1. 好感度显示混乱 ✅ 已修复

**修改文件**：`src/affection.js` / `src/game-engine.js` / `src/ui/option-panel.js`

| 根因 | 修复方式 |
|---|---|
| A. `triggerAffectionFromChoice` 匹配失败 silent return | 增加 opt 参数优先使用 AI 返回 affName/affDelta；匹配不到时 fallback 到 pendingAffChanges；终极 fallback 显示 Toast |
| B. 选项面板未展示 affDelta | 按钮下方追加 `affName +affDelta · affReason` 和 `风险：riskMember riskDelta` 小字标签 |
| C. 飘字 300ms 防抖吞变化 | 改为队列累积机制：100ms 窗口内同成员 delta 累加后统一飘出 |
| D. 好感日志需手动点击 | 选择后自动 `showToast` 汇总（如"+3 胜宽  -1 圆佑  -1 珉奎"） |

---

## 2. 采访间衔接断裂 ✅ 已修复

**修改文件**：`src/prompts.js` / `src/data.js`

| 根因 | 修复方式 |
|---|---|
| A. "禁止复述"压制 interview | system prompt rule 6 改为"narrative 禁止复述；interview 可以提及刚发生事件作为触发点"；`noRepeatNote` 同样区分 narrative 和 interview |
| B. 历史上下文容量不足 | `TODAY_TEXT_CAP` 从 8000 提升到 12000 |
| C. `noRepeatNote` 一刀切 | 改为 interview 允许引用前文事件，仅 narrative 禁止复述 |

---

## 3. Day 3 跳转无剧情 ✅ 已修复

**修改文件**：`src/game-engine.js`

| 原因 | 修复方式 |
|---|---|
| `advancePhase` 无重入锁（双击按钮跳过时段） | 添加 `_advancingPhase` 锁，带重入检测 + try/finally 释放 |

其他两个原因（`GS.aiEnabled` 为 false / API 失败 catch）已在前面的提交中解决，本次加锁使其不再出现。

---

## 4. 选项多样性不足 ✅ 已修复

**修改文件**：`src/prompts.js` / `src/ai-generator.js` / `src/state.js` / `src/game-engine.js`

| 根因 | 修复方式 |
|---|---|
| P0. 无历史选项黑名单 | `GS.todayOptionTexts` 积累当天已用选项文本，在 `buildUserMessage` 末尾注入 prompt 作为"禁止使用"列表 |
| P0. prevSummary 多样性只限 regenerate | 移除 `type === 'phase' \|\| type === 'regenerate'` 条件限制，所有类型都收到多样性指令 |
| P1. temperature 0.7 偏低 | 默认 temperature 0.7 → 0.85（`ai-generator.js` + 5 处调用点） |
| P1. noRepeatNote 约束偏弱 | 改为"禁止再次出现"已用列表 + 强化约束措辞 |

---

*后续待修项继续追加*
