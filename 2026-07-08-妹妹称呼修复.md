# 2026-07-08 修改清单：队友的妹妹称呼修复

> 模式范围：💗 只为你心动（1v1）+ 娱乐圈世界观 + 队友的妹妹（关系户＝亲哥哥，role==='哥哥'）
> 状态：✅ 已修复并通过 `npm run build`

---

## 修改项 #1（测试发现的问题）

**现象**：女主选择「队友的妹妹」身份时，剧情里她对成员**直呼名字**（如"圆佑""净汉"），与身份设定矛盾。

**应为**：女主比全团 13 名成员都小（`worlds/entertainment.js` 人设已写明"你比团里十三个人都小"），是圈外晚辈，理应对成员称**「前辈」**（默认/礼貌），与男主感情升温后亲密时可自然称**「欧巴」**，不该直呼名字。

---

## 根因（三处规则互相打架，把正确称呼压成了直呼）

| 位置 | 旧行为 | 后果 |
|---|---|---|
| `src/prompts.js` 1v1 全局写作规则 | 强制"直呼名字。好感度高时可称欧巴" | AI 被迫直呼 |
| `src/prompts.js` 哥哥角色段 | "只能直呼名字或好感度高时称欧巴"，无"前辈"选项 | 妹妹无"前辈"可用 |
| `src/validator.js` 全局校验 | 检测"前辈/后辈"即判违规 | 即便 AI 写对也被打回重生成 |
| `src/ui/narrative-box.js` 渲染前处理 | `text.replace(/前辈/g,'').replace(/后辈/g,'')` 直接删词 | 正文"前辈"被硬吃掉 |

---

## 修改清单

### 1. `src/utils.js` — 新增共享判定
- 导出 `isSisterSetting()`：当且仅当 `gameMode==='oneHeart' && worldSetting==='entertainment' && oneHeartRelationCharacter.role==='哥哥'` 时为真。供以下三处复用。

### 2. `src/prompts.js` — 注入妹妹称呼规则
- 顶部新增 `import { isSisterSetting } from './utils.js';`
- 全局写作规则「称呼规则」行：`isSisterSetting()` 为真时改为 **"你是队友的妹妹，比全团十三名成员都小，是圈外晚辈。对成员默认称'前辈'（如'XX前辈'），与男主感情升温、关系亲密后可自然改口称'欧巴'，绝对禁止对成员直呼名字。"**
- 哥哥角色段：妹妹设定下改用 **"默认称前辈、亲密称欧巴、禁止直呼名字"**；非妹妹分支保留原"直呼名字/好感度高称欧巴"。亲哥哥「我哥」/名字豁免保留。

### 3. `src/validator.js` — 放行前辈/欧巴
- 顶部新增 `import { isSisterSetting } from './utils.js';`
- 全局"前辈/后辈"禁令：`!isSisterSetting()` 时才判违规。
- 女主称"哥"违规文案：妹妹设定下改为"应称'前辈'或'欧巴'"。
- 汇总纠正文案：妹妹设定下改为"默认称前辈、亲密称欧巴，禁止直呼名字；禁止'-xi'"。

### 4. `src/ui/narrative-box.js` — 保留正文"前辈"
- 顶部新增 `import { isSisterSetting } from '../utils.js';`
- 删"前辈/后辈"逻辑包在 `if (!isSisterSetting())` 内：妹妹设定下不再删词，正文"前辈"得以显示。

---

## 影响范围 / 防回归
- 仅 `isSisterSetting()` 为真时改变称呼行为；**换乘恋爱模式、1v1 其他身份（站姐/素人/同公司师妹…）、其他世界观完全不受影响**。
- 不动团内相处规则（成员互称"直呼名字"是对队员之间，保留）。
- 不动经典模式 `formatters.js getAddressRules`（仅经典模式使用）。
- 不改任何状态字段、不引入依赖。

---

## 验证方式
1. `npm run build` → EXIT:0 ✅（4 个改动文件 lint 无新增错误）
2. 本地 `npm run dev` → 进 💗只为你心动 → 娱乐圈 → 队友的妹妹：
   - 确认 AI 生成剧情中女主称成员"前辈"、对男主亲密时"欧巴"，不再直呼名字；
   - 确认 validator 不再把"前辈"判违规；
   - 确认正文"前辈"不被删除。
3. 切换 换乘恋爱 / 其他身份，确认称呼规则无变化。

---

## 备注
- 本清单为「2026-07-08」测试问题收集，此条为 #1。后续测试中若再发现其他不好的点，可继续在本文档追加「修改项 #2 / #3 …」。

---

## 修改项 #2：私密体质「选了什么就是什么」

**现象**：女主私密体质为「多选」，自己没勾选「敏感带在耳后」，但 1v1 剧情里出现了"耳后敏感带"描写。

**根因**：`src/prompts.js` 两处（换乘模式 `~138`、1v1 `~981`）把若干特征（含私密体质「敏感带在耳后」）当成女主**固有特征**无条件写死注入，AI 被暗示女主本就有该体质。

**修改清单**：
1. `src/prompts.js` — 新增 `buildFeaturePosNote(hp)`，仅当女主 `appearance` / `privateTraits` 实际包含该特征时才输出其"固定位置"说明；替换上述两处硬编码"注意"串。换乘 / 1v1 同步修复。
2. `src/validator.js` — 导入 `PRIVATE_TRAITS`；新增 `checkPrivateTraitViolation(content)`：正文出现某私密体质名但未在 `GS.heroineProfile.privateTraits` 中时，回写 correction 强制重写（warning 级），引擎带 correction 重试。

**影响范围**：仅约束 AI 对未选体质的编造；已选体质、外貌特征、模板/随机人设均不受影响。

---

## 修改项 #3：1v1 时钟 / 用餐锚定（保持 4 小时跨度）

**现象**：1v1 剧情推进到 16:00 时仍写"吃午饭 / 中午休息"，与时段不符。

**根因**：`ONE_HEART_TIME_PERIODS` 边界（12→下午、16→傍晚、20→深夜）+ 1v1 无"用餐时间锚定"，AI 拿到"12 时"自然写午饭并顺延。

**修改清单**：
1. `src/utils.js` — 调整 `ONE_HEART_TIME_PERIODS` 边界：`上午8-12 / 中午12-13 / 下午13-18 / 傍晚18-21 / 深夜21-24`，使 `+4` 时钟序列正确命中 `8=上午 / 12=中午 / 16=下午 / 20=傍晚`，午饭天然归位。
2. `src/prompts.js` — 新增 `getOneHeartMealHint(clock)`，按 `oneHeartDayClock` 给出"现在该不该写吃饭"的明确指引，并**显式禁止非午饭时段写午饭**；在 1v1 user message `[场景状态]` 后注入 `[用餐锚定]`。
3. `src/validator.js` — 新增 `checkTimeMealMismatch(content)`（仅 `gameMode==='oneHeart'` 生效）：正文出现"午饭/午餐/午休"但时钟不在 `11-13` 时回写 correction 强制重写。

---

## 修改项 #4：情敌事件好感提示错乱（显示层 A + 逻辑层 B）

**现象**：情敌事件选项后，顶部弹出的好感提示不对——明明加的是男主好感，却显示成情敌好感。

**根因**：
- A（显示）：男主好感飘字在 Header `#affectionHint`（顶部），情敌倾向 Toast 走 `showToast`（`top:20px` 居中），两者堆顶部、情敌 Toast 更显眼，被误当好感提示。
- B（逻辑，真 bug）：`applyOneHeartOptionAffection` 中只要选项 `affName` 匹配情敌名即整段 `return` 吞给情敌，男主一分没拿到。匹配过宽（含文案提到情敌名即误判）。

**修改清单**：
1. `src/utils.js` — `showToast(message, pos)` 新增 `pos` 参数，`'bottom'` 时改 `bottom:20px`。
2. `src/affection.js` — `updateRivalTendency` 的 Toast 改 `showToast('【情敌倾向】' + ..., 'bottom')`，与顶部男主飘字位置 + 文案双重区分。
3. `src/game-engine.js` — 移除"情敌匹配即整段 return 吞掉男主好感"逻辑；男主 `affDelta` 始终经 `updateAffection` 结算，仅当选项**显式**带 `rivalAffDelta`（且未转正）才 `updateRivalTendency`。并在 1v1 prompt 引导 AI 用 `rivalAffDelta` 表达情敌取向。

---

## 修改项 #5：聊天记录 content 为对象导致 escHtml 崩溃

**现象**：
```
utils.js:71 Uncaught (in promise) TypeError: s.replace is not a function
    at escHtml (utils.js:71:12)
    at showChatModal (chat-modal.js:28:62)
```

**根因**：1v1 主动消息路径（`game-engine.js`）把 `generateMessage()` 返回的**对象** `{ sender, text }` 直接存进 `chatHistory[*].content`，渲染时 `escHtml(c.content)` 对对象调 `.replace` 崩溃；普通聊天回复存的是字符串故不触发。旧存档已混入对象型条目，打开聊天即崩。

**修改清单**：
1. `src/game-engine.js` — 主动消息写入取 `_msg.text`（支持字符串兜底），非字符串/空则跳过。
2. `src/utils.js` — `escHtml` 增加字符串化兜底：非字符串且真值时 `return String(s)`，杜绝任何对象/数字漏网导致整页崩溃。
3. `src/modals/chat-modal.js` — 渲染前对 `c.content` 做一次 `typeof` 归一（非字符串则 `String` / 跳过），双保险。

---

## 验证方式（#2-#5）
- `npm run build` → EXIT:0 ✅（79 modules transformed；仅 chunk >500kB 提示，非错误）。
- 本地 `npm run dev` 进 💗只为你心动：
  - #2 未选体质不再出现在剧情；validator 能对编造未选体质回写 correction。
  - #3 时钟 16:00 不再写午饭；prompt 注入 `[用餐锚定]`。
  - #4 情敌事件后顶部只弹男主好感飘字、底部弹 `【情敌倾向】` 提示；男主好感正常涨。
  - #5 打开聊天弹窗不再崩溃；旧存档对象型 content 被兜底渲染。
