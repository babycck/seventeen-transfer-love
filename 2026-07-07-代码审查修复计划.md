# 代码审查修复计划（2026-07-07）

> 本计划汇总对《换乘恋爱 × SEVENTEEN》全代码审查（剧情生成 / 记忆压缩 / 任务系统 / 好感度 / 突发剧情与人物 / 跨天清理）发现的待修复项。
> 后续用户将追加更多需求，统一在此文件累积，最后一起改。

## 审查范围
- `src/game-engine.js`、`src/memory.js`、`src/prompts.js`、`src/parser.js`、`src/ai-generator.js`、`src/state.js`
- `src/skeleton/scheduler.js`、`src/skeleton/event-triggers.js`、`src/skeleton/affection-engine.js`、`src/skeleton/drink-engine.js`、`src/skeleton/option-engine.js`
- `src/affection.js`、`src/modals/task-panel.js`、`src/data.js`、`src/utils.js`、`src/portrait.js`

## 严重度说明
- 🔴 高危：影响实际玩法正确性 / 数据持久化 / 可能卡死
- 🟡 中危：逻辑不严谨、语义偏差、规范偏离
- 🟢 低危：健壮性 / 一致性 / 文档偏差

---

## 🔴 高危（建议优先修复）

### BUG-01：`re_10` 随机事件永远不触发
- **位置**：`src/data.js:540`（cond `affection>=30`）、`src/utils.js:83`（`resolveValue('affection')`）
- **原因**：`evaluateCond` 把 `affection` 解析为 `affections['heroine']`，而 `GS.affection` 的 key 是成员 id，不存在 `'heroine'`，恒为 0 → `0>=30` 永远 false。
- **修复（已定，见事件池审查 BUG-23 段）**：不新增 `maxAffection` token（避免扩大 `evaluateCond` 改动面，影响其余事件），直接将 `re_10` 的 cond 改为 `day>=4`，使"撞见旧伤"在进入中段剧情后自然出现。

### BUG-02：普通模式选项好感度即时结算后未持久化（刷新丢分）
- **位置**：`src/game-engine.js:1067-1120`（`triggerAffectionFromChoice` 各分支）
- **原因**：各分支调用 `updateAffection` / `addAffectionLog` 但无 `saveGame()`。普通选项走即时路径，仅在内存；进入下一段（`skeletonSettleAff` 才 saveGame）前刷新/崩溃，本次好感变化丢失。
- **修复状态**：已在当前代码中修复——`handleOptionChoice` 在 `triggerAffectionFromChoice` 之后（约 788 行 `try` 块末尾）已调用 `saveGame()`，好感度即时结算后即持久化；代码审查时的"无 saveGame"描述与当前实现不符，无需改动。

### BUG-03：`triggerAffectionFromChoice` fallback 到 pending 导致重复加分
- **位置**：`src/game-engine.js:1085-1111`
- **原因**：选项无 `affName` 且文本未匹配成员时，从 `GS.pendingAffChanges[0]` 取 `firstChange` 立即 `updateAffection`；下一段 `settlePendingAffChanges` 又结算同一笔 → 同一变化被加两次。
- **修复**：fallback 分支只提示+记录，不从 pending 即时结算；或在消费后从 `pendingAffChanges` 移除该项。

### BUG-04：`rivalryIntensity` 稳态重复累加
- **位置**：`src/skeleton/affection-engine.js:48-57`
- **原因**：只要 `>=2` 人好感≥60 且 `<5` 就每结算一次 `++`；该函数在每进入新段（`game-engine.js:60`）都调用，而普通选项好感不走此函数，导致进入新段时因已满足条件又 `+1`，很快触顶 5。
- **修复（已落地）**：新增状态字段 `rivalryHighActive`（默认 `false`，`state.js` 定义 + `migrateSave` 兜底）。每次 `settlePendingAffChanges` 结算后，仅当 `highAffCount>=2` 且 `rivalryHighActive` 为 `false`（即"从 <2 跃迁到 ≥2"的上升沿）时 `rivalryIntensity++` 并将 `rivalryHighActive` 置 `true`；当 `highAffCount<2` 时复位 `rivalryHighActive=false`，允许后续再次跃迁升温（仍受上限 5 约束）。`rivalryHighActive` 跨天不重置，保证同一"高危期"只 +1。

### BUG-05：`xItemsRevealRound` 跨天未重置，Day4/5 记忆物品轮转错位
- **位置**：`src/skeleton/scheduler.js:7-55`（`resetDayState`），`src/state.js:233` 定义，`src/prompts.js:494-502` 使用
- **原因**：`resetDayState` 未重置 `GS.xItemsRevealRound`；`prompts.js` 用它 `(xItemsRevealRound+1) % (length*2)` 推进，跨天不归零会使 Day5 物品公开 `revealIdx` 基准偏移，导致同一成员物品重复/错乱。
- **修复（已落地，已修正原方案偏差）**：`resetDayState` 中加 `GS.xItemsRevealRound = 0`（恢复每日轮转基准）与 `GS.todayOptionTexts = []`（选项文本去重黑名单按天清空，原本在 `proceedToNextDay` 也清，此处补一致）。**注意**：原计划"补清 `GS.xItemsRevealState = {}`"经代码核实会破坏 Day5 逻辑——`game-engine.js` 在 Day4 将 `xItemsRevealState[mi]='first'`、Day5 才升级为 `'second'`（依赖 Day4 的 `'first'` 标记），而 `resetDayState` 在 `proceedToNextDay` 中于次日 `generatePhaseNarrative` 之前执行；若清空则 Day5 `'second'` 升级断裂。故 `xItemsRevealState` 跨天保留、不在此清空。

### BUG-06：Day11 深夜醉酒分支可能卡死
- **位置**：`src/game-engine.js:460-463`（`generateDrunkNarrative` 结尾）
- **原因**：drunk 剧情结尾依赖 AI 返回的 `options`，若选项文本不含"照顾/阳台/客厅/X"等关键词会落入普通 consequence 分支，`phaseOptionCount` 递增到上限后无"进入深夜"入口 → 玩家卡死。
- **修复（已落地）**：① `generateDrunkNarrative` 与 `generateDrunkConsequence` 在设置 `currentOptions` 时，若 AI 返回选项中不含"进入深夜"文本，则追加固定兜底选项 `▶ 进入深夜`；② 重构 `handleOptionChoice` 的醉酒分支——原先女主醉酒下"不命中 照顾/阳台/客厅/X 关键词"的选项会落入普通 consequence 分支，在 Day11 深夜 `phaseOptionCount` 触顶后无出口而卡死；现改为：任何未命中关键词的醉酒选项统一路由到 `generateDrunkConsequence`，杜绝落入普通分支，且醉酒分支在 `PHASE_ACTION_LIMIT` 检查之前 `return`，永不触发次数上限。点击"进入深夜"由 `handleOptionChoice` 开头的关键词检测直接 `advancePhase` 结束当日。

### BUG-07：`compressTodayForInjection` 每段重复 AI 压缩（token 浪费 + 内容不一致）
- **位置**：`src/game-engine.js:191` + `src/memory.js:119-143`
- **原因**：每次进入时段前都触发完整 AI 摘要；同天多次调用重复烧 token，且失败 fallback（`slice(0,4000)`）与成功覆盖交替，注入内容前后不一致。
- **修复**：加守卫，仅在 `todayFullText` 首次超阈值（`TODAY_TEXT_CAP`）时压缩一次（缓存本次会话结果）。

### BUG-08：`parsed.affChanges` 的 risk 扣分未在 consequence 结算
- **位置**：`src/game-engine.js:735-744`（`handleOptionChoice`）
- **原因**：`parseNarrative` 已提取选项 `affChanges`（含 `riskMember/riskDelta` 风险扣分），但 `handleOptionChoice` 只调 `applyDrinksFromParsed` 和 `triggerAffectionFromChoice`，后者不处理 `riskDelta` → 风险选项扣分被丢弃。
- **修复**：在 `handleOptionChoice` 中显式遍历 `parsed.affChanges` 并 `updateAffection`，或让 `triggerAffectionFromChoice` 覆盖 `riskDelta`。

### BUG-23：秘密任务 6/8 永不触发（事件池审查，🔴 高危）
- **位置**：`src/data.js:476-493`（`SECRET_MISSIONS` 定义 Day 2/5/7/10 共 8 个任务）、`src/utils.js:123-148`（`shouldTriggerSecretMission`）
- **原因**：
  - `var isDatingDay = ([4,5,8,9,10].indexOf(GS.day)>=0); if (isDatingDay) return false;` → Day 5/10（约会日）的秘密任务被排除。
  - `var mandatoryDays = [1,3,6,7,11,12]; if (mandatoryDays.indexOf(GS.day)>=0) return false;` → Day 7 也被排除。
  - 结果：`SECRET_MISSIONS` 8 个任务里**只有 Day 2 的 2 个能触发**，Day 5/7/10 共 6 个为死数据。
- **修复（已与用户确认）**：保留 Day 2 + Day 7，删除 Day 5/10 两块（约会日不放秘密任务，符合"自由约会"设计）；`utils.js` 的 `mandatoryDays` 移除 `7`（Day 7 仅傍晚 `questionBox` 占用，上午空闲可放秘密任务）。删除的 `SECRET_MISSIONS[5]/[10]` 仅定义数据、无运行期引用，删除安全。

---

## 🟡 中危

### BUG-09：`showMissionCardModal` 死代码
- **位置**：`src/game-engine.js:1727`（原定义并 export，全局无调用点）
- **原因**：`proceedToNextDay` 只弹 toast 通知任务卡，独立弹窗永不出现；`modals.js` 也未导出该函数。
- **修复（已落地）**：删除死代码 `showMissionCardModal`（含其 modal-overlay 结构），由 `proceedToNextDay` 的 toast 通知 + 统一任务面板（`doActionTask` / `doInputTask`）承接；原位置保留注释说明删除原因。

### BUG-10：`re_13` 语义偏差（检查女主而非成员）
- **位置**：`src/data.js:543`（原 cond `drinkCount>=1`）、`src/utils.js:84`
- **原因**：`resolveValue('drinkCount')` 返回 `GS.drinkCounts['heroine']`（女主），但文案写"某成员喝到微醺"。
- **修复（已落地）**：`utils.js` 的 `resolveValue` 新增 `memberDrinkCount` token——遍历 `GS.selectedMembers` 取 `GS.drinkCounts[id]` 最大值；`re_13` 的 cond 由 `drinkCount>=1` 改为 `memberDrinkCount>=1`，正确反映"某成员微醺"。

### BUG-11：`MEMBERS.find(...)` 无 undefined 容错，可能中断当日生成
- **位置**：`src/skeleton/event-triggers.js:27,68,84,116`
- **原因**：极端情况下 `MEMBERS.find` 返回 undefined，可在 `generatePhaseNarrative` 的 try 块内抛错中断当日剧情生成。
- **修复（已落地）**：① `checkJealousyMissionTrigger` / `checkExMessageTrigger` 的 `find` 后加 `if (!targetMember) return false;`；② `handleExMessageChoice` 的 `find` 后加 `if (!member) return;`；③ `checkJealousyEvent` 的 `map` 链追加 `.filter(function(m){return !!m;})`，剔除 undefined 后再访问 `.name`，避免 `members[i].name` 抛错。

### BUG-12：好感度疲劳值无上限
- **位置**：`src/skeleton/affection-engine.js:38`
- **原因**：`affFatigue[memberId] = fatigue + 1` 无限增长，与 SEVENTEEN.txt"上限 5"不符；`Math.floor(fatigue/2)` 随值增大过度抑制好感。
- **修复（已落地）**：`GS.affFatigue[ch.memberId] = Math.min(5, fatigue + 1)`；封顶后不再增长衰减量，新目标切回 0 的逻辑保持不变。

### BUG-13：好感度下限双标
- **位置**：`src/affection.js:10`（全局 `-100`）vs `src/game-engine.js:1181`（1v1 模式 clamp 到 `-20`）
- **修复（已落地）**：`affection.js` 导出统一常量 `AFFECTION_MIN=-100` / `AFFECTION_MAX=100`，`updateAffection` 改用之；`game-engine.js` 的 `applyOneHeartOptionAffection` 删除 `-20` 二次 clamp，1v1 与 transfer 共用 `updateAffection` 单一下限，消除双标。新增 `_rivalSwitched` 等无关逻辑未触碰。

### BUG-14：`prompts.js` 违反"禁模板字符串"规范
- **原诊断**：`src/prompts.js` 通篇使用反引号模板字面量，与 AGENTS.md「`+` 拼接、禁模板字符串」规范偏离。
- **核查结论（2026-07-07）**：**该诊断不成立，无需修改。** 经实测：
  - `src/prompts.js` 反引号数量 = **0**，全文已统一使用单引号字符串 + `+` 拼接（如 `var result = '你是…' + '\n\n' + ...`）。
  - 全 `src/` 搜索 `${`（模板插值特征）= **0**；所有被正则误匹配到的反引号，均来自"匹配 markdown ```` ``` ```` 代码围栏"的正则/字符串字面量（如 `result.match(/```(?:json)?\s*([\s\S]*?)```/)`、`t.indexOf('```')` 等），**并非模板字面量**。
  - 即整个代码库当前并不存在任何"模板字面量"用法，规范已实质满足。
- **性质**：与 BUG-02 同类——计划描述基于更早的代码状态，当前实现已合规。原计划"独立批次改写 prompts.js 模板字符串"的前提消失，**作废**。
- **修复状态**：**已核实·无需修改（no-op）**。若未来新增代码，继续遵守 `+` 拼接约定即可。

### BUG-15：`portrait.js` 立绘函数全项目无调用点
- **位置**：`src/portrait.js` 全文件（`getMemberPortrait` / `renderMemberPortrait` 等）
- **原因**：疑似未启用/死代码；`getMemberPortrait` 无 `memberId` 空值防御。
- **修复（已落地，防御先行）**：`getMemberPortrait` / `getMemberAvatar` 均加 `if (!memberId) return ''` 空值防御（下游 `renderMemberPortrait`/`renderMemberAvatar` 内部依赖这两个函数，防御自动级联）。未启用功能保持原样，待确认是否启用；启用前已不缺失防御。

---

## 🟢 低危 / 健壮性

- **BUG-16**：`migrateSave` 仅在 `version!=='v22'` 时兜底（`src/state.js:311` 外层 `if`），v22 存档缺新字段不补。
  - **修复（已落地）**：在 `migrateSave` 的 version 分支**之外**、函数返回前，新增无条件兜底段——遍历 `defaultGameState()` 的所有顶层 key，凡 `GS[key] === undefined` 的均补回默认值（不覆盖已存在值）。无论 v21/v22/其他，缺字段一律补回，杜绝运行期读取 undefined 崩溃。
- **BUG-17**：`updateAffection` 对 `delta` 无 `Number.isFinite` 防御（`src/affection.js:7`），`NaN` 会污染存档。
  - **修复（已落地）**：函数开头加 `if (!Number.isFinite(delta)) { console.warn(...); return; }`，非有限数值直接忽略，不写入 `GS.affection`。
- **BUG-18**：`evaluateCond` 对未知字段名静默 `result=true`（`src/utils.js:70-71`），可能掩盖条件笔误。
  - **修复（已落地）**：未知字段/无运算符匹配时 `console.warn('[evaluateCond] 未知条件字段或格式...')` 并 `result = false`（fail-closed，按"不满足"处理），不再静默放行；现有事件均使用已知 token，不受影响。
- **BUG-19**：`parseNarrative` 思考泄漏拦截关键词过严（`src/parser.js:270-296`），"我决定""这段剧情"等正常叙事也出现，可能误触发重试。
  - **修复（已落地）**：从 `reasoningKeywords` 移除 4 个易与正常叙事混淆的词（'我决定'/'这段剧情'/'我没想到'/'不太合适'），保留明显推理标记（'generateJSON'/'blocks[0]'/'affName'/'riskMember' 等）+ 既有结构特征启发式（含'我'、>8 行、带字段名），降低误判。
- **BUG-20**：`re_8` 的 `phase==evening` 依赖调用方传字符串（`src/utils.js:80`），改数字索引会静默失效。
  - **修复（已落地）**：`resolveValue` 的 `phase` 分支增加数字索引映射——若 `phase` 为 number 且 `PHASES[phase]` 存在则映射到时段字符串，否则原样返回；兼容字符串与数字两种传参。
- **BUG-21**：`drinkCounts` 每日清零（`src/skeleton/scheduler.js:43`），随机喝酒事件只能判当天，跨天醉酒经历无法体现。
  - **修复（已落地）**：在清空处补注释明确语义——`drinkCounts` 为按天计数，`re_13`（现 `memberDrinkCount>=1`）仅当日本轮有人喝酒时触发，代表"当日微醺"而非历史累计；如需累计应另立字段。
- **BUG-22**：AGENTS.md 称"条件只支持 `==` 和 `>=`"，代码实际还支持 `&& < > <=`，文档与实现偏差。
  - **修复（已落地）**：同步 AGENTS.md 第 191 行，列出实际支持的运算符（`== >= <= > <` + 多条件 `&&`）与可用字段名（day/phase/season/weather/affection/drinkCount/memberDrinkCount），并注明不支持 `||`。
- **BUG-24（事件池审查·🟢 低危）**：随机事件池标注与 id 跳号。
  - `src/data.js:526` 注释"随机日常事件池（15个）"实为 16 个；`src/data.js:538` 注释"情感冲击 6个"实为 re_9~re_13 共 5 个 → 修正注释计数。
  - id 跳号：re_13 直接到 re_15（缺 re_14）。`re_15`→`re_14`、`re_16`→`re_15` 重编号（已核实无外部引用，仅 `data.js` 与 `app.js.backup` 出现）。
- **BUG-25（事件池审查·🟢 低危）**：`re_2`「空调坏了」仅覆盖夏/冬。
  - `re_2_summer`/`re_2_winter` 用 `season==summer/winter`，春/秋该事件永不触发 → 补充 `re_2_spring`（空调时好时坏）/ `re_2_autumn`（秋燥静电/空调调试），与既有分支并列，使"空调坏了"全年可触发。
- **BUG-26（事件池审查·🟢 低危）**：随机/秘密任务互斥不对称。
  - `shouldTriggerSecretMission` 单向依赖 `todayRandomEventTriggered`，但随机事件可在任意时段触发、秘密任务只在上午；若上午随机事件先占则秘密任务被压。修复后 Day 2 为唯一秘密任务日（morning 先于随机事件判定，不会被抢占），影响有限，可顺手在互斥守卫上双向检查，非阻塞。

---

## 事件系统与数值平衡改进（设计层，非 BUG）

> 以下问题不影响"能不能跑"，但影响"玩起来对不对味"。多位触发条件绑死在绝对高数值上，数值一旦被平衡压低就会哑火；事件往 prompt 灌指令也缺乏优先级仲裁。

### IMP-01：特殊事件同相位指令碰撞（🟡 中/高）— ✅ 已落地
- **位置**：`src/prompts.js`（`buildUserMessage` 内各 `[INSTRUCTION]` 注入点）、`src/skeleton/event-triggers.js`
- **现象**：`exMessage`（D5-9 傍晚/深夜）、`jealousyMission`（可落 D6/D7）、`midnightCall`（D6 phase3）、`questionBox`（D7 phase2）都可能在**同一段**同时往 `buildUserMessage` 灌 `[INSTRUCTION]`，且没有任何"每相位只让一个主事件"的守卫。AI 会拿到互相打架的指令，质量下降、容易丢事件。
- **修复**：新增相位级事件仲裁器 `activeEventThisPhase`（优先级如 提问箱/午夜电话 > 嫉妒任务 > 前任来电），每段只放行一个主导事件指令，其余顺延或静默跳过。
- **落地（2026-07-07）**：
  - `src/skeleton/event-triggers.js` 新增导出函数 `activeEventThisPhase()`：相位锁死强制事件（`questionBox` D7 phase2 / `truthPunishment` D11 phase3）最高优先；多相位事件按 `jealousyMission > exMessage > xGhostEvent` 择一。返回主导事件 id。
  - `src/prompts.js` 顶部 `import { activeEventThisPhase } from './skeleton/event-triggers.js'`，在事件注入段前计算 `var dominantEvent = activeEventThisPhase()`，并将 `嫉妒任务 / X幽灵 / 前任来电 / 真心话惩罚 / Day7提问箱` 五处 `[INSTRUCTION]` 注入块加 `dominantEvent === '...'` 门控。
  - **说明**：`midnightCall` 经核查为玩家手动触发弹窗（D6 phase3 按钮 → 独立 consequence 生成），并不经 `buildUserMessage` 注入 `[INSTRUCTION]`，故不存在提示级碰撞，未纳入仲裁（IMP-04 互斥以触发层处理）。
  - 非主导事件保留在 GS，后续相位若仍 active 会自动顺延注入，不会丢失。

### IMP-02：修罗场 `rivalryIntensity` 改相对阈值（🟡 中）
- **位置**：`src/skeleton/affection-engine.js:48-57`（与 BUG-04 配套）
- **现象**：BUG-04 只修了"稳态重复累加"，但触发阈值仍是绝对高值「`>=2` 人好感 `>=60` 才 `+1`」。若数值整体被平衡压到 40-55 的"温和带"，修罗场几乎永远不升温。
- **修复**：在 BUG-04"仅跃迁瞬间 +1"的基础上，把判定从绝对阈值改为**相对判定**——「好感最高的两位差距 `≤15` **且**两者都 `>=40`」。即"只要在争、且都够格"就升温，和数值通胀脱钩。上限 5 不变。

### IMP-03：吃醋事件 `checkJealousyEvent` 改相对判定（🟡 中）
- **位置**：`src/skeleton/event-triggers.js:133`（观察者 `aff < 50` 直接跳过）
- **现象**：观察者阈值写死 50，数值温和时同样哑火；且低好感者可能"无故吃醋"动机不足。
- **修复**：观察者阈值从 `50` 降到 `40`，并加一条「观察者不能是三人里好感最低的」（即他得有真实敌意动机）。

### IMP-04：嫉妒任务日程收敛（🟡 中）
- **位置**：`src/skeleton/event-triggers.js:13`（`checkJealousyMissionTrigger`）、`src/prompts.js`
- **现象**：排除日 `[1,3,9,11,12]` + 约会日 `[4,5,8,9,10]`，实际只落 D2/6/7。D2 过早（好感未起），真正该有的"三角升温"集中在 D6/D7，恰好和午夜电话/提问箱撞车（参见 IMP-01）。
- **修复**：把嫉妒任务限制在 **D6-10** 且**强制与 `midnightCall`/`questionBox` 互斥**（即 `activeEventThisPhase` 仲裁中嫉妒任务优先级低于那两者）。

### IMP-05：1v1 随机事件"抽法太笨"（🟡 中）— ✅ 已落地
- **位置**：`src/game-engine.js`（`generateOneHeartRound` 内随机事件抽取）、`src/data.js`（`ONE_HEART_RANDOM_EVENTS` 分组）、`src/state.js`（`oneHeartRandomUsed`）、`src/ui-renderer.js` 触发入口
- **现象**：1v1 随机事件仅在玩家点「🎲 随机事件」时手动触发（无自动，设计合理），但抽取存在三问题：① 纯均匀随机、无视阶段；② 无去重/无"近期用过"保护；③ `ONE_HEART_RANDOM_EVENTS` 的主题分组目前代码完全没用到，被浪费。
- **修复（2026-07-07）**：
  - `src/game-engine.js` 新增 `getOneHeartRelationshipStage(aff)`（初识<20 / 暧昧20-39 / 热恋40-59 / 深恋60+）与 `pickOneHeartRandomEvent()`：按阶段从 `ONE_HEART_RANDOM_EVENTS` 对应索引区间抽取（暧昧升温0-21 / 日常甜暖22-37 / 情感冲击38-49 / 戏剧冲突50-59），保证不串味。
  - `GS.oneHeartRandomUsed` 环形缓冲（保留最近 10 个 id），抽过的近期不再抽；池内全抽过时自动重置。
  - `state.js` 新增 `oneHeartRandomUsed: []` 字段 + `migrateSave` 兜底。
  - **兜底确认**：`generateOneHeartRound(extra)` 已在第 1821 行 `extra = extra || {}`，`ui-renderer.js` 无参调用 `generateOneHeartRound()` 不会抛错——该隐患经核查已不存在，无需额外改动。

### IMP-06：任务卡"有发无收"回填（🟢 低，已纳入任务卡方案）— ✅ 已关联
- **说明**：当前任务卡只存进 `GS.todayMissionCard`，无完成入口回流剧情。该缺口已由独立的"统一任务面板"方案覆盖（行动类即时生成 / 输入类 `pendingTaskResult` 注入），此处仅作关联记录，不重复列项。**本次审查无独立代码改动。**

### IMP-07：任务卡方案文档修正（🟢 低）— ✅ 已记录
- **说明**：独立任务卡计划里写"新增 `cleanupMissionCard()`"，但该函数**已存在于 `src/skeleton/scheduler.js:191`** 且逻辑合理（写历史+清空+清 `pendingTaskResult`）。计划中"resetDayState 加 `todayMissionCard=null`"是**冗余但无害**（因 `proceedToNextDay` 已调 `cleanupMissionCard`）。执行任务卡方案时删掉该冗余句，避免混淆。**本次审查无代码改动，仅在此标注供后续执行任务卡方案时参考。**

### IMP-08：记忆系统优化（⏳ 占位，细节待确认）— ⏸️ 待用户补充
- **说明**：前序对话提及"记忆系统优化"需一并规划，但本次审查未展开具体项。待用户补充细节后填入（可能涉及 `src/memory.js` 压缩/回顾逻辑）。**本次未实施。**

### IMP-09：女主人格画像（Persona）（🟡 中，低投入高回报）— ✅ 已落地
- **位置**：`src/state.js`（新增 `heroinePersona` 字段）、`src/game-engine.js`（`triggerAffectionFromChoice`/`handleFreeAction` 推断通道）、`src/prompts.js`
- **现象**：现在选择只影响成员好感，但"女主是什么样的人"没有积累，导致每局体验雷同、重玩价值受限。
- **设计**：暗藏 `heroinePersona`（如 撒娇型/独立型/撩拨型/迟钝型），由选项文本与自由输入关键词推断（复用 `triggerAffectionFromChoice` 的现有文本匹配通道）。成员后续反应据此微调——同一句台词，对"迟钝型"成员更主动、对"撩拨型"反被撩。
- **落地（2026-07-07）**：
  - `state.js` 新增 `heroinePersona: { scores:{}, current:'' }` + `migrateSave` 兜底（scores/current 均容错初始化）。
  - `game-engine.js` 新增 `PERSONA_KEYWORDS`/`PERSONA_LABELS` 与 `updateHeroinePersona(text)`：按关键词累加四类人格分（每句同型最多+1 防刷分），取最高分为 `current` 标签；在 `triggerAffectionFromChoice(choiceText)` 与 `handleFreeAction(actionText)` 入口调用。
  - `prompts.js` 在 transfer 分支（私密体质提醒后）与 1v1 `buildOneHeartUserMessage` 均注入 `[INSTRUCTION] 女主人格画像` 指令，要求 AI 自然体现、不对成员映射、呼应性格底色。
  - 纯状态累加，零额外 AI 成本，transfer 与 1v1 均生效。

### IMP-10：谎言 / 脚踏两条船系统（🟡 中）— ✅ 已落地
- **位置**：`src/state.js`（新增 `secretDating` 状态）、`src/skeleton/event-triggers.js`（`updateSecretDating`）、`src/skeleton/affection-engine.js` / `src/game-engine.js`（结算后刷新）、`src/prompts.js`（升级指令）
- **设计**：换乘模式核心张力是"秘密前任 X + 现任候补"。新增 `secretDating`：若同时与 `>=2` 人好感 `>=50` 且未公开，则进入"被撞见"高风险期。
- **表现**：`questionBox`（D7）升级为"当场拆穿"剧本（尖锐指向周旋两人）；`buildUserMessage` 注入通用高风险期紧张感指令。玩家在「隐瞒 / 摊牌」间博弈，放大修罗场爽点。
- **落地（2026-07-07）**：
  - `state.js` 新增 `secretDating: false` + `migrateSave` 兜底。
  - `event-triggers.js` 新增 `updateSecretDating()`：统计非 X 成员中好感 `>=50` 的人数，`>=2` 置 `true`；在 `settlePendingAffChanges`（affection-engine，结算末尾）与 `applyParsedSideEffects`（game-engine）调用。
  - `prompts.js` transfer 分支：修罗场强度块后新增 `[INSTRUCTION] 脚踏两条船高风险期` 通用指令；`questionBox` 块在 `secretDating` 时追加尖锐问题指向。
  - 注：`midnightCall` 为玩家手动弹窗（非 prompt 注入），其"拆穿"由玩家选择驱动，未做强制升级，符合 IMP-01 结论。

### IMP-11：成员间关系网（🟡 中）— ✅ 已落地
- **位置**：`src/state.js`（`memberBond`）、`src/skeleton/event-triggers.js`（`updateMemberBond` + 吃醋/任务埋点）、`src/skeleton/affection-engine.js`（修罗场升温恶化）、`src/prompts.js`（关系网注入）
- **现象**：现在成员只和女主互动，A 吃 B 醋只针对女主，缺乏成员间张力。
- **设计**：新增 `memberBond[A][B]`（成员彼此好感/敌意），A 吃 B 醋时不仅针对女主，还会与 B 暗中较劲或结盟。修罗场从"三角"升级为"关系网"，戏剧性指数上升。
- **落地（2026-07-07）**：
  - `state.js` 新增 `memberBond: {}` + `migrateSave` 兜底。
  - `event-triggers.js` 新增 `updateMemberBond(idA,idB,delta)`（key 按 id 字典序拼 `|`，值夹在 [-5,5]）；在 `checkJealousyEvent` 提交时：吃醋者对被选者 `-2`、向第三方 `+1`，并埋 IMP-12 延迟种子；在 `checkJealousyMissionTrigger` 对"被排除者"埋延迟种子。
  - `affection-engine.js` 修罗场"跃迁升温"时，对好感最高的两位调用 `updateMemberBond(-1)` 恶化竞争关系（导入 `updateMemberBond`）。
  - `prompts.js` transfer 分支在关系网块注入 `[INSTRUCTION] 成员间关系网`，列出各对"敌对/同盟/微妙"状态，要求群戏自然体现。

### IMP-12：抉择代价链（长线后果）（🟡 中）— ✅ 已落地
- **位置**：`src/state.js`（`futureConsequences` 队列）、`src/skeleton/event-triggers.js`（`plantFutureConsequence` + 埋点）、`src/prompts.js`（到期注入）
- **现象**：当前 `rivalryIntensity` 等均为即时结算，缺乏"延迟爆雷"的悬念感。
- **设计**：部分关键选择埋 `futureConsequence` flag，3-5 天后才爆发（如今天帮 A 怼了 B，Day9 B 借故冷落你）。形成长线因果链，让早期选择在后期持续回响。
- **落地（2026-07-07）**：
  - `state.js` 新增 `futureConsequences: []` + `migrateSave` 兜底。
  - `event-triggers.js` 新增 `plantFutureConsequence(type,text,minDelay,maxDelay)`（内部 `randIntSafe`）与 `randIntSafe` 工具；在 `checkJealousyEvent` 提交时埋"观察者几天后冷落/试探"种子；在 `checkJealousyMissionTrigger` 对"被排除者"埋"若即若离/较劲"种子。
  - `prompts.js` transfer 分支新增 `[INSTRUCTION] 延迟爆发的后果`：每轮检查 `dueDay <= GS.day && !triggered` 的条目，注入一次后标记 `triggered`（每日仅触发一次），形成长线因果回响。

### IMP-13：情绪状态链（🟡 中，沉浸质量核心）— ✅ 已落地
- **位置**：`src/state.js`（`mood`）、`src/skeleton/mood-engine.js`（新文件）、`src/skeleton/affection-engine.js`（结算驱动）、`src/skeleton/event-triggers.js`（事件驱动）、`src/game-engine.js`（`proceedToNextDay` 衰减 + `triggerAffectionFromChoice` 未选中漂移）、`src/prompts.js`（注入）
- **落地（2026-07-07，务实调整）**：原计划在 `applyParsedSideEffects` 后读 `parsed.affName/affDelta/riskMember/riskDelta`，但经核查 `parser.js` 这些字段是**逐选项**（`parsed.options[i]`）而非顶层，且调用路径未必对应"被选中项"。故改为锚定**好感度实际结算点**：① `settlePendingAffChanges` 逐笔 `updateMoodFromAffChange`（>0→happy / <0→cold）；② `triggerAffectionFromChoice` 三处"未选中者" `driftMood(...,'jealous')`；③ 事件：`checkJealousyEvent` 观察者置 `jealous`(int2)、`handleExMessageChoice` ignore→cold / comfort→moved。新增 `mood-engine.js`（`setMood`/`setMoodByEvent`含 `syncRivalryFromMood`≥2人负面情绪时 `rivalryIntensity++`/`driftMood`/`decayMoodDaily`/`buildMoodInstruction`）；`state.js` 加 `mood:{}`+兜底；`proceedToNextDay` 调 `decayMoodDaily`；`prompts.js` transfer 分支注入「当前心境(强制遵循)」块。**约束**：零 UI/后端、不碰 1v1、文本经 `escHtml`。
- **现象**：现有角色状态只有 `affection[成员]`（一个数字）和全局 `rivalryIntensity`，**没有"某成员此刻心情"的状态**。每段剧情 AI 都是重新生成，虽收到记忆注入（`getLayeredHistory`），但无"珉奎现在正因为你刚选了圆佑而吃醋"的显式信号。结果：phase 2 冷落了珉奎 → phase 3 珉奎突然甜到发慌、毫无铺垫，瞬间出戏。
- **设计**：
  - **数据模型**：`GS.mood = { memberId: { state, cause, since, intensity } }`。`state` 取离散值：`calm`(平静)/`happy`(开心)/`touched`(心动)/`jealous`(吃醋)/`cold`(冷淡)/`angry`(生气)/`moved`(感动)。`cause` 记录原因（让 AI 反应具体不套路），`intensity` 1-3 表程度。
  - **更新通道**：复用现有副作用解析——在 `applyParsedSideEffects(parsed)` 之后挂 `updateMoodFromParsed(parsed)`，直接读 `affName/affDelta/riskMember/riskDelta`：
    - 选项 `affName=A && affDelta>0` → A 置 `happy`/`touched`（cause=本次选择）
    - 选项 `riskMember=B && riskDelta<0` → B 置 `jealous`/`cold`（cause=被比下去）
    - 一轮中**未被选中的在场成员** → 轻微向 `jealous`/`cold` 漂移（程度 +1，有上限）
  - **事件联动**：`checkJealousyEvent` 触发→观察者置 `jealous`（cause 写具体）；`midnightCall`/`questionBox` 拆穿→涉及者置 `angry`/`cold`；安慰类（`comfort`）→置 `moved`。
  - **衰减**：`intensity` 每天 `decayMoodDaily()` 自动 -1，归零回 `calm`，避免吃醋卡死一整天。
  - **与修罗场联动**：≥2 人同时 `jealous` 时 `rivalryIntensity` 自然升温（与 IMP-02 相对阈值互补）。
  - **和解循环**：`cold`/`angry` 不会自动好，必须后续主动选"哄他/解释"类选项才解除 → 关系像"挣来的"（复用 IMP 第三批"矛盾-和解"思路，由 mood 驱动）。
- **注入（让 AI 不瞎写）**：`buildUserMessage` 从 `GS.mood` 拼"当前心境"块 + 强制规则：
  ```
  ## 当前心境(强制遵循)
  - 珉奎：吃醋(intensity 2) —— 你连续两轮选了圆佑，他在意且不服
  - 圆佑：开心(intensity 1) —— 你刚才选了他
  - 知勋：平静
  规则：reaction 必须与该心境吻合。情绪转变必须有铺垫，禁止无理由变脸。
        例：标"吃醋"者本时段应表现出试探/阴阳怪气/刻意靠近，而非突然热络。
  ```
- **落地文件**：
  | 文件 | 改动 |
  |---|---|
  | `state.js` | `defaultGameState` 加 `mood: {}`；`migrateSave` 兜底 `if (GS.mood === undefined) GS.mood = {}` |
  | `src/skeleton/mood-engine.js` | **新增** `updateMoodFromParsed()` / `decayMoodDaily()` / `setMood()` |
  | `game-engine.js` | `applyParsedSideEffects` 后调 `updateMoodFromParsed`；事件触发器内 `setMood`；`proceedToNextDay` 调 `decayMoodDaily` |
  | `prompts.js` | `buildUserMessage` 注入"当前心境"块 + 强制连贯规则 |
  | `affection-engine.js` | 结算时好感骤降联动置 `cold`（可选） |
- **约束**：全部状态 + prompt 层改动，**零新增 UI、零后端、不碰 1v1 模式**（mood 只在 `transfer` 分支维护）；`GS` 引用用 `Object.assign` 维护；AI 文本注入前仍走 `escHtml`。
- **核心一句话**：好感度是"分数"，情绪状态链是"他现在心里什么滋味"——后者才是让角色像个真人、让你出不来戏的那块。

### IMP-14：连续性硬事实锚（🟡 中，沉浸保底）— ✅ 已落地
- **位置**：`src/prompts.js`（`buildUserMessage` transfer 分支末尾注入"不可推翻的事实"块）
- **落地（2026-07-07）**：纯 prompt 层，零新状态字段。在 `buildUserMessage` 末尾注入 `[INSTRUCTION] 不可推翻的事实`：天气(`GS.weather`)/季节(`GS.season`)/天数(`GS.day`)/在场成员(`GS.selectedMembers` 名)/已定论(`GS.consequenceNarratives` 末条 `choiceText` 回退 `GS.pendingChoiceText`)，并附"禁止改写前面已发生的事"规则。与 IMP-15 共用"当前心境"上下文。
- **现象**：AI 每轮重新生成，虽收到 `getLayeredHistory` 叙述性记忆，但无"不可推翻"的硬约束。结果可能自己改设定：天气晴变雨、不在场的成员被写进对话、时间线乱跳。老玩家一眼发现矛盾，瞬间出戏——比"角色没特色"更致命，破坏的是世界观可信度。
- **设计**：
  - 在 `buildUserMessage` 末尾拼一小块"已确立硬事实"，每轮注入 + "禁止推翻"规则：
    ```
    ## 不可推翻的事实(你必须遵守)
    - 今天天气：<GS.weather> / 季节：<GS.season> / 游戏第 <GS.day> 天
    - 在场成员：<当前在场者列表>
    - 已定论：<当轮 choiceText 反映的刚发生事件>
    规则：禁止修改上述天气/日期/在场者/已定论事件。需要演进时只能在
          新写剧情里自然推进，不能"改写"前面已发生的事。
    ```
  - **数据来源**：天气/季节/天数直接用现有 `GS.weather`/`GS.season`/`GS.day`；在场成员从当前 phase 在场者列表取；已定论事件取最近一次 `choiceText`（你刚做的选择）。
  - **与现有"禁止复述"互补**：原指令说"别重复"，本指令说"别推翻"——一个防冗余，一个防矛盾。
  - **可选增强**：玩家在自由输入里"确立"的强事实（如"我老家在xx""我怕黑"）可缓存进 `GS.playerFacts`（IMP 后续），之后既进硬事实锚、又供角色主动呼应，构成"他真的懂我"的来源。
- **约束**：纯 prompt 层改动，零新状态字段（除非启用 playerFacts 增强）、零 UI、零后端、不碰 1v1 模式（transfer 分支）；与 IMP-15 共用"当前心境"上下文。
- **核心一句话**：叙述性记忆让 AI"记得发生了什么"，硬事实锚让 AI"不敢改已经定下的事"——后者灭掉最致命的崩塌类出戏。

### IMP-15：三人差异化反应（🟡 中，修罗场密度）— ✅ 已落地
- **位置**：`src/prompts.js`（`buildUserMessage` transfer 分支，共享事件相位注入）
- **落地（2026-07-07）**：纯 prompt 层，零状态。仅在共享事件相位（嫉妒任务/前任来电/X幽灵/提问箱/午夜电话/`rivalryIntensity>=2`）且 `selectedMembers>=2` 时，注入 `[INSTRUCTION] 三人反应差异化(强制)`：按各成员 `personality` 标签列出三人应写出音色迥异的反应，禁止"同构(只换名字)"，要求体现各自性格 + 当前心境（与 IMP-13 `GS.mood` 双约束）。
- **现象**：一个共享事件（制作组宣布规则、前任消息被大家看到等）发生时，三位成员容易写出**同构变体**——只换名字和口头禅、反应骨架一模一样（都是"先惊讶、再温柔安慰"）。读起来像一个人戴三张面具，修罗场的"网"就散了。
- **设计**：
  - 在共享事件注入处加强制指令：
    ```
    ## 三人反应差异化(强制)
    以下事件三人都会知道，但必须写出三种音色迥异的反应：
    - <成员A 性格标签>：应<该性格下的典型反应方式>
    - <成员B>：应<...>
    - <成员C>：应<...>
    禁止三人给出"同构"反应(只换名字的版本)。每人反应必须体现各自
    性格 + 当前心境(见"当前心境"块)。
    ```
  - **双约束来源**：成员性格用已有 `personality`/`secondCareer` 数据；"当前心境"直接用 IMP-13 的 `GS.mood`——三人反应 = 性格 × 当前心境，双约束下几乎不可能写出同构。
  - **触发范围**：仅在确实存在共享事件（≥2 成员在场且事件公开）时注入，避免对私人戏份过度约束。
- **约束**：纯 prompt 层改动，零状态、零 UI、零后端、不碰 1v1 模式；与 IMP-13/IMP-14 同一注入块，互不冲突。
- **核心一句话**：差异化反应让"三个人"真成三个人，修罗场才有网的密度，而不是一个灵魂轮换三张脸。

### IMP-16：艺人行程 / 探班（1v1·娱乐圈世界观专属）— ✅ 已落地
- **位置**：`src/worlds/entertainment.js`（`ENT_SCHEDULE_POOL` 偶像本职活动池）、`src/state.js`（`GS.oneHeartSchedule`/`GS.visitLog` + migrate 兜底）、`src/game-engine.js`（`generateOneHeartSchedule()`/`doVisitMember(roleKey)`）、`src/prompts.js`（`buildOneHeartUserMessage` 加 `visitSet` 分支）、`src/ui-renderer.js`（娱乐圈下渲染行程条 + 探班按钮）
- **范围**：仅 `GS.gameMode === 'oneHeart'` 且 `GS.worldSetting === 'entertainment'`。其他 1v1 世界观（校园/办公室/邻居）及 transfer 模式完全不触发。
- **角色设定纠正**：娱乐圈世界观下，男主/关系户/情敌三者**都是 SEVENTEEN 成员**（`oneHeartMember`/`oneHeartRelationCharacter.memberId`/`oneHeartRival.memberId`，均取自 `MEMBERS` 池），**没有第二职业**。因此日程素材**不依赖 `secondCareer`**，统一用「偶像本职活动」池。
- **现象**：1v1 娱乐圈核心是「他跑行程的间隙抽空见你、每次见面都要刻意安排」（`entertainment.js` coreTension 第 5 条）。当前 1v1 无行程系统，这段张力完全没被激活——错过最贴世界观的沉浸金矿。
- **设计**：
  - **日程素材**：`ENT_SCHEDULE_POOL`（偶像本职，**非** secondCareer，池越大越不容易重复感），按类分组、可随机抽：
    - **录音 / 音乐类**：录音室录新歌 demo / 主打歌 vocal 录制 / 编曲后期混音 / 配唱指导 / 自作曲 demo 试听会
    - **舞蹈 / 舞台类**：练习室编舞排练 / 打歌舞台彩排 / 演唱会彩排 / 舞蹈教室授课 / 齐舞录制（练习室公开直播）
    - **综艺 / 访谈类**：综艺节目录制（棚内） / 综艺外景录制 / 电台 DJ 嘉宾 / 深夜电台直播 / 访谈节目嘉宾 / 主持人特别出演
    - **拍摄 / 时尚类**：杂志画报拍摄 / 杂志封面拍摄 / 私服画报 / 品牌代言广告片拍摄 / MV 拍摄 / 综艺宣传写真
    - **粉丝向活动**：粉丝见面会 / 签售会 / 生日粉丝会 / 应援视频录制 / 直播带货（品牌合作） / 线上视频通话粉丝会
    - **影视 / 跨界类**：剧组客串拍摄 / 剧本围读或试镜 / 跨界合作会议（音乐×时尚） / 颁奖礼彩排 / 颁奖礼红毯
    - **行程类**：海外行程出发（机场） / 海外打歌或巡演 / 品牌活动出席（公开活动）
    三人共用同一池，按 `memberId` 落到各自名字 + 时间窗口错开；同一天尽量抽不同类型避免三人同框。
  - **每日生成**：开局 / `goToNextDay` 调 `generateOneHeartSchedule()` 产出 `GS.oneHeartSchedule = { main:{role,timeOfDay,place,task,visited,visitWindow}, related:{...}, rival:{...} }`，三人窗口错开逼选其一。
  - **时间轴**：复用现有 `GS.oneHeartTimeOfDay`（上午/午后/傍晚/深夜），工作 block 落午后~傍晚。
  - **探班机制（时间锁）**：上午可正常互动（相处类 notice 可点）；到工作 block 岔路给「探男主 / 探关系户 / 探情敌 / 都不去」。选谁 → `oneHeartTimeOfDay` 跳到他那的工作时段，生成 `visitSet` 场景；**一旦选探 A，上午没做的 notice 全锁灰，B/C 探班按钮变灰**（时间已过，今天把时间给了谁就是谁）；都不去 → 三人外出工作、一段独处戏，世界照常转。
  - **探班呈现（visitSet，三人三种味道）**：探男主=职场反差（冷脸专注→你出现偷喜）× 地下恋风险（怕被工作人员/狗仔撞见）；探关系户=关系张力（关系网角色身份因设定而异，如本角色「队友的妹妹」下即亲哥，男主在意会误会）；探情敌=修罗场直球，调 `updateRivalTendency(+, '你主动探情敌')` 男主后续吃醋。场景内可送咖啡/带饭（复用送礼）、聊天（复用聊天）。
  - **探班好感（待定点①）**：探谁只给谁 `+`；另两人**不扣好感分**，只靠 IMP-13 的 `GS.mood` 漂 `jealous`/`cold`（直接喂修罗场升温）。
  - **撞见彩蛋（待定点②）**：低概率事件，AI 在 `visitSet` 场景里自然写「某人也来了 / 被吸引来」，撞见→修罗场/尴尬/默契，靠 prompt 指令实现，不硬编码分支。角色专属优先级：**当关系网角色为「哥哥」（队友的妹妹设定），探男主时撞见对象优先是哥哥**（同队同行、日程高度重叠），其次才是情敌——比随机更贴"顶流男团同吃同住"的真实性；撞见哥哥→直接推高 IMP-17 的 `GS.brotherSuspicion`。
  - **日程密度（待定点③）**：每天 3 人都有日程，但探班窗口偶尔不开放（如某人当天封闭录制，`visitWindow=false`），避免变打卡。
  - **UI**：娱乐圈世界观下剧情区上方轻量「📋 今日行程」条列 3 人；工作时段对应人显示「🎬 去探班」按钮，探过一人后另两人灰掉。纯当次会话，零后端。
- **与现有架构咬合**：`GS.oneHeartTimeOfDay` / `oneHeartMember/RelationCharacter/Rival` 现成；`buildOneHeartUserMessage('visitSet', ...)` 新分支写职场反差/地下恋风险；`generateOneHeartRound` 模式 + `generateWithRetry`/`parseNarrative`/`applyParsedSideEffects` 生成；`updateRivalTendency()` 现成（`ui-renderer.js:2576`）；IMP-13 `GS.mood` 驱动未探者漂移；affection / 送礼 / 聊天复用。
- **约束**：仅 `gameMode==='oneHeart' && worldSetting==='entertainment'` 分支，transfer 与其他 1v1 世界观一行不动；纯前端、零新 UI 框架（复用现有剧情区 + 快捷指令/提示条风格）。
- **核心一句话**：行程让「他抽空见你」从设定变成可玩机制，探班=偷会，把娱乐圈地下恋的核心张力直接变成玩家的每天选择。
- **落地（2026-07-07）**：
  - `src/worlds/entertainment.js` 新增导出 `ENT_SCHEDULE_POOL`：约 35 条偶像本职活动（录音/舞蹈/综艺/拍摄/粉丝/跨界/行程 七类），每条 `{id, cat, task, place}`。
  - `src/state.js` 新增 `oneHeartSchedule: null` / `oneHeartVisitLog: []` / `oneHeartVisitLocked: ''` + migrate 兜底。
  - `src/game-engine.js` 新增 `generateOneHeartSchedule()`（为男主/关系户/情敌各抽一条、时间窗错开 上午/午后/傍晚/深夜、同天尽量异类）与 `doVisitMember(roleKey)`（async；置 `pendingChoiceText`/`oneHeartTimeOfDay`/`oneHeartVisitLocked`、标记已探、入 `oneHeartVisitLog`、调 `generateOneHeartRound({isVisit:true, visitRoleKey})`）；`generateOneHeartRound` 的 `_lockMorning` block 在新的一天调 `generateOneHeartSchedule()`。
  - `src/prompts.js` 在 `buildOneHeartUserMessage` 末尾：当 `extra.isVisit && extra.visitRoleKey` 注入 `[INSTRUCTION] 探班场景（visitSet）`（main=职场反差/related=关系张力/rival=修罗场直球）。
  - `src/ui-renderer.js` 在 1v1 操作区加 `<div id="oneHeartScheduleStrip"></div>` + `renderOneHeartScheduleStrip()`（渲染三人日程卡 + 探班按钮，探过一人后另两人灰掉，绑 `doVisitMember`）。
  - 约束：仅 `gameMode==='oneHeart' && worldSetting==='entertainment'` 生效，transfer 与其他 1v1 世界观零改动；未探者靠 IMP-13 `GS.mood` 漂 `jealous`/`cold`（探班好感待定点①按设计不扣分，仅漂移）。

### IMP-17：哥哥＝信任圈内人（参谋 / 掩护 / 考验）（1v1·娱乐圈「队友的妹妹」专属）— ✅ 已落地
- **重锚说明**：原 IMP-17 将哥哥设计为"察觉度威胁源 / 必须隐瞒"，与玩家实际玩法冲突——玩家对哥哥无话不谈、刚有苗头就问"这人能继续交往吗"。故重锚为 **哥哥＝信任圈内人（参谋＋掩护＋考验）**；本角色"偷偷摸摸"的张力来自**公众 / 粉丝 / 公司 / 行业视线**，不是自家客厅。`entertainment.js` 的「队友的妹妹」条已按此重写（IMP-20）。
- **位置**：`src/state.js`（`GS.brotherStance`/`GS.brotherAtHome`/`oneHeartBrotherPool` + migrate 兜底）、`src/game-engine.js`（`updateBrotherStance(delta, reason)`、在家窗口随 `generateOneHeartSchedule()` 同步、`goToNextDay` 清账）、`src/prompts.js`（`buildOneHeartUserMessage` 注入哥哥立场 + 在家状态 + 参谋指令）、`src/data.js`（IDENTITY 关系映射已含 `livesTogether:true`，补充哥哥专属事件池文案）、`src/worlds/entertainment.js`（队友的妹妹条已重写为"对哥哥无话不谈 / 哥哥是参谋"）
- **仅触发条件**：`GS.gameMode==='oneHeart' && worldSetting==='entertainment' && oneHeartRelationCharacter.role==='哥哥'`（即「队友的妹妹」设定）。其他身份/世界观完全不触发，零改动。
- **代码事实基线**（`src/data.js:1241`）：`'队友的妹妹': { role:'哥哥', gender:'male', fromSEVENTEEN:true, isRival:false, needExtraRival:true, livesTogether:true }` → 阵容为 男主 + 哥哥(关系户,SEVENTEEN成员,非情敌) + 独立情敌；`livesTogether`=同住。用户**比全团十三人都小**，因哥哥介绍（工作/学业来首尔找哥哥）才认识团队，哥哥故乡不在首尔故非童年熟稔——背景已落地 `entertainment.js`。
- **核心机制（IMP-17 主体）**：
  - **哥哥立场 `GS.brotherStance`（非威胁，是支持度）**：`'consultant'`(默认：你告诉他一切、他给意见) / `'supportive'`(认可男主、主动掩护) / `'testing'`(试探男主诚意) / `'protective'`(提醒公众曝光风险——反对的是"被拍"，不是"这段关系")。由相关事件翻转，正反馈升 `supportive`。
    - **升 supportive**：男主在哥哥面前尊重你/对你妹好、你向哥哥汇报后哥哥认可、哥哥看到男主为你做的实在事。
    - **转 testing/protective**：男主在哥哥面前敷衍、出现轻率曝光风险、你犹豫不决——哥哥转为提醒或试探，而非反对。
    - **结算**：`updateBrotherStance(delta, reason)`（仿 `updateRivalTendency` 现成模式）；`goToNextDay` 不强制衰减（立场是关系，不是会冷却的数值）。
  - **哥哥「在家 / 不在家」窗口 `GS.brotherAtHome`**：由 IMP-16 的 `oneHeartSchedule.related`（哥哥日程）派生——哥哥出差 / 录节目 / 不在首尔时 `brotherAtHome=false`=**正当空档**（你敢放男主进门，且哥哥知情）；哥哥在家时 `true`=男主不便来。这是本角色签名场景，但"偷"的对象是公众视线，不是哥哥。复用 IMP-16 时间轴，零新框架。
  - **哥哥的参谋对话**：你向哥哥汇报进展时，prompt 生成哥哥回应（建议 / 调侃 / 提醒），强化"无话不谈"的亲密与喜剧；可喂 IMP-13 男主 `mood`（哥哥支持→男主放松）。
- **团内群像 / 事件层增强（④⑤⑥⑦，prompt + 事件池，低成本）**：
  - **④ 比全团小、被当妹妹 / 后辈（非"团宠"）**：你因哥哥介绍才认识他们、年龄最小→被当妹妹 / 后辈尊重。轻量 prompt：其他成员偶尔关照你、拿你开哥哥玩笑、无意点到男主时你慌一下（怕哥哥多心，但哥哥其实知情）。复用 IMP-15 差异化写法。
  - **⑤ 男主怕哥哥（喜剧）**：男主紧张 protective 哥哥，但哥哥其实知情 / 支持→"见哥哥来手忙脚乱、哥哥在旁边笑"的喜剧反差。prompt 写。
  - **⑥ 哥哥专属事件池 `oneHeartBrotherPool`**：参谋 / 掩护 / 调侃 / 考验类（哥哥帮你打掩护、在家庭群替你圆、故意留你俩独处试探男主诚意、提醒"别被拍"）。因 `state.js` 已有同款 `oneHeart*Pool` 数组，落地极轻——新增 `oneHeartBrotherPool:[]` + migrate 兜底即可。
  - **⑦ 中点「哥哥的考验」**：中局哥哥找男主"谈谈"，试探他是否真心对你好——专属 setpiece，正向张力，给本角色清晰情感弧线（事件池抽取或固定 mid-game 触发）。
- **UI（极轻）**：哥哥立场可在行程条旁显示（如 🤝参谋 / 💚支持 / 🔍试探 / 🛡️提醒），不强制；纯当次会话，零后端。
- **与现有架构咬合**：`oneHeartRelationCharacter`/`oneHeartRival`/`oneHeartSchedule`(IMP-16) 现成；`updateRivalTendency` 模式可复制为 `updateBrotherStance`；`oneHeart*Pool` 数组架构复用；IMP-13 `GS.mood` 联动；IMP-16 窗口联动；IMP-18 HE 接入 `brotherStance==='supportive'` 作为祝福条件。
- **约束**：仅 `role==='哥哥'` 分支，其他身份（未婚夫/师兄/队友…）及 transfer 一行不动；纯前端、零新 UI 框架（复用现有状态条/提示条风格）。
- **核心一句话**：把哥哥从"要瞒的人"变成"最信任的参谋与掩护"——偷偷摸摸的张力来自公众视线，而非自家客厅；这更贴你"无话不谈"的玩法，也让哥哥成为 HE 的祝福者而非破坏者。
- **落地（2026-07-07）**：
  - `src/state.js` 新增 `brotherStance:'consultant'` / `oneHeartBrotherAff:0` / `oneHeartBrotherLog:[]` / `oneHeartBrotherPool:[]` / `brotherAtHome:true` + migrate 兜底（仅娱乐圈·队友的妹妹设定生效）。
  - `src/game-engine.js` 新增导出 `updateBrotherStance(delta,reason)`（仿 `updateRivalTendency`；立场由 `oneHeartBrotherAff` 单一推导：>=30 supportive / 0~30 consultant / -30~0 testing / <-30 protective；Toast 反馈 + saveGame）；在 `generateOneHeartSchedule()` 由 `related`（哥哥）行程 `cat==='行程'` 派生 `brotherAtHome`（不在家=正当空档）；`doVisitMember('related')` 探哥哥 +3 推高 `brotherStance`；`generateOneHeartRound` 在 `genCount===15` 注入「哥哥的考验」setpiece 并 +2（仅一次，`_brotherTestNudged` 守卫）。
  - `src/prompts.js` `buildOneHeartUserMessage` phase 分支注入「哥哥的立场与在家状态（强制遵循）」块：立场标签 + 对应行为指令 + 设定铁律（张力来自公众视线非哥哥）+ 参谋指令；仅 `role==='哥哥'` 时注入，transfer 与其他 1v1 世界观零改动。
  - `src/ui-renderer.js` `renderOneHeartScheduleStrip` 在哥哥设定下追加「🤝哥哥立场：参谋/支持/试探/提醒曝光 · 🏠在家/✈️不在家(空档)」指示（随探班/新一天自动刷新）。
  - ④⑤⑥⑦ 中：⑥ 缓冲数组已落；⑦ 中点考验 setpiece 已落；④⑤ 为 prompt 层风味（由立场指令自然驱动，未另立开关）；`oneHeartBrotherPool` 作为运行期事件缓冲（同款 `oneHeart*Pool` 架构），待后续事件抽取接入。

### IMP-18：结局系统（1v1·多仪表盘聚合，HE/NE/BE）— ✅ 已落地
- **位置**：`src/state.js`（`GS.oneHeartEnding`/`GS.endingMeters` 快照 + migrate 兜底）、`src/game-engine.js`（`evaluateEnding()`、`rollEnding()`、`buildOneHeartUserMessage('ending', ...)` 分支）、`src/prompts.js`（`buildOneHeartSystemPrompt` 注入结局倾向 + `ending` 分支指令）、`src/ui-renderer.js`（1v1 结局页渲染 + 重启入口）、`src/data.js`（ENDING_TEMPLATES 文案池）
- **范围**：仅 `GS.gameMode === 'oneHeart'`。transfer 模式零改动。娱乐圈「队友的妹妹」设定的哥哥立场 `brotherStance==='supportive'`（IMP-17，哥哥的祝福）作为 HE 解锁条件之一接入，但结局系统本身对所有 1v1 世界观通用。
- **现象**：当前 1v1 **完全没有结局系统**——游戏走到最后没有收束，玩家没有"赌注感"，前面 IMP-13/16/17 攒的所有数值（情绪/哥哥立场/情敌倾向）没有出口。地下恋若注定 HE，偷偷摸摸就只是调味不是张力。
- **设计（三档结局，多仪表盘聚合）**：
  - **评判输入**（复用前序 IMP 已建仪表盘，不新增独立数值）：
    - `brotherStance`(IMP-17) — 哥哥是否站你 / 给出祝福（`'supportive'` 为 HE 条件之一，而非"摊牌危机"）
    - `oneHeartRivalAff`(情敌倾向) — 情敌线是否翻车
    - `GS.mood`(IMP-13) — 男主当前情绪基调（guarded/nervous 拉低 HE 概率）
    - `scandal` 曝光度（`oneHeartScandalPool` 累积）— 是否翻车
    - `affection`（男主好感度）— 底线门槛
  - **HE（修成正果 / 公开被祝福）**：曝光可控 + 男主表现好（哥哥 `brotherStance==='supportive'` 给出祝福）+ 情敌未翻车 + affection 达标。娱乐圈下"哥哥的祝福"是 HE 的关键正向条件（他本就是你最信任的参谋）。
  - **NE（中性相守）**：仍在地下但稳定 —— 中等所有 meter，未公开也未崩盘，平淡相守。
  - **BE（分手 / 曝光翻车）**：scandal 曝光翻车 + 情敌赢了 + 关系本身失败，任一爆表即触发。**哥哥发现不再作为 BE 来源**——他知情且是参谋，发现反可能是温情"我早知道"（见 IMP-21 G）。
  - **判定时机**：1v1 专属「走向大结局」指令 / 快捷指令抽屉调用 `evaluateEnding()` → 聚合 meter → `rollEnding()` 抽档 → `buildOneHeartUserMessage('ending', {endingType})` 生成收束剧情。
- **与现有架构咬合**：IMP-13/16/17 的 meter 现成直接复用；`buildOneHeartUserMessage` 已有 `phase/chat/moment/theater` 分支，加 `ending`；`generateOneHeartRound` 模式 + `generateWithRetry` 生成；`ENDING_TEMPLATES` 仿 `oneHeart*Pool` 数组架构入 `data.js`；结局页复用现有剧情区 + 重启按钮风格。
- **约束**：仅 `gameMode==='oneHeart'` 分支，transfer 一行不动；纯前端、零新 UI 框架；结局文案经 `escHtml()` 转义。
- **核心一句话**：把前面所有 IMP 攒下的数值变成"赌注的出口"，让地下恋的每一次偷偷摸摸都有意义——你可能修成正果（连哥哥都为你祝福），也可能在公众视线的放大镜下失去一切。
- **落地（2026-07-07）**：
  - `src/data.js` 新增 `ONE_HEART_ENDING_TEMPLATES`（HE/NE/BE 三档文案池：label/desc/tone）。
  - `src/state.js` 新增 `oneHeartEnding:''` / `endingMeters:null` + migrate 兜底。
  - `src/game-engine.js` 新增 `evaluateEnding()`（聚合 affection / `brotherStance`(IMP-17) / `oneHeartRivalAff` / `oneHeartScandalPool` / `GS.mood`(IMP-13) 五仪表盘算分，按 scandal>=4 / rival>=50 / aff<20 → BE，score>=35 且 aff>=60 且 brotherStance==='supportive' → HE，否则 NE）+ `rollEnding()`；`generateOneHeartRound({isEnding})` 走 `'ending'` 分支、按档位定调注入 sysMsg 与 userMsg、`GS.finalResult` 收尾、`endingArchive` 图鉴入档。
  - `src/prompts.js` `buildOneHeartUserMessage` 新增 `'ending'` 分支（按 `extra.endingType` 写 1200-1800 字收束，禁止开新支线）。
  - `src/ui-renderer.js` 1v1 结局页升级：显示 HE💍/NE🌙/BE💔 类型徽标 + 颜色横幅 + 结局仪表盘快照（哥哥立场/情敌倾向/曝光风险/男主心境值）+「开始新的故事」入口；`endingArchive` 图鉴复用展示。

---

### IMP-19：节奏 / 渐进系统（1v1 通用，娱乐圈尤甚）
- **位置**：`src/game-engine.js`（`GS.oneHeartRomanceStage` 或复用 `genCount`/`round` 驱动阶段）、`src/prompts.js`（`buildOneHeartUserMessage` 按 stage 注入"感情进度"指令）、`src/data.js`（事件池按 stage 标注可用区间）
- **仅触发条件**：`GS.gameMode==='oneHeart'`（所有 1v1 世界观通用；娱乐圈因"顶流地下恋"对节奏最敏感，优先落地）。transfer 零改动。
- **现象（用户反馈）**：当前事件太激进、跳脱——"第二回合情敌就把我拉进后台小房间""男主好感>40 情敌就直接告白"。感情（男主＋情敌）应**随回合 / 天数渐进发展**，不是数值到阈值就摊牌。
- **设计原则**：激进 / 告白类事件按"进度门控"，需 **round/day（进度）＋ 好感（浓度）双条件**，而非单好感阈值。
  - **早期（round 1-3）**：暧昧 / 越界但**不过线**——眼神停留、试探、吃醋苗头、私下称呼变化；情敌只是"更关照你"，不强行肢体 / 不告白。
  - **中期**：明确好感但**克制**；情敌开始正面竞争（争宠 / 较劲），但仍非告白。
  - **后期（climax）**：**告白 / 摊牌设为高潮事件**，需好感达标（如男主≥60、情敌≥50）**且**进度达标（如 round≥8 或 day≥某值）。
  - **情敌告白阈值**从 40 提到后期，且须先有"竞争"铺垫；未经铺垫的突兀告白视为跳脱，prompt 明确禁止"round<5 出现告白 / 强拉私密空间"。
  - **阶段驱动**：`GS.oneHeartRomanceStage`(0=初识/1=暧昧/2=明确/3=高潮) 由 `genCount`/关键事件推进；`buildOneHeartUserMessage` 注入"当前应处于 X 阶段，禁止越级行为"指令。
- **与现有架构咬合**：IMP-15 三人差异化（各人进度可不同）、IMP-17 哥哥参谋（你汇报进展的节奏也受 stage 影响，early stage 哥哥只给泛泛建议）、IMP-18 结局（stage 进度影响 HE/NE/BE 概率）。
- **约束**：仅 1v1 分支；纯前端、零新 UI 框架；prompt 指令经 `escHtml` 转义。
- **核心一句话**：把"感情随时间长出来"做成硬门控——第二回合就告白 / 拉进小房间是跳脱，渐进才是顶流地下恋该有的呼吸感。

### IMP-20：娱乐圈世界观文案修订（已落地 `src/worlds/entertainment.js`）
- **已完成的代码修订**（记录备查，路线图与代码已对齐）：
  - `memberRole` 由 `'顶流偶像/演员'` 收敛为 `'顶流偶像（SEVENTEEN 成员）'`；`memberDesc` 去"演员"，明确"SEVENTEEN 的成员之一"。
  - `secondCareerNote` 重写：13 人皆为 SEVENTEEN 顶流成员，音乐制作 / 编舞 / 演戏等**只是个人爱好，并非独立职业**——消除与"无第二职业"纠正的矛盾。
  - 「队友的妹妹」背景重写：比全团小、经哥哥介绍（工作 / 学业来首尔找哥哥）认识、对哥哥无话不谈（哥哥是参谋非隐瞒对象）、哥哥不在才有正当理由的见面空档、男主与情敌心意早已越界但**感情渐进**。
- **影响面**：IMP-16（行程 / 探班，三人皆 SEVENTEEN 成员）、IMP-17（哥哥重锚为信任圈内人）、IMP-19（渐进）均基于此修订；transfer 与其他 1v1 世界观零影响。

### IMP-21：队友的妹妹 深度增强包（A~H，reframe for brother-as-confidant）— ✅ 已落地
- **位置**：`src/worlds/entertainment.js`（注记）、`src/prompts.js`（prompt 指令）、`src/state.js`（`oneHeartBrotherPool` 等数组）、`src/game-engine.js`（事件抽取）
- **仅触发**：`gameMode==='oneHeart' && worldSetting==='entertainment' && role==='哥哥'`。
- **A 同住空间的哥哥存在感（喜剧 / 张力，非"被抓"）**：哥哥物品（拖鞋 / 游戏机 / 合照）充斥你家，男主每次来都处在"哥哥的存在感"包围里——又紧张又好笑，是独家偷感来源。prompt 指令。
- **B 哥哥直男关心反差喜剧**：哥哥对你唠叨（早点睡 / 别乱吃），男主就坐在旁边，哥哥却在对你说教——荒诞反差既是甜点也是掩护。prompt。
- **C 男主在哥哥面前的"演技"甜点（reframe 为三人默契）**：当哥面叫你"妹妹"、关上门才抱你——但哥哥知情，变成三人默契喜剧，核心糖保留。prompt。
- **D 家庭层面独有场景（reframe 支持向）**：哥哥生日 / 家庭群 / 父母来电（"你哥说你最近怪怪的"），哥哥**替你打掩护**而非拆穿。入 `oneHeartBrotherPool`。
- **E 伪装进出的"间谍片"细节（躲的是公众，非哥哥）**：男主走消防梯 / 戴全套装 / 车库进，怕的是邻居 / 狗仔 / 其他队员，不是哥哥。prompt。
- **F 哥哥临时改行程的突发（reframe 喜剧）**：哥哥提前回→你慌张让男主躲，哥哥发"我回来啦，你那位还在吗？"调侃——紧张变笑点，仍推 `brotherStance` 向 `supportive`。入 `oneHeartBrotherPool`。
- **G 「被发现」复合 BE（reframe）**：BE 来自**公众曝光翻车 / 关系本身失败**，非哥哥发现；哥哥发现反而可能是温情"我早知道"——彻底解除"哥哥=BE 来源"的旧设定。连接 IMP-18。
- **H 手机 / 家庭群曝光（reframe 安全向）**：哥哥在家庭群发消息，因知情所以安全 / 喜剧；你回"哥别闹"即可化解。prompt。
- **与现有架构咬合**：`oneHeart*Pool` 数组架构、`updateBrotherStance`(IMP-17)、`brotherAtHome` 窗口(IMP-17)、IMP-13 `mood`、IMP-16 行程；纯前端、零新 UI 框架。
- **核心一句话**：把"同住＋全团最小＋哥哥知情"这三张牌打成只属于这条角色的独家戏——既有间谍片的刺激，又有三人默契的甜。
- **落地（2026-07-07）**：纯 prompt 层（仅 `role==='哥哥'` 分支，transfer 与其他 1v1 世界观零改动）。
  - `src/prompts.js` `buildOneHeartUserMessage` phase 分支在 IMP-17 哥哥块后追加 `队友的妹妹·独家戏（强制遵循）` 块，注入 A~H 全部 reframe 指令：A 同住空间哥哥存在感、B 直男关心反差喜剧、C 当哥面"演技"三人默契、D 家庭群掩护、E 伪装进出躲公众、F 哥哥提前回喜剧（按 `brotherAtHome` 区分"提前回/不在"两种 flavor）、G 已定（撞见=温情"我早知道"，非 BE 源，连 IMP-18）、H 家庭群安全喜剧。统一收束于"张力来自公众视线+三人默契喜剧，哥哥是内应"。
  - 状态/UI/架构复用 IMP-17（`updateBrotherStance`/`brotherAtHome`）与 IMP-18（G 的 BE 来源定义）、IMP-13（`GS.mood`）、IMP-16（行程）；`oneHeartBrotherPool` 缓冲数组已在 IMP-17 ⑥ 落地，待后续事件抽取接入。

---

## 已完成（前序修复，记录备查）
- 任务面板「做任务」按钮失效：`task-panel.js` 补导入 `doActionTask` 并移除不存在的 `window.doActionTask` 全局引用。
- 旧存档任务卡 `type`/`inputLabel` 缺失：`state.js` 导入 `MISSION_CARDS` 并在 `migrateSave` 按 id 补齐兜底。

---

## 待追加需求
> 用户后续将补充更多需求，统一在此追加，最后一起改。

- [ ] （占位：用户后续需求 1）
- [ ] （占位：用户后续需求 2）

---

## 修复顺序建议
1. 先修 🔴 高危 BUG-01 ~ BUG-08（玩法正确性 + 持久化 + 卡死）。
2. 再修 🟡 中危 BUG-09 ~ BUG-15。
3. 最后 🟢 低危 BUG-16 ~ BUG-22（可随其他改动顺手处理）。
4. 规范类（BUG-14 模板字符串）：经核查 `prompts.js` 当前已全程使用 `+` 拼接、反引号=0，规范已满足，无需改动（详见 BUG-14 条目）。

## 回归验证
- `npm run dev` 后实测：随机事件 re_10 可触发；Day4/5 物品不重复；Day11 深夜醉酒不卡死；选项好感度刷新后保留；修罗场强度不异常爬升；风险选项扣分生效。
- **事件池专项**：Day2 / Day7 上午能弹出秘密任务（Day5/10 不再有死任务）；随机事件池 id 连续无跳号（re_1~re_15，re_2 含 spring/summer/autumn/winter 四季变体）；春/秋也能抽到 `re_2` 空调事件；re_10 在 Day>=4 后可触发；随机事件池无报错。
- 跨天清理核查：确认 `xItemsRevealRound` / `xItemsRevealState` / `todayOptionTexts` / `flashbackShown('pending')` 已清理。
