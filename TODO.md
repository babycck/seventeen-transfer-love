# SEVENTEEN 项目 — 待修改清单

> 本文件记录所有已确认但尚未执行的修改项。
> 每次新对话开启时，请将此文件内容直接贴给 Agent。
> 新增修改项请追加到末尾，不要删除已有条目。

---

## 一、Prompt 与剧情生成规则

| # | 修改项 | 状态 | 详细说明 |
|---|--------|------|----------|
| 1 | Day 1 傍晚读信字数 | 已确认 | 300-500 字 → 200 字 |
| 2 | Day 1 傍晚读信 prompt 精简 | 已确认 | 7 个步骤压缩为 2-3 句，末尾加格式提醒保险 |
| 3 | 短信剧情加入采访间规则 + 字数缩减 | 已确认 | 对齐自由行动标准：`包含正文 + 🎙女主采访间 + 🎤成员采访间 + 🎬导演OS + 💭观察员OS`，字数 `~1000字` → `~500字` |
| 4 | 短信内容保密规则 | 已确认 | 心动短信内容只有收发双方知道，其他成员只能观察表情反应，不能猜测/评论/引用内容 |
| 5 | 私密体质专属限定 | 已确认 | 私密体质是**女主专属设定**，prompt 三处追加声明禁止映射到成员身上 |
| 6 | 进入下一天滚动到顶部 | 已确认 | `goToNextDay()` 生成记忆压缩面板后，`window.scrollTo(0, 0)` |
| 7 | 压缩记忆删除对话归属要求 | 已确认 | `compressTodayToSummary()` system prompt 中删除第2条"对话归属"要求 |
| 15 | 普通选项剧情字数缩减 | 已确认 | 普通选项（A/B/C）剧情字数：`~1500字` → `~1000字` |
| 14 | 深夜禁止 AI 自动发短信 | 已确认 | prompt 追加规则：正文中绝对禁止描写女主发送短信的动作 |
| 23 | 约会内容强制保密规则 | 已确认 | 约会具体细节只有女主和约会对象知道，其他成员只能看到"约会回来了"，不能猜测/评论/引用细节 |

## 二、女主设定与系统配置

| # | 修改项 | 状态 | 详细说明 |
|---|--------|------|----------|
| 8 | API 提供商重构 | 已确认 | 双层下拉：提供商（DeepSeek/硅基流动/千问）+ 模型选择；删除 Kimi/Moonshot |
| 9 | 女主设定：生日取代星座 | 已确认 | 移除星座芯片选择，增加「生日」输入框（月/日），星座自动计算并显示 |

## 三、季节、天气、日期与节日系统

| # | 修改项 | 状态 | 详细说明 |
|---|--------|------|----------|
| 10 | 季节与日期系统 | 已确认 | 随机生成季节+月份（2月-12月）+ 连续12天日期；UI 仅显示「X月X日」，年份隐藏 |
| 11 | 每日天气系统 | 已确认 | 每天基于季节从天气池随机抽取；80%概率关联前一天，20%概率突变 |
| 12 | 节日系统（纯随机无保底） | 已确认 | 节日池：2/14 情人节、8/29 七夕、12/25 圣诞节；成员生日同理；若12天内覆盖则触发，无保底不偏移 |

## 四、日程与强制任务调整

| # | 修改项 | 状态 | 详细说明 |
|---|--------|------|----------|
| 17 | Day 3 说故事重复触发 | 已确认 | `getMandatoryTask()` Day 3 补齐 `afternoon`/`evening`/`night` 描述，避免 fallback 到 morning 导致全天重复说故事 |
| 19 | 约会日日程调整（方案A） | 已确认 | 第二次约会从 Day 5 移到 Day 6；第二批X记忆公开从 Day 6 移到 Day 5；集体外出 Day 7 不变；第三次约会 Day 8 不变 |
| 20 | 约会日去掉「随机约会」选项 | 已确认 | Day 4/6/8/9 上午只保留「▶ 进入约会场景」按钮，去掉「随机约会」选项；自由输入保留 |
| 24 | 约会骰子结果延迟写入 | 已确认 | `showDatingDiceModal()` 修复：投骰子后立即把 `result.partner` 写入 `GS.pendingDatingResult`，不要等 `generatePhaseNarrative()` 结束 |
| 25 | `resetPhaseState()` 清空约会对象状态 | 已确认 | `resetPhaseState()` 不应清空 `currentDatingPartner` 和 `pendingDatingResult`（全天级状态） |

## 五、好感度与互动系统

| # | 修改项 | 状态 | 详细说明 |
|---|--------|------|----------|
| 13 | 好感度标记严格丢弃 | 已确认 | 无原因的 `[好感度: 成员名 +N]` 标记直接忽略，不记录也不加好感度 |
| 21 | 好感度延迟结算机制 | 已确认 | 改为延迟结算：`parseNarrative()` 提取 `[好感度: ...]` 但不立即写入 state，暂存 `pendingAffChanges`；每次进入新剧情前结算上一段；重新生成时旧 pending 被自然覆盖 |
| 32 | 好感度难度提升（无上限版） | 已确认 | ① 同一行为重复做效果递减（短信第二次+5→+3→+1，送礼同理）；② 负面行为扣分增加（未收到短信-1→-3，吃醋-3→-5）；③ 新增"好感度疲劳值"，连续对同一人行动效率下降 |
| 33 | 选项明确扣好感度 | 已确认 | 某些 A/B/C 选项会明确降低特定成员好感度（如"选择帮 A 做饭"→B 好感度 -2），在选项按钮旁显示风险提示 |
| 34 | 成员间竞争（队内修罗场） | 已确认 | A 看到女主和 B 互动后，不会夸 C，而是针对 B：讲 B 坏话、暗示 B 不适合、阴阳怪气、故意对比表现、私下"提醒"女主；触发后 A 与 B 之间产生张力，女主必须处理 |

## 六、X 记忆物品系统重构

| # | 修改项 | 状态 | 详细说明 |
|---|--------|------|----------|
| 18 | X 记忆物品在 X 档案中显示 | 已确认 | `showXArchiveModal`（`modals.js`）中每个 X 的故事前面追加显示 `GS.xItems[id]` 记忆物品列表，仅显示已公开的成员 |
| 26 | X记忆物品按需生成2个 + 50容量缓存池 | 已确认 | 首次某（女主+3成员+X）组合且缓存未满50时，AI 生成2个完整记忆物品（名称+样式+故事约50字）存入缓存；已满50时从已有组合中随机调用；相同组合复用；第一次公开只显示名称+样式，第二次公开追加故事；场外X物品维持简洁索引不变 |

## 七、午夜电话亭重构

| # | 修改项 | 状态 | 详细说明 |
|---|--------|------|----------|
| 30 | 午夜电话亭即时生成剧情 + 正文不注入 | 已确认 | 选择目标并输入内容后，立即调用 AI 生成约100字接听反应剧情，在弹窗内展示；关闭弹窗后记录状态；`prompts.js` 不再把电话亭信息注入正文剧情 |
| 31 | 午夜电话亭记录按钮 | 已确认 | Day 6 之后（Day 7 起），Header 增加 📞 按钮，点击弹窗显示当晚匿名电话的目标成员、内容和接听反应 |

## 八、UI 与交互优化

| # | 修改项 | 状态 | 详细说明 |
|---|--------|------|----------|
| 16 | 选项与自由输入次数拆分 | 已确认 | 每时段：选项（A/B/C）最多选 1 次，自由输入最多输入 1 次，合计上限 2 次 |
| 22 | 保存意图后进入下一段自动清空输入框 | 已确认 | 点击 💾 保存意图后，当用户通过选项/跳时段等方式进入下一段剧情时，渲染后自动清空 `GS.freeInput` 及输入框内容 |
| 28 | 礼物改造后显示「已改造」标签 | 已确认 | `isRemade=true` 时显示「✅ 已改造」标签，改造按钮仍隐藏；改造时保留原名称不替换 |
| 29 | 送礼剧情缺少女主人设 | 已确认 | `sendGift()` system prompt 注入女主名字（`GS.heroineProfile.name`）和基本信息，避免 AI 自由编造女主名字 |

## 九、已执行代码修改（待验证）

以下修改已写入文件，但尚未经过完整测试验证：

- [x] `ui-renderer.js` 导入 `resetPhaseState`
- [x] `ui-renderer.js` 导入 `compressTodayToSummary`
- [x] `modals.js` 导入 `showLoading`/`hideLoading`
- [x] `data.js` 添加硅基流动 + 千问提供商 + 模型双层下拉结构
- [x] `state.js` 新增 `apiModel` 字段 + `migrateSave` 兜底
- [x] `api.js` 调用 API 时优先使用 `GS.apiModel`
- [x] `ui-renderer.js` Step 1 增加「AI 模型」下拉框 + 切换事件
- [x] `modals.js` API 设置弹窗增加「AI 模型」下拉框 + 切换事件

---

## 十、新增修改项（请在此区域追加）

| # | 修改项 | 状态 | 详细说明 |
|---|--------|------|----------|
| 35 | 匿名提问箱选项未渲染 | 已确认 | `ui-renderer.js` 缺少 `questionBox` 的选项区渲染，需增加「如实回答/拒绝回答并喝酒」两个按钮，点击后调用 `handleQuestionBoxChoice` |
| 36 | 匿名提问箱选项改为固定按钮 | 已确认 | `ui-renderer.js` 中提问箱活跃时固定渲染「如实回答/拒绝回答并喝酒」两个按钮，不依赖 AI 返回的选项；`handleQuestionBoxChoice` 根据按钮索引判断，不再用 `indexOf('喝酒')` |
| 37 | 匿名提问箱点取消后卡死 | 已确认 | `showQuestionBoxAnswerModal()` 点取消返回空字符串时直接 `return`，没有恢复选项状态，导致提问箱 `active` 仍为 true 但按钮全 disabled；修复：点取消后应关闭弹窗并恢复选项，或允许重新选择 |
| 38 | 匿名提问箱缺少执行过程描写 | 已确认 | prompt 追加要求：必须描写女主回答/喝酒的**动作过程**（站起来、念回答、拿起酒杯等），不能只写现场反应 |
| 39 | Day 7 提问箱喝酒分支特殊剧情 | 已确认 | 女主选择拒绝回答并喝酒（+2杯）后，触发"醉倒被送回房间"的特殊剧情描写；但流程照常，仍可进入深夜发短信 |
| 40 | 完整剧情文本导出 | 已确认 | 增加「📥 导出记录」按钮，将所有 `dailyFullTexts` 中的原始剧情文本合并导出为 `.txt` 文件下载，仅导出剧情正文，不包含好感度/SMS历史等数据 |
| 41 | Day 8 约会修罗场竞争机制 | 已确认 | 仅 Day 8 约会触发（30%概率）：若骰子选中的成员对女主好感度最低，其他两位成员有几率口头抗议/争取；弹出新选项「维持原结果/换成B/换成C」；无论选哪个，最终都可进入约会场景（只是剧情走向不同） |
| 42 | 深夜去掉选项按钮 | 已确认 | 深夜时段（phaseIndex === 3）只去掉选项区 A/B/C 按钮，保留「重新生成本段」「跳过进入下一时段」「发送心动短信」三个按钮和自由输入框 |
| 43 | Day 9 X约会日改为双向读信 | 已确认 | 原单向（女主读给X）改为双向：X也读写给女主的分手信，顺序由AI自由安排；X的信由AI根据X档案自动生成，长度300字；女主的信长度也改为300字 |
| 44 | 约会地点池与季节/天气联动 | 已确认 | 增加约会地点池，每次约会随机抽取注入prompt；地点与季节、天气联动（如雨天偏向室内），每个地点附带氛围描述。具体地点池：<br>自然：樱花林、海边栈道、山顶观景台、湖边野餐<br>城市：深夜便利店、旧书店、复古游戏厅、天台篮球场<br>室内：私人影院、陶艺工坊、猫咪咖啡馆、录音棚<br>特殊：废弃车站、凌晨的急诊室、24小时投币洗衣房 |
| 45 | 短信草稿动态生成 | 已确认 | 短信草稿不再使用固定模板，要求 AI 基于当天剧情内容动态生成（如当天去了海边→草稿含"海风"意象），与剧情强关联 |
| 46 | Day 11 选项限制修正 | 已确认 | 将 `isDay11` 改为 `isTruthRound`（`GS.day === 11 && GS.truthState && GS.truthState.active`），只有真心话环节不限制选项，上午最终约会仍限制 |
| 47 | Day 11 真心话游戏化重构 | 已确认 | 成员回合：AI 生成50-100字极简剧情（抽卡+看到问题的反应），自动判定（50%回答/50%喝酒）；女主回合：先弹出「抽卡」按钮（类似约会日投骰子弹窗），点击后揭示问题卡内容+简短成员反应，再弹出「如实回答」（自由输入框）/「拒绝回答并喝酒（+1杯）」；循环12轮；扩音器只说节目设定，不提前透露"触发醉酒剧情"等元游戏机制 |
| 48 | 系统注入标记防泄漏 | 已确认 | ① `prompts.js` 所有 `## ` 前缀标记改为 `[SYSTEM]` 或 `[INSTRUCTION]` 格式；② system prompt 追加规则：所有 `[SYSTEM]` 和 `[INSTRUCTION]` 标记仅供内部参考，绝对禁止出现在正文输出中 |
| 49 | Day 11 真心话自动折叠 | 已确认 | narrative-box 中真心话已完成轮次自动折叠为摘要行（如"第1轮：圆佑回答/净汉喝酒/珉奎回答/你喝酒"），当前轮完整展开；点击摘要可展开回看；不影响导出、重新生成、继续今天等机制 |
| 50 | 成员真心话回合显示题目 | 已确认 | 成员抽卡时，UI 展示该成员名字+抽中的问题卡内容+判定结果（回答/喝酒），像综艺字幕一样让玩家也看到题目 |
| 51 | 真心话喝酒计数 bug | 已确认 | ① AI 不知道累计数：prompt 未注入 `drinkCounts`，导致女主第2次喝酒时 AI 仍写"喝了一杯"；② 成员双重计数：`handleTruthRound` +1 + parser.js 从 `[喝酒: 成员名]` 再 +1 |
| 52 | 真心话喝酒计数实时显示 | 已确认 | `ui-renderer.js` 真心话模式下选项区下方显示实时喝酒计数面板：`圆佑：1杯 | 净汉：2杯 | 珉奎：0杯 | 你：1杯` |
| 53 | 测试模式（本地文件模拟AI） | 已确认 | `src/api.js` `callDeepSeek` 增加测试模式分支：Header 增加 🧪 开关，开启后不调用真实 API，改为读取 `public/test-mocks/` 下对应 txt 文件（`phase.txt`/`consequence.txt`/`sms.txt`/`freeAction.txt`/`stay.txt`/`truth.txt`/`drunk.txt`/`xArchive.txt`/`memory.txt`）作为 `rawText` 返回，走完整解析渲染流程，方便无 API Key 时测试 UI 和剧情推进 |
| 54 | Day 12 最终选择增加仪式感铺垫 | 已确认 | Day 12 上午不只是直接给选项，而是先生成铺垫剧情（起床、早餐、紧张氛围、制作组召集、选择仪式感、女主内心挣扎），然后才弹出最终选项；选择后进入现有 ~1500 字结局叙事 |