# SEVENTEEN 项目 — 架构迁移完整路线图

> **目标**：把当前"AI 包办一切"的架构，迁移到"框架管骨架、AI 只生成剧情文案"的架构。
> **原则**：渐进式迁移，每个阶段结束游戏都能正常运行；不下大爆炸式重写。
> **生成日期**：2026-06-22

---

## 目录

1. [现状分析与问题](#一现状分析与问题)
2. [目标架构](#二目标架构)
3. [迁移路线总览](#三迁移路线总览)
4. [Phase 0：紧急 bug 修复（已完成）](#phase-0紧急-bug-修复已完成)
5. [Phase 1：状态机分层](#phase-1状态机分层)
6. [Phase 2：AI 输出 Schema 强化](#phase-2ai-输出-schema-强化)
7. [Phase 3：Prompt 模块化拆分](#phase-3prompt-模块化拆分)
8. [Phase 4：游戏骨架抽取（核心阶段）](#phase-4游戏骨架抽取核心阶段)
9. [Phase 5：UI 组件化 + 消灭循环依赖](#phase-5ui-组件化--消灭循环依赖)
10. [Phase 6：事件图谱（Storylets）](#phase-6事件图谱storylets)
11. [Phase 7：长期记忆与角色性格向量](#phase-7长期记忆与角色性格向量)
12. [关键文件改造清单](#八关键文件改造清单)
13. [风险与回退策略](#九风险与回退策略)
14. [验收标准](#十验收标准)

---

## 一、现状分析与问题

### 1.1 当前代码规模

| 文件 | 行数 | 职责 | 问题 |
|------|------|------|------|
| `game-engine.js` | 1674 | 主编排器，31 个导出函数 | 业务逻辑 + UI 弹窗混在一起 |
| `ui-renderer.js` | 1360 | 全部渲染 | `innerHTML` 字符串拼接 + 全量重绘 |
| `prompts.js` | 628 | system prompt + user message | 一个 628 行的巨型字符串拼接 |
| `modals.js` | 720 | 13 个弹窗 | 与 engine/sms 循环依赖 |
| `data.js` | 482 | 常量与数据表 | 散落各处的参数 |
| `state.js` | 290 | GS 单例 + 持久化 | 60+ 字段，`migrateSave` 永远在打补丁 |
| `api.js` | 472 | callDeepSeek 封装 | 含测试模式分支 |
| `validator.js` | 305 | 7 条内容规则校验 | 只做 warning，不阻塞 |
| `parser.js` | 197 | JSON 解析 + 字段重分配 | 已是 JSON 解析，问题较小 |
| `schema.js` | 196 | AI 输出 schema 定义 | 缺少副作用字段约束 |
| `formatters.js` | 185 | 格式化和工具函数 | 偏稳定 |
| `memory.js` | 175 | 记忆压缩 | 1000 字摘要丢细节 |
| `x-archive.js` | 241 | X 档案生成 | 偏独立 |
| `sms.js` | 66 | 短信发送 | 小巧 |

### 1.2 核心架构问题

#### 问题 1：AI 越权——剧情走向、好感度、副作用都由 AI 决定

当前流程：
```
玩家选选项 → AI 生成剧情 + 选项后续 + 好感度变化 + 喝酒计数 + 新选项
```

AI 同时承担了：
- 创意写作（正文）✅ 这是 AI 该做的
- 决定分支走向 ❌ 应该由骨架控制
- 好感度增减数值 ❌ 应该由骨架根据玩家选择结算
- 喝酒计数 ❌ 应该由骨架根据规则结算
- 生成下一批选项 ❌ 应该由骨架基于事件图谱决定
- 触发随机事件 ❌ 应该由骨架基于条件概率决定

**后果**：AI 输出不可控，OOC 频发，validator 永远在追着擦屁股。

#### 问题 2：状态散乱——GS 是个 60+ 字段的散沙对象

- `state.js` 的 `defaultGameState` 有 60+ 顶层字段
- `migrateSave` 是 80 行 `if (GS.X === undefined) GS.X = ...` 的兜底
- 每加一个功能就要在 3 个地方改：`defaultGameState` + `migrateSave` + 业务代码
- 状态变更是直接赋值，没有变更通道，无法追溯、无法回滚

#### 问题 3：UI 全量重绘——`innerHTML` 拼接 + 全局 `window.__renderAll`

- `ui-renderer.js` 每次都全量替换 `#app.innerHTML`
- 事件绑定靠 `addEventListener` 手动绑，每次重绘重新绑
- 触摸穿透、事件泄漏靠 `preventDefault` + `stopPropagation` 打补丁
- `game-engine.js` / `sms.js` / `modals.js` 通过 `window.__renderAll()` 触发渲染，因为循环依赖

#### 问题 4：Prompt 巨石——628 行的字符串拼接

- `buildSystemPrompt` 是一个 190 行的 `+` 拼接
- `buildUserMessage` 是一个 400 行的 `if/else` 链
- 改一条规则要在巨型字符串里找
- 状态注入散落在几十处 `if` 分支里
- prompt 模板和状态数据混在一起，无法独立测试

#### 问题 5：两个潜在崩溃 bug（已修复）

- `game-engine.js:19` `pushCorrections` 无限递归 → 任何 validator correction 都会栈溢出
- `api.js:396` `temperature: 1` 硬编码 → 温度分层从未生效

### 1.3 当前依赖图

```
data.js ← (无依赖)
  ↑
state.js ← story-cache.js
  ↑
api.js, utils.js
  ↑
core.js (barrel)
  ↑
affection.js, parser.js, validator.js, formatters.js, memory.js, x-archive.js
  ↑
prompts.js (依赖 formatters/memory/validator)
  ↑
sms.js, modals.js, game-engine.js
  ↑
ui-renderer.js ←→ game-engine.js (循环，用 window.__renderAll 打破)
  ↑
init.js
```

**两个循环依赖**：
1. `ui-renderer.js` ↔ `game-engine.js`：用 `window.__renderAll` 打破
2. `ui-renderer.js` → `modals.js` → `sms.js` → `window.__renderAll` 打破

---

## 二、目标架构

### 2.1 核心理念：骨架化 + AI 纯文案

```
┌─────────────────────────────────────────────────────┐
│  游戏骨架（JS 代码框架）                              │
│  ─────────────────────                               │
│  • 日程推进（Day 1-12 × 4 时段）                      │
│  • 强制任务（每天必做事件）                           │
│  • 选项分支（A/B/C 选项由骨架生成，不由 AI 决定）     │
│  • 好感度结算（根据玩家选择，骨架计算 ±N）            │
│  • 喝酒计数（根据规则，骨架累加）                     │
│  • 事件触发（嫉妒/前任来电/真心话等，骨架决定是否触发）│
│  • 状态流转（phase 推进、day 推进、game over）       │
│  • 副作用（设置 flag、解锁心记、推进任务进度等）      │
└─────────────────────────────────────────────────────┘
              │  传入"世界状态快照" + "生成任务"
              ▼
┌─────────────────────────────────────────────────────┐
│  AI 生成层（只管文案）                                │
│  ─────────────────────                               │
│  输入：                                               │
│  • 世界状态快照（日期/天气/好感度/成员状态/历史事件） │
│  • 当前场景任务（"生成 Day 3 下午主线剧情 ~1000 字"） │
│  • 性格约束（成员性格向量）                           │
│  • 风格约束（写作规则、称呼规则、禁止事项）           │
│                                                       │
│  输出（严格 JSON）：                                  │
│  • blocks: [{type, member, content}]  正文 + 采访间   │
│  • observers: [{name, line}]          观察员 OS       │
│  • smsDrafts: [String]                深夜短信草稿    │
│  （不输出 options、不输出 drinks、不输出好感度变化）  │
└─────────────────────────────────────────────────────┘
              │  框架接过 JSON，应用到 state
              ▼
┌─────────────────────────────────────────────────────┐
│  渲染层（UI 组件）                                    │
│  ─────────────────────                               │
│  • 订阅 store 变更，局部更新                          │
│  • 不再全量重绘                                       │
│  • 选项按钮由骨架数据驱动渲染                         │
│  • 好感度面板、喝酒面板由骨架数据驱动                 │
└─────────────────────────────────────────────────────┘
```

### 2.2 AI 的职责缩减

| 当前 AI 负责 | 迁移后 AI 负责 | 迁移后由骨架负责 |
|---|---|---|
| 正文叙事 | ✅ | |
| 女主采访间 | ✅ | |
| 成员采访间 | ✅ | |
| 导演 OS | ✅ | |
| 观察员 OS | ✅ | |
| 短信草稿 | ✅ | |
| 生成下一批选项 | | ✅ 骨架基于事件图谱 |
| 好感度 ±N | | ✅ 骨架根据玩家选择 |
| 喝酒计数 | | ✅ 骨架根据规则 |
| 触发随机事件 | | ✅ 骨架根据概率 |
| 触发嫉妒/前任来电 | | ✅ 骨架根据条件 |
| 决定是否进入特殊场景 | | ✅ 骨架根据日程 |

### 2.3 目标依赖图

```
data.js (常量)
  ↑
state.js (持久化) ← store.js (dispatch/reducer/subscribe)
  ↑
api.js (callDeepSeek) ← ai-generator.js (生成编排)
  ↑
skeleton/
  scheduler.js (日程推进)
  option-engine.js (选项生成)
  affection-engine.js (好感度结算)
  event-triggers.js (事件触发)
  scene-router.js (场景路由)
  ↑
game-engine.js (退化成薄编排层)
  ↑
components/ (UI 组件，订阅 store)
  ↑
app.js → init.js
```

**循环依赖消失**：UI 订阅 store，store 不依赖 UI；engine 调 dispatch，不调 render。

---

## 三、迁移路线总览

| 阶段 | 内容 | 工期 | 优先级 | 可独立性 |
|---|---|---|---|---|
| Phase 0 | 紧急 bug 修复 | 10 分钟 | **已完成** | - |
| Phase 1 | 状态机分层（store.js） | 1-2 天 | 高 | 可独立 |
| Phase 2 | AI Schema 强化 + Validator 升级 | 2-3 天 | 高 | 可独立 |
| Phase 3 | Prompt 模块化拆分 | 3-4 天 | 高 | 可独立 |
| **Phase 4** | **游戏骨架抽取（核心）** | **5-7 天** | **最高** | 依赖 Phase 1-3 |
| Phase 5 | UI 组件化 + 消灭循环依赖 | 5-7 天 | 中 | 可独立 |
| Phase 6 | 事件图谱（Storylets） | 1-2 周 | 低（可选） | 依赖 Phase 4 |
| Phase 7 | 长期记忆 + 性格向量 | 1 周 | 低 | 可独立 |

**建议顺序**：Phase 1 → Phase 2 → Phase 3 → **Phase 4** → Phase 5 → （Phase 6/7 可选）

---

## 四、Phase 0：紧急 bug 修复（已完成）

### 4.1 Bug 1：`pushCorrections` 无限递归

- **文件**：`src/game-engine.js:19-21`
- **原代码**：
  ```js
  function pushCorrections(corrections) {
    pushCorrections(corrections);  // 递归自己 → 栈溢出
  }
  ```
- **修复后**：
  ```js
  function pushCorrections(corrections) {
    if (corrections && corrections.length > 0) {
      if (!GS.aiCorrections) GS.aiCorrections = [];
      GS.aiCorrections = GS.aiCorrections.concat(corrections);
    }
  }
  ```
- **状态**：✅ 已修复

### 4.2 Bug 2：`temperature` 参数被忽略

- **文件**：`src/api.js:396`
- **原代码**：
  ```js
  temperature: 1   // 硬编码，丢弃第 5 参数
  ```
- **修复后**：
  ```js
  temperature: (typeof temperature === 'number' ? temperature : 1)
  ```
- **状态**：✅ 已修复

---

## 五、Phase 1：状态机分层

### 5.1 目标

把散落的 `GS.xxx = ` 直接赋值收敛到 `dispatch(action)` 通道。**不改 GS 的字段结构**——只是包一层。从这一刻起，所有状态变更都通过一个通道，可加日志、撤销、时间轴调试。

### 5.2 新建文件：`src/store.js`

```js
import { GS, saveGame } from './state.js';

// ==================== 订阅系统 ====================
var _listeners = [];
export function subscribe(fn) {
  _listeners.push(fn);
  return function unsubscribe() {
    var i = _listeners.indexOf(fn);
    if (i >= 0) _listeners.splice(i, 1);
  };
}

function notify(action) {
  for (var i = 0; i < _listeners.length; i++) {
    try { _listeners[i](action); } catch (e) { console.error('[store] listener error:', e); }
  }
}

// ==================== Dispatch ====================
export function dispatch(action) {
  if (!action || !action.type) {
    console.warn('[store] invalid action:', action);
    return;
  }
  #if DEBUG
  console.log('[store] dispatch:', action.type, action.payload || {});
  #endif
  reducer(GS, action);       // 直接 mutate GS（保持兼容）
  saveGame();                // 持久化
  notify(action);           // 通知订阅者
}

// ==================== Reducer ====================
function reducer(state, action) {
  switch (action.type) {

    // ---- 时间推进 ----
    case 'ADVANCE_PHASE':
      state.phaseIndex = action.payload.phaseIndex;
      break;
    case 'ADVANCE_DAY':
      state.day = action.payload.day;
      break;

    // ---- 叙事 ----
    case 'SET_PHASE_NARRATIVE':
      state.phaseNarrative = action.payload.rawText;
      state.parsedNarrative = action.payload.parsed;
      break;
    case 'PUSH_CONSEQUENCE':
      state.consequenceNarratives.push(action.payload);
      break;
    case 'SET_OPTIONS':
      state.currentOptions = action.payload.options;
      break;
    case 'CLEAR_CONSEQUENCES':
      state.consequenceNarratives = [];
      break;

    // ---- 好感度 ----
    case 'ADD_AFFECTION':
      // 注意：实际的递减/疲劳计算仍在 affection-engine.js，这里只做最底层赋值
      if (!state.affection) state.affection = {};
      state.affection[action.payload.memberId] = (state.affection[action.payload.memberId] || 0) + action.payload.delta;
      break;
    case 'ADD_AFFECTION_LOG':
      if (!state.affectionLog) state.affectionLog = {};
      if (!state.affectionLog[action.payload.memberId]) state.affectionLog[action.payload.memberId] = [];
      state.affectionLog[action.payload.memberId].push({
        delta: action.payload.delta,
        reason: action.payload.reason,
        time: Date.now()
      });
      break;
    case 'SET_PENDING_AFF':
      state.pendingAffChanges = action.payload.changes;
      break;
    case 'CLEAR_PENDING_AFF':
      state.pendingAffChanges = [];
      break;

    // ---- 喝酒 ----
    case 'ADD_DRINK':
      if (!state.drinkCounts) state.drinkCounts = {};
      state.drinkCounts[action.payload.name] = (state.drinkCounts[action.payload.name] || 0) + action.payload.count;
      break;

    // ---- 时段状态重置 ----
    case 'RESET_PHASE_STATE':
      state.phaseNarrative = '';
      state.parsedNarrative = { narrative: '', directorOS: '', observerOS: '', interviews: [], memberInterviews: [], xInterviews: [], options: [] };
      state.consequenceNarratives = [];
      state.currentOptions = [];
      state.isInConsequence = false;
      state.smsSentToday = false;
      state.smsTarget = '';
      state.smsDrafts = [];
      state.stayCount = 0;
      state.phaseChoiceCount = 0;
      state.freeInput = '';
      state.prevSummary = '';
      state.prevRawText = '';
      state.pendingJealousy = null;
      break;

    // ---- 文本日志 ----
    case 'PUSH_TODAY_TEXT':
      state.todayFullText.push(action.payload.text);
      break;
    case 'PUSH_DAILY_TEXTS':
      state.dailyFullTexts.push(action.payload.texts);
      break;
    case 'PUSH_DAILY_SUMMARY':
      state.dailySummaries.push(action.payload.summary);
      break;

    // ---- 选项/自由输入计数 ----
    case 'INC_PHASE_CHOICE_COUNT':
      state.phaseChoiceCount++;
      break;
    case 'INC_FREE_INPUT_COUNT':
      state.freeInputCount++;
      break;
    case 'INC_STAY_COUNT':
      state.stayCount++;
      break;

    // ---- AI 校验 ----
    case 'PUSH_CORRECTIONS':
      if (!state.aiCorrections) state.aiCorrections = [];
      state.aiCorrections = state.aiCorrections.concat(action.payload.corrections);
      break;
    case 'CLEAR_CORRECTIONS':
      state.aiCorrections = [];
      break;

    // ---- 游戏状态 ----
    case 'SET_GAME_OVER':
      state.gameOver = true;
      state.finalChoice = action.payload.choice;
      state.finalResult = action.payload.result;
      break;

    // ---- 其他 flag 类 ----
    case 'SET_FLAG':
      state[action.payload.key] = action.payload.value;
      break;

    default:
      console.warn('[store] unknown action type:', action.type);
  }
}

// ==================== Action Creators（可选，方便调用） ====================
export var actions = {
  advancePhase: function(phaseIndex) {
    return { type: 'ADVANCE_PHASE', payload: { phaseIndex: phaseIndex } };
  },
  setPhaseNarrative: function(rawText, parsed) {
    return { type: 'SET_PHASE_NARRATIVE', payload: { rawText: rawText, parsed: parsed } };
  },
  pushConsequence: function(rawText, parsed, choiceText) {
    return { type: 'PUSH_CONSEQUENCE', payload: { rawText: rawText, parsed: parsed, choiceText: choiceText } };
  },
  setOptions: function(options) {
    return { type: 'SET_OPTIONS', payload: { options: options } };
  },
  addAffection: function(memberId, delta) {
    return { type: 'ADD_AFFECTION', payload: { memberId: memberId, delta: delta } };
  },
  addAffectionLog: function(memberId, delta, reason) {
    return { type: 'ADD_AFFECTION_LOG', payload: { memberId: memberId, delta: delta, reason: reason } };
  },
  addDrink: function(name, count) {
    return { type: 'ADD_DRINK', payload: { name: name, count: count || 1 } };
  },
  resetPhaseState: function() {
    return { type: 'RESET_PHASE_STATE' };
  },
  pushTodayText: function(text) {
    return { type: 'PUSH_TODAY_TEXT', payload: { text: text } };
  },
  incPhaseChoiceCount: function() {
    return { type: 'INC_PHASE_CHOICE_COUNT' };
  },
  // ... 其他 action creator
};
```

### 5.3 迁移策略

**第一周：只迁移最高频的 6 个 action**

| Action | 当前直接赋值位置 | 迁移后 |
|---|---|---|
| `SET_PHASE_NARRATIVE` | `game-engine.js` `generatePhaseNarrative` 中 `GS.phaseNarrative = rawText` | `dispatch(actions.setPhaseNarrative(rawText, parsed))` |
| `PUSH_CONSEQUENCE` | 9 处 `GS.consequenceNarratives.push(...)` | `dispatch(actions.pushConsequence(...))` |
| `SET_OPTIONS` | `GS.currentOptions = parsed.options` | `dispatch(actions.setOptions(parsed.options))` |
| `RESET_PHASE_STATE` | `resetPhaseState()` 内 15 行赋值 | `dispatch(actions.resetPhaseState())` |
| `ADD_AFFECTION` | `affection.js` `updateAffection` | `dispatch(actions.addAffection(id, delta))` |
| `ADD_DRINK` | `applyDrinksFromParsed` | `dispatch(actions.addDrink(name, count))` |

**其他地方继续直接赋 GS**，不强制一次性全迁移。`dispatch` 内部仍然 mutate `GS`，所以现有代码完全兼容。

### 5.4 加上时间轴调试（可选）

```js
// 在 dispatch 开头
var _history = [];
export function dispatch(action) {
  _history.push({ time: Date.now(), action: JSON.parse(JSON.stringify(action)), stateSnapshot: JSON.parse(JSON.stringify(GS)) });
  if (_history.length > 100) _history.shift();  // 只保留最近 100 步
  // ...
}

export function getTimeAxis() { return _history; }
export function rewindTo(index) {
  // 把 GS 恢复到第 index 步的状态
  var entry = _history[index];
  if (entry) {
    Object.keys(GS).forEach(function(k) { delete GS[k]; });
    Object.assign(GS, JSON.parse(JSON.stringify(entry.stateSnapshot)));
    saveGame();
    notify({ type: 'REWIND' });
  }
}
```

---

## 六、Phase 2：AI 输出 Schema 强化

### 6.1 目标

- AI 输出严格 JSON，本地做硬校验
- Validator 升级为"双道检查"：error 必须修正，warning 可忽略
- AI 调用失败自动重试，带 correction 反馈
- 模型分层路由：不同场景用不同模型

### 6.2 升级 `schema.js`

当前 `SCENE_SCHEMA` 已经定义了结构。补充：

```js
// schema.js 追加

// AI 输出契约（Phase 4 之后的精简版）
// 注意：options / drinks / 好感度 标记全部从 AI 输出中移除
// 这些都由骨架生成
export var SCENE_SCHEMA_V2 = {
  type: 'object',
  required: ['blocks'],
  properties: {
    blocks: {
      type: 'array',
      minItems: 1,
      items: {
        type: 'object',
        required: ['type', 'content'],
        properties: {
          type: { enum: ['narrative', 'interview', 'xInterview', 'memberInterview', 'directorOS', 'observerOS'] },
          member: { type: 'string' },
          content: { type: 'string', minLength: 1 }
        }
      }
    },
    observers: {
      type: 'array',
      items: {
        type: 'object',
        required: ['name', 'line'],
        properties: {
          name: { type: 'string' },
          line: { type: 'string', maxLength: 50 }
        }
      }
    },
    smsDrafts: {
      type: 'array',
      description: '仅深夜时段输出',
      items: { type: 'string', maxLength: 25 }
    }
    // 不再有 options / drinks / affChanges / secretMissionDone
  }
};

// Phase 4 过渡期（向后兼容）继续用原 SCENE_SCHEMA
// 骨架完全接管后切换到 SCENE_SCHEMA_V2
```

### 6.3 校验升级：`sanitizeScene` 加硬校验

```js
// schema.js 追加

export function sanitizeSceneV2(obj, type) {
  if (!obj || typeof obj !== 'object') {
    return { blocks: [], observers: [], smsDrafts: [] };
  }
  var out = {
    blocks: Array.isArray(obj.blocks) ? obj.blocks.filter(function(b) {
      return b && typeof b === 'object'
        && BLOCK_TYPES.indexOf(b.type) >= 0
        && typeof b.content === 'string' && b.content.trim().length > 0;
    }).map(function(b) {
      return {
        type: b.type,
        member: typeof b.member === 'string' ? b.member.trim() : '',
        content: b.content.trim()
      };
    }) : [],
    observers: Array.isArray(obj.observers) ? obj.observers.filter(function(o) {
      return o && typeof o.name === 'string' && typeof o.line === 'string'
        && o.line.trim().length > 0 && o.line.trim().length <= 80;
    }).map(function(o) {
      return { name: o.name.trim(), line: o.line.trim() };
    }) : [],
    smsDrafts: Array.isArray(obj.smsDrafts) ? obj.smsDrafts.filter(function(s) {
      return typeof s === 'string' && s.trim().length > 0 && s.trim().length <= 30;
    }).map(function(s) { return s.trim(); }) : []
  };
  // 硬丢弃：options / drinks / affChanges / secretMissionDone 一律忽略
  // 骨架完全接管后，AI 不再返回这些字段
  return out;
}
```

### 6.4 Validator 双道检查

```js
// validator.js 升级

export function validateNarrative(rawText, parsed) {
  var corrections = [];

  // 现有 7 条规则保留，每条返回 { severity, type, message }
  // severity: 'error'（必须修正） | 'warning'（可忽略）

  var personalityCorr = checkPersonalityDeviation(parsed);
  var xLeakCorr = checkXIdentityLeak(parsed);
  // ... 其他 5 条

  // 新增结构化校验
  if (!parsed.blocks || parsed.blocks.length === 0) {
    corrections.push({ severity: 'error', type: 'format', message: '正文为空' });
  }
  if (parsed.narrative && parsed.narrative.length < 200) {
    corrections.push({ severity: 'warning', type: 'length', message: '正文过短（' + parsed.narrative.length + '字）' });
  }
  if (parsed.narrative && parsed.narrative.length > 3000) {
    corrections.push({ severity: 'warning', type: 'length', message: '正文过长（' + parsed.narrative.length + '字）' });
  }

  return corrections;
}
```

### 6.5 自动重试包装：`src/ai-generator.js`

```js
// src/ai-generator.js（新建）
import { callDeepSeek } from './api.js';
import { parseNarrative, sanitizeSceneV2 } from './schema.js';
import { validateNarrative } from './validator.js';
import { dispatch } from './store.js';

// 生成并校验，带 1 次自动重试
export async function generateWithRetry(sysPrompt, userMsg, opts) {
  opts = opts || {};
  var tokens = opts.tokens || 5000;
  var temp = opts.temperature || 0.7;
  var sceneType = opts.sceneType || 'phase';

  for (var attempt = 0; attempt < 2; attempt++) {
    var raw = await callDeepSeek(sysPrompt, userMsg, tokens, true, temp);
    var parsed = parseNarrative(raw);
    var corr = validateNarrative(raw, parsed);

    // error 级必须修正
    var errors = corr.filter(function(c) { return c.severity === 'error'; });
    if (errors.length === 0) {
      // 通过校验，推送 corrections 并返回
      if (corr.length > 0) dispatch({ type: 'PUSH_CORRECTIONS', payload: { corrections: corr } });
      return { raw: raw, parsed: parsed, corrections: corr };
    }

    // 有 error，带反馈重试一次
    if (attempt === 0) {
      console.warn('[ai-generator] retrying due to errors:', errors.map(function(e) { return e.message; }));
      userMsg += '\n\n[INSTRUCTION] ⚠️ 上次输出有以下问题，请修正：\n' +
        errors.map(function(e) { return '- ' + e.message; }).join('\n');
    }
  }

  // 两次都失败，接受结果但记录 error
  console.error('[ai-generator] failed after retry, errors:', errors);
  if (corr.length > 0) dispatch({ type: 'PUSH_CORRECTIONS', payload: { corrections: corr } });
  return { raw: raw, parsed: parsed, corrections: corr };
}
```

### 6.6 模型分层路由

在 `data.js` 的 `API_PROVIDERS` 加字段：

```js
// data.js 修改 API_PROVIDERS
var API_PROVIDERS = {
  deepseek: {
    name: 'DeepSeek',
    endpoint: 'https://api.deepseek.com/v1',
    model: 'deepseek-chat',
    models: [
      { value: 'deepseek-chat', label: 'DeepSeek Chat（主）' },
      { value: 'deepseek-reasoner', label: 'DeepSeek Reasoner（重）' }
    ],
    // 新增：场景分层默认模型
    sceneModels: {
      heavy: 'deepseek-chat',      // 主线剧情
      light: 'deepseek-chat',      // 短信/送礼
      cheap: 'deepseek-chat'       // 记忆压缩
    }
  },
  siliconflow: {
    name: '硅基流动',
    endpoint: 'https://api.siliconflow.cn/v1',
    model: 'Qwen/Qwen2.5-7B-Instruct',
    models: [
      { value: 'Qwen/Qwen2.5-7B-Instruct', label: 'Qwen 7B（轻）' },
      { value: 'deepseek-ai/DeepSeek-V3', label: 'DeepSeek V3（重）' },
      { value: 'Qwen/Qwen2.5-72B-Instruct', label: 'Qwen 72B（重）' }
    ],
    sceneModels: {
      heavy: 'deepseek-ai/DeepSeek-V3',
      light: 'Qwen/Qwen2.5-7B-Instruct',
      cheap: 'Qwen/Qwen2.5-7B-Instruct'
    }
  },
  qwen: {
    name: '千问',
    endpoint: 'https://dashscope.aliyuncs.com/compatible-mode/v1',
    model: 'qwen-plus',
    models: [
      { value: 'qwen-plus', label: 'Qwen Plus（主）' },
      { value: 'qwen-turbo', label: 'Qwen Turbo（快）' },
      { value: 'qwen-max', label: 'Qwen Max（重）' }
    ],
    sceneModels: {
      heavy: 'qwen-plus',
      light: 'qwen-turbo',
      cheap: 'qwen-turbo'
    }
  }
};
```

`api.js` 根据场景选模型：

```js
function pickModel(sceneType) {
  var cfg = getProviderConfig();
  // 玩家手动覆盖优先
  if (GS.apiModel) return GS.apiModel;
  // 场景路由
  var heavy = ['phase', 'consequence', 'finalResult', 'truth'];
  var light = ['sms', 'gift', 'midnightCall', 'freeAction', 'stay'];
  var cheap = ['compress', 'xItems', 'dailySummary'];
  if (heavy.indexOf(sceneType) >= 0) return cfg.sceneModels.heavy || cfg.model;
  if (light.indexOf(sceneType) >= 0) return cfg.sceneModels.light || cfg.model;
  if (cheap.indexOf(sceneType) >= 0) return cfg.sceneModels.cheap || cfg.model;
  return cfg.model;
}
```

`callDeepSeek` 加 `sceneType` 参数（第 6 个参数，可选）：

```js
export async function callDeepSeek(systemPrompt, userMessage, maxTokens, useJson, temperature, sceneType) {
  // ...
  var model = pickModel(sceneType || 'phase');
  // ...
}
```

---

## 七、Phase 3：Prompt 模块化拆分

### 7.1 目标

把 628 行的 `prompts.js` 拆成模块化结构。**按职责切，不按场景切**。

### 7.2 目标目录结构

```
src/
  prompts/
    index.js                ← 重新导出 buildSystemPrompt / buildUserMessage（兼容）
    system-prompt.js        ← buildSystemPrompt 拆出
    context-snapshot.js     ← 状态快照注入（替代散落的字符串拼接）
    scene-types/
      phase.js              ← Day X 时段开头
      consequence.js        ← 选项后续
      sms.js                ← 短信
      freeAction.js         ← 自由行动
      stay.js               ← 继续今天
      finalResult.js        ← 结局
      truth.js              ← 真心话
    rules/
      writing-rules.js      ← 全局写作规则
      personality.js        ← 成员性格约束
      affection.js          ← 好感度行为参考
      x-rules.js            ← X 身份/揭示节奏
    injectors/
      jealousy.js           ← 嫉妒任务
      ex-message.js         ← 前任来电
      midnight-call.js      ← 午夜电话亭
      question-box.js       ← 提问箱
      truth-punishment.js
```

### 7.3 上下文快照：`context-snapshot.js`

替代当前散落在 `buildUserMessage` 里的状态注入。

```js
// src/prompts/context-snapshot.js
import { GS } from '../state.js';
import { MEMBERS, PHASES } from '../data.js';
import { getMandatoryTask } from '../formatters.js';

export function buildContextSnapshot() {
  return {
    day: GS.day,
    phase: GS.phaseIndex,
    phaseLabel: PHASES[GS.phaseIndex],
    season: GS.season,
    weather: GS.weather,
    holiday: GS.todayHoliday ? GS.todayHoliday.name : null,
    dateLabel: GS.gameMonth + '月' + GS.gameDay + '日',
    mandatoryTask: getMandatoryTask(GS.day, PHASES[GS.phaseIndex]),
    selectedMembers: GS.selectedMembers.map(function(id) {
      var m = MEMBERS.find(function(x) { return x.id === id; });
      return { id: m.id, name: m.name, stageName: m.stageName };
    }),
    secretX: GS.secretX,
    affection: Object.assign({}, GS.affection),
    drinkCounts: Object.assign({}, GS.drinkCounts),
    todayKeyEvents: GS.todayRevealedInfo || '',
    pendingPromises: GS.pendingPromises || [],
    truthState: GS.truthState
  };
}

export function snapshotToText(snap) {
  var lines = ['[CONTEXT]'];
  lines.push('Day ' + snap.day + ' ' + snap.phaseLabel + ' ' + snap.dateLabel);
  if (snap.season) lines.push('季节：' + snap.season + ' 天气：' + snap.weather);
  if (snap.holiday) lines.push('特别日：' + snap.holiday);
  if (snap.mandatoryTask) lines.push('强制任务：' + snap.mandatoryTask);
  lines.push('在场成员：' + snap.selectedMembers.map(function(m) { return m.name; }).join('、'));
  if (snap.truthState) lines.push('真心话状态：' + JSON.stringify(snap.truthState));
  if (snap.pendingPromises && snap.pendingPromises.length > 0) {
    lines.push('待兑现约定：' + snap.pendingPromises.join('；'));
  }
  lines.push('[/CONTEXT]');
  return lines.join('\n');
}
```

### 7.4 拆分步骤（每步保持可运行）

**Step 1：** 创建 `prompts/` 目录和 `prompts/index.js`，内容就是把原 `prompts.js` 整个搬过去，改名为 `prompts/legacy.js`，`index.js` 重新导出。

**Step 2：** 把 `buildSystemPrompt` 拆到 `system-prompt.js`。先原样搬，不改逻辑。验证 `npm run build` + 手动测试。

**Step 3：** 把规则块逐个搬到 `rules/*.js`，`system-prompt.js` import 它们拼接。每搬一个跑构建。

**Step 4：** 把 `buildUserMessage` 的 `if (type === 'phase')` 分支抽到 `scene-types/phase.js`，其他分支不动。

**Step 5：** 逐个搬迁 `scene-types/` 下的其他分支。

**Step 6：** 把条件注入搬到 `injectors/*.js`。

**Step 7：** 引入 `context-snapshot.js`，替代 `buildUserMessage` 里散落的状态注入。

**Step 8：** `prompts/index.js` 退化为只做组装。

### 7.5 AGENTS.md 规则例外

当前 AGENTS.md 规定用 `+` 不用模板字符串。**在 prompts 模块允许例外**——否则拆分后字符串拼接会更难维护。建议在 AGENTS.md 里加：

> prompt 模块允许使用模板字符串，其他业务代码保持 `+`。

---

## 八、Phase 4：游戏骨架抽取（核心阶段）

> **这是整个迁移的核心**。目标：把剧情走向、好感度、副作用从 AI 手里拿走，AI 只负责生成文案。

### 8.1 目标

| 职责 | 当前由谁 | 迁移后由谁 |
|---|---|---|
| 决定 Day X 的强制任务 | `getMandatoryTask` + AI prompt | `skeleton/scheduler.js` |
| 生成选项 A/B/C | AI 输出 `options[]` | `skeleton/option-engine.js` |
| 好感度 ±N | AI 输出 `affName/affDelta` | `skeleton/affection-engine.js` |
| 喝酒计数 | AI 输出 `drinks[]` | `skeleton/drink-engine.js` |
| 触发随机事件 | AI 描述 | `skeleton/event-triggers.js` |
| 触发嫉妒/前任来电 | `checkJealousyEvent` + AI | `skeleton/event-triggers.js` |
| 推进 phase/day | `advancePhase` + AI | `skeleton/scheduler.js` |
| 正文叙事 | AI | AI（不变） |
| 采访间/导演OS/观察员OS | AI | AI（不变） |
| 短信草稿 | AI | AI（不变） |

### 8.2 新建目录：`src/skeleton/`

```
src/
  skeleton/
    index.js               ← 总入口，导出骨架的所有功能
    scheduler.js           ← 日程推进（Day/Phase 推进、强制任务）
    option-engine.js       ← 选项生成（基于事件图谱 + 当前状态）
    affection-engine.js    ← 好感度结算（根据玩家选择计算 ±N）
    drink-engine.js        ← 喝酒计数（真心话/提问箱规则）
    event-triggers.js      ← 事件触发（嫉妒/前任来电/随机事件）
    scene-router.js        ← 场景路由（决定调用哪个 AI 生成任务）
    storylets/
      day-1.js ... day-12.js  ← 每天的剧情骨架定义
      common.js            ← 通用事件
```

### 8.3 `skeleton/scheduler.js`：日程推进

```js
// src/skeleton/scheduler.js
import { GS } from '../state.js';
import { dispatch } from '../store.js';
import { PHASES, PHASE_LABELS } from '../data.js';
import { getMandatoryTask } from '../formatters.js';

// 推进到下一时段
export async function advancePhase() {
  if (GS.dayCompleted) return;
  if (GS.phaseIndex < 3) {
    dispatch({ type: 'ADVANCE_PHASE', payload: { phaseIndex: GS.phaseIndex + 1 } });
    dispatch({ type: 'RESET_PHASE_STATE' });
    await generatePhaseScene();
  } else {
    await goToNextDay();
  }
}

// 进入下一天
export async function goToNextDay() {
  if (GS.day >= 12) {
    dispatch({ type: 'SET_GAME_OVER', payload: { choice: '', result: '' } });
    return;
  }
  // 压缩今天的剧情到摘要
  await compressToday();
  // 显示记忆回顾面板
  dispatch({ type: 'SET_FLAG', payload: { key: 'pendingMemoryReview', value: generateMemoryReview() } });
  // 玩家确认后 proceedToNextDay
}

export async function proceedToNextDay() {
  dispatch({ type: 'ADVANCE_DAY', payload: { day: GS.day + 1 } });
  // 清理过期状态
  cleanupExpiredStates();
  // 推进日期/天气/节日
  advanceDate();
  // 生成新的一天
  await generatePhaseScene();
}

// 生成当前时段的场景（骨架调用 AI）
async function generatePhaseScene() {
  var day = GS.day;
  var phase = GS.phaseIndex;
  var mandatoryTask = getMandatoryTask(day, PHASES[phase]);

  // 1. 骨架决定是否触发特殊事件
  var events = checkEvents(day, phase);

  // 2. 骨架生成选项（不由 AI 决定）
  var options = generateOptions(day, phase, events);

  // 3. 构造 AI 生成任务
  var task = {
    sceneType: 'phase',
    wordCount: getPhaseWordCount(day, phase),
    mandatoryTask: mandatoryTask,
    events: events
  };

  // 4. 调 AI 生成文案（只生成 blocks + observers + smsDrafts）
  var sysPrompt = buildSystemPrompt();
  var userMsg = buildUserMessage('phase', { task: task });
  var result = await generateWithRetry(sysPrompt, userMsg, {
    tokens: TOKEN_CONFIG.phaseNarrative,
    temperature: 0.7,
    sceneType: 'phase'
  });

  // 5. 骨架应用结果
  dispatch({ type: 'SET_PHASE_NARRATIVE', payload: { rawText: result.raw, parsed: result.parsed } });
  dispatch({ type: 'SET_OPTIONS', payload: { options: options } });  // 选项来自骨架，不来自 AI
  dispatch({ type: 'PUSH_TODAY_TEXT', payload: { text: result.raw } });
  notify();
}
```

### 8.4 `skeleton/option-engine.js`：选项生成

```js
// src/skeleton/option-engine.js
import { GS } from '../state.js';
import { MEMBERS, QUESTION_BOX_QUESTIONS, TRUTH_CARDS } from '../data.js';
import { getMandatoryTask } from '../formatters.js';

// 根据当前场景生成选项
export function generateOptions(day, phase, events) {
  var phaseLabel = PHASES[phase];

  // 特殊日期的选项
  if (day === 4 || day === 6 || day === 8 || day === 9) {
    if (phase === 0) {
      // 约会日上午：只有"进入约会场景"按钮
      return [{ text: '▶ 进入约会场景', action: 'ENTER_DATING' }];
    }
  }

  if (day === 11 && GS.truthState && GS.truthState.active) {
    // 真心话：不生成常规选项
    return [];
  }

  if (GS.questionBox && GS.questionBox.active) {
    // 提问箱：固定两个按钮
    return [
      { text: '如实回答', action: 'QB_ANSWER' },
      { text: '拒绝回答并喝酒', action: 'QB_DRINK' }
    ];
  }

  // 默认：从事件图谱生成 3 个选项
  return generateStandardOptions(day, phase, events);
}

// 生成标准选项（非特殊场景）
function generateStandardOptions(day, phase, events) {
  var members = GS.selectedMembers.map(function(id) {
    return MEMBERS.find(function(m) { return m.id === id; });
  });

  var options = [];

  // 选项 1：和某成员互动
  var target1 = pickOptionTarget(members, 0);
  options.push({
    text: '和' + target1.name + '一起' + randomActivity(day, phase),
    action: 'INTERACT',
    payload: { memberId: target1.id },
    affEffects: [
      { memberId: target1.id, delta: +3, reason: '主动互动' },
      // 其他人小幅变化
      { memberId: otherMemberId(members, target1, 0), delta: -1, reason: '忽略了' }
    ]
  });

  // 选项 2：和另一成员互动
  var target2 = pickOptionTarget(members, 1);
  options.push({
    text: '找' + target2.name + randomActivity2(day, phase),
    action: 'INTERACT',
    payload: { memberId: target2.id },
    affEffects: [
      { memberId: target2.id, delta: +3, reason: '主动互动' },
      { memberId: otherMemberId(members, target2, 0), delta: -1, reason: '忽略了' }
    ]
  });

  // 选项 3：独自行动
  options.push({
    text: '独自待一会儿',
    action: 'SOLO',
    affEffects: members.map(function(m) {
      return { memberId: m.id, delta: -1, reason: '没有互动' };
    })
  });

  return options;
}

// 选项活动词库（按 day/phase 变化）
function randomActivity(day, phase) {
  var acts = ['做饭', '聊天', '散步', '看电影', '打扫', '整理行李'];
  // 根据 day 和 phase 调整
  if (phase === 0) return ['做饭', '准备早餐', '聊天'].sample();
  if (phase === 3) return ['聊天', '看星星', '夜谈'].sample();
  return acts.sample();
}
```

### 8.5 `skeleton/affection-engine.js`：好感度结算

```js
// src/skeleton/affection-engine.js
import { GS } from '../state.js';
import { dispatch } from '../store.js';
import { MEMBERS } from '../data.js';

// 结算一次选择的好感度变化
export function settleChoiceAffection(option) {
  if (!option.affEffects) return;

  for (var i = 0; i < option.affEffects.length; i++) {
    var eff = option.affEffects[i];
    var finalDelta = calculateDelta(eff);
    dispatch({ type: 'ADD_AFFECTION', payload: { memberId: eff.memberId, delta: finalDelta } });
    dispatch({ type: 'ADD_AFFECTION_LOG', payload: {
      memberId: eff.memberId,
      delta: finalDelta,
      reason: eff.reason
    }});
  }
}

// 计算实际 delta（考虑递减/疲劳/负面增强）
function calculateDelta(eff) {
  var delta = eff.delta;
  var reason = eff.reason || '';

  // 负面行为扣分增加
  if ((reason.indexOf('吃醋') >= 0 || reason.indexOf('嫉妒') >= 0) && delta < 0) {
    delta = -5;
  }
  if ((reason.indexOf('未收到') >= 0 || reason.indexOf('忽略') >= 0) && delta < 0) {
    delta = -3;
  }

  // 同一行为重复做效果递减
  if (!GS.affActionHistory) GS.affActionHistory = {};
  var actionKey = eff.memberId + '_' + reason;
  var count = GS.affActionHistory[actionKey] || 0;
  if (count >= 1 && delta > 0) {
    if (count === 1) delta = Math.max(1, delta - 2);
    else if (count >= 2) delta = Math.max(1, delta - 4);
  }
  GS.affActionHistory[actionKey] = count + 1;

  // 疲劳值
  if (!GS.affFatigue) GS.affFatigue = {};
  var lastTarget = GS.affFatigue._lastTarget;
  if (lastTarget === eff.memberId && delta > 0) {
    var fatigue = GS.affFatigue[eff.memberId] || 0;
    delta = Math.max(1, delta - Math.floor(fatigue / 2));
    GS.affFatigue[eff.memberId] = fatigue + 1;
  } else if (delta > 0) {
    GS.affFatigue[eff.memberId] = 0;
  }
  if (delta > 0) GS.affFatigue._lastTarget = eff.memberId;

  return delta;
}

// 检查成员对女主的最终选择（结局用）
export function determineMemberChoice(memberId) {
  var aff = GS.affection[memberId] || 0;
  if (aff >= 80) return 'choose';       // 选择女主
  if (aff >= 50) return 'hesitate';     // 犹豫
  return 'reject';                       // 拒绝
}
```

### 8.6 `skeleton/drink-engine.js`：喝酒计数

```js
// src/skeleton/drink-engine.js
import { GS } from '../state.js';
import { dispatch } from '../store.js';

// 成员真心话回合自动判定
export function autoTruthJudge(memberId) {
  // 50% 回答，50% 喝酒
  var willDrink = Math.random() < 0.5;
  if (willDrink) {
    dispatch({ type: 'ADD_DRINK', payload: { name: getMemberName(memberId), count: 1 } });
    return { action: 'drink', memberId: memberId };
  }
  return { action: 'answer', memberId: memberId };
}

// 女主拒绝回答 +2 杯
export function heroineRefuseDrink() {
  dispatch({ type: 'ADD_DRINK', payload: { name: '女主', count: 2 } });
}

// 检查是否触发醉酒
export function checkDrunkTrigger() {
  var count = GS.drinkCounts['女主'] || 0;
  return count >= 2;
}
```

### 8.7 `skeleton/event-triggers.js`：事件触发

```js
// src/skeleton/event-triggers.js
import { GS } from '../state.js';
import { randInt } from '../utils.js';

// 嫉妒任务触发检查（25% 概率）
export function checkJealousyMissionTrigger() {
  if (GS.jealousyTriggeredToday) return false;
  if (GS.jealousyMissionTriggeredDays && GS.jealousyMissionTriggeredDays.indexOf(GS.day) >= 0) return false;
  // 约会日/强制任务日不触发
  if ([4, 5, 6, 7, 8, 9, 10, 11, 12].indexOf(GS.day) >= 0) return false;
  // 需要至少 2 个成员好感度 ≥ 30
  var highAffCount = GS.selectedMembers.filter(function(id) {
    return (GS.affection[id] || 0) >= 30;
  }).length;
  if (highAffCount < 2) return false;
  // 25% 概率
  return Math.random() < 0.25;
}

// 前任来电触发检查（Day 5-9 傍晚/深夜，15% 概率）
export function checkExMessageTrigger() {
  if (GS.day < 5 || GS.day > 9) return false;
  if (GS.phaseIndex < 2) return false;
  if (GS.exMessage) return false;
  // 需要至少一位非 X 成员好感度 ≥ 20
  var hasEligible = GS.selectedMembers.some(function(id) {
    return id !== GS.secretX && (GS.affection[id] || 0) >= 20;
  });
  if (!hasEligible) return false;
  return Math.random() < 0.15;
}

// 随机事件触发
export function checkRandomEvent() {
  if (GS.todayRandomEventTriggered) return null;
  if (Math.random() < 0.15) {
    GS.todayRandomEventTriggered = true;
    return pickRandomEvent();
  }
  return null;
}
```

### 8.8 `skeleton/scene-router.js`：场景路由

```js
// src/skeleton/scene-router.js
import { GS } from '../state.js';

// 决定当前应该走哪个 AI 生成任务
export function routeScene() {
  var day = GS.day;
  var phase = GS.phaseIndex;
  var hasConsequence = GS.consequenceNarratives.length > 0;
  var isTruthActive = GS.truthState && GS.truthState.active;
  var isQuestionBoxActive = GS.questionBox && GS.questionBox.active;

  if (isTruthActive) return 'truth';
  if (isQuestionBoxActive) return 'questionBox';
  if (GS.drunkTrigger) return 'drunk';
  if (hasConsequence && GS.isInConsequence) return 'consequence';
  if (GS.smsSentToday) return 'sms';
  if (GS.gameOver) return 'finalResult';
  return 'phase';
}
```

### 8.9 AI 输出契约变更

**Phase 4 之后，AI 只输出以下字段**：

```json
{
  "blocks": [
    { "type": "narrative", "content": "..." },
    { "type": "interview", "content": "..." },
    { "type": "memberInterview", "member": "尹净汉", "content": "..." },
    { "type": "directorOS", "content": "..." }
  ],
  "observers": [
    { "name": "李龙真", "line": "..." },
    { "name": "夫胜宽", "line": "..." }
  ],
  "smsDrafts": ["...", "...", "..."]
}
```

**不再输出**：
- `options[]` — 由 `option-engine.js` 生成
- `drinks[]` — 由 `drink-engine.js` 记录
- `affName/affDelta` — 由 `affection-engine.js` 结算
- `secretMissionDone` — 由骨架根据玩家操作判定

### 8.10 Prompt 变更

`buildSystemPrompt` 中关于"选项生成"的指令全部删除。追加：

```
[INSTRUCTION] 你只负责生成剧情文案，不负责：
- 生成选项（选项由系统自动生成）
- 好感度变化（好感度由系统结算）
- 喝酒计数（由系统记录）
- 触发事件（由系统判断）
你只需输出 blocks、observers、smsDrafts 三个字段。
```

`buildUserMessage` 中，`type === 'consequence'` 分支不再要求 AI 生成 `options`，改为只要求生成正文文案：

```
请生成约 1000 字的后续剧情，包含正文 + 采访间 + 导演OS + 观察员OS。不需要生成选项。
```

### 8.11 迁移步骤（关键！分步执行）

**Step 1：先建骨架文件，但只接管好感度**

- 新建 `skeleton/affection-engine.js`
- `option-engine.js` 先沿用 AI 输出的 `options`，只是在选项上加 `affEffects` 字段
- `handleOptionChoice` 中，好感度结算从 `parser.js` 的 `affChanges` 切换到 `affection-engine.js` 的 `settleChoiceAffection(opt)`
- AI prompt 仍保留输出 `affName/affDelta`（向后兼容），但骨架**忽略**这些字段
- 这一步只改好感度来源，不动其他

**Step 2：接管喝酒计数**

- 新建 `skeleton/drink-engine.js`
- `handleTruthRound` 中，成员喝酒判定从 AI 输出切换到 `autoTruthJudge`
- AI prompt 中删除"喝了一杯的人"的指令
- `applyDrinksFromParsed` 退化为只处理极少场景（如提问箱女主主动喝酒）

**Step 3：接管选项生成**

- 新建 `skeleton/option-engine.js`
- 在 `generatePhaseNarrative` 结束后，**覆盖** `GS.currentOptions`（用骨架生成的选项替代 AI 的）
- AI prompt 中删除"生成 3 个选项"的指令
- 这一步改动最大，建议先只在一个 Day（如 Day 3）试点
- 试点稳定后再全量切换

**Step 4：接管事件触发**

- 新建 `skeleton/event-triggers.js`
- 把 `checkJealousyMissionTrigger`、`checkExMessageTrigger`、`checkRandomEvent` 从 `game-engine.js` 搬过来
- 这些函数当前已经基本是骨架式的（不靠 AI），只需搬家

**Step 5：接管日程推进**

- 新建 `skeleton/scheduler.js`
- `advancePhase` / `goToNextDay` / `proceedToNextDay` 从 `game-engine.js` 搬过来
- 改用 `dispatch` 而不是直接赋值

**Step 6：`game-engine.js` 退化为薄层**

- `game-engine.js` 只剩"调用骨架 + 调 AI + 应用结果"的薄编排
- UI 弹窗函数（`showDatingDiceModal` 等）搬到 `modals/` 下

**每一步都跑 `npm run build` + 手动测试 Day 1-3 流程**。

---

## 九、Phase 5：UI 组件化 + 消灭循环依赖

### 9.1 目标

- 拆 `ui-renderer.js` 的 1360 行 `innerHTML` 拼接
- 引入事件委托，消灭逐个 `addEventListener`
- 局部更新替代全量重绘
- 消灭 `window.__renderAll`，通过 `store.subscribe` 驱动渲染

### 9.2 新建目录：`src/components/`

```
src/
  components/
    header.js              ← 顶栏（日期、好感度徽章、按钮）
    narrative-box.js      ← 剧情渲染区
    option-bar.js         ← 选项按钮区
    action-bar.js         ← 行动按钮（重新生成、跳时段、短信）
    free-input.js         ← 自由输入框
    affection-panel.js    ← 好感度面板（modal）
    sms-panel.js          ← 短信 UI（modal）
    x-archive-modal.js    ← X 档案（modal）
    gift-panel.js         ← 礼物（modal）
    truth-card.js         ← 真心话卡片
    drink-counter.js      ← 喝酒计数面板
    setup-wizard.js       ← 创建游戏向导
    modal-factory.js      ← 通用 modal 工厂
    event-delegator.js    ← 事件委托中心
```

### 9.3 事件委托：`event-delegator.js`

```js
// src/components/event-delegator.js
// 顶层一次性绑定，替换所有 addEventListener

export function bindEventDelegator(rootEl) {
  rootEl.addEventListener('click', function(e) {
    var btn = e.target.closest('[data-action]');
    if (!btn) return;
    var action = btn.dataset.action;
    var payload = btn.dataset.payload ? JSON.parse(btn.dataset.payload) : {};
    handleAction(action, payload);
  });

  rootEl.addEventListener('input', function(e) {
    var input = e.target.closest('[data-input]');
    if (!input) return;
    handleInput(input.dataset.input, input.value);
  });

  rootEl.addEventListener('submit', function(e) {
    e.preventDefault();
    var form = e.target.closest('[data-form]');
    if (!form) return;
    handleSubmit(form.dataset.form, new FormData(form));
  });
}

function handleAction(action, payload) {
  // 由骨架/引擎处理
  import('../skeleton/index.js').then(function(skel) {
    skel.handleAction(action, payload);
  });
}
```

### 9.4 局部更新：`subscribe` 驱动渲染

```js
// src/components/narrative-box.js
import { subscribe } from '../store.js';
import { GS } from '../state.js';

var _rootEl = null;

export function initNarrativeBox(el) {
  _rootEl = el;
  subscribe(function(action) {
    if (action.type === 'SET_PHASE_NARRATIVE' || action.type === 'PUSH_CONSEQUENCE') {
      updateNarrativeBox();
    }
  });
}

function updateNarrativeBox() {
  if (!_rootEl) return;
  var html = renderNarrativeHTML(GS.parsedNarrative, GS.consequenceNarratives);
  _rootEl.innerHTML = html;
}

function renderNarrativeHTML(parsed, consequences) {
  // ... 渲染逻辑
}
```

### 9.5 消灭 `window.__renderAll`

**当前**：`game-engine.js` 调 `window.__renderAll()`，因为 `game-engine.js` 不能 import `ui-renderer.js`（循环依赖）。

**改造后**：
- `game-engine.js` 调 `dispatch({ type: 'GENERATED_NARRATIVE' })`
- 各组件订阅自己关心的 action，自动更新
- 循环依赖自然消失

具体：
1. `game-engine.js` 所有 `window.__renderAll()` 替换为 `dispatch({ type: 'RERENDER' })`（或更细粒度的 action）
2. 删除 `init.js:9` 的 `window.__renderAll = renderAll`
3. 各组件 `subscribe` 监听 `RERENDER` 或更具体的 action
4. `ui-renderer.js` 的 `renderAll` 退化为初始化时调一次，之后由订阅驱动

### 9.6 拆分 `game-engine.js` 的 UI 部分

当前 `game-engine.js` 混了 6 个 UI 弹窗函数：

| 函数 | 行 | 搬到 |
|---|---|---|
| `showDatingDiceModal` | 1297 | `components/dating-dice-modal.js` |
| `showDay10DatingModal` | 1393 | `components/day10-dating-modal.js` |
| `showMidnightCallModal` | 1429 | `components/midnight-call-modal.js` |
| `showDrunkMemberSelectModal` | 394 | `components/drunk-member-modal.js` |
| `showTruthAnswerModal` | 451 | `components/truth-answer-modal.js` |
| `showQuestionBoxAnswerModal` | 1541 | `components/question-box-modal.js` |

搬完后 `game-engine.js` 只剩业务逻辑（`generatePhaseNarrative`、`handleOptionChoice`、`goToNextDay` 等）。

### 9.7 迁移步骤

**Step 1：** 新建 `components/modal-factory.js`，把 `modals.js` 里的 `createModal` 函数搬过去。

**Step 2：** 新建 `components/event-delegator.js`，在 `init.js` 里绑定一次。

**Step 3：** 逐个新建组件文件，每个组件只负责渲染自己的 HTML + 绑定自己范围内的事件（改用 `data-action` 属性）。

**Step 4：** 在 `init.js` 里初始化所有组件，每个组件 `subscribe` 自己关心的 action。

**Step 5：** `game-engine.js` 里的 `window.__renderAll()` 全部替换为 `dispatch`。

**Step 6：** 删除 `window.__renderAll`。

**Step 7：** `ui-renderer.js` 退化为"初始化 + 组件协调器"。

---

## 十、Phase 6：事件图谱（Storylets）

### 10.1 目标（可选阶段）

把硬编码的 `getMandatoryTask` 和散落的"某天某时段触发某事"的逻辑，抽象为事件图谱。**此阶段可选，如果当前线性 12 天架构能满足需要，可延后**。

### 10.2 事件 YAML 结构

```yaml
# src/skeleton/storylets/day-3.yaml
- id: day3_morning_intro
  requires:
    day: 3
    phase: 0
  narrative_prompt: "Day 3 早上，新的成员入驻……"
  options:
    - text: "去厨房帮忙"
      action: INTERACT
      payload: { memberId: mingyu }
      affEffects:
        - { memberId: mingyu, delta: +3, reason: "一起做饭" }
        - { memberId: wonwoo, delta: -1, reason: "忽略了" }
    - text: "在客厅聊天"
      action: INTERACT
      payload: { memberId: wonwoo }
      affEffects:
        - { memberId: wonwoo, delta: +3, reason: "聊天" }
        - { memberId: mingyu, delta: -1, reason: "没去帮忙" }
    - text: "独自整理行李"
      action: SOLO
      affEffects:
        - { memberId: mingyu, delta: -1, reason: "没有互动" }
        - { memberId: wonwoo, delta: -1, reason: "没有互动" }
```

### 10.3 事件调度器

```js
// src/skeleton/storylets/scheduler.js
export function getActiveEvents() {
  var candidates = ALL_EVENTS.filter(function(e) {
    return checkRequires(e.requires);
  });
  return candidates;
}

function checkRequires(requires) {
  if (requires.day && requires.day !== GS.day) return false;
  if (requires.phase && requires.phase !== GS.phaseIndex) return false;
  if (requires.flag && !GS[requires.flag]) return false;
  if (requires.notFlag && GS[requires.notFlag]) return false;
  if (requires.affGte) {
    for (var memberId in requires.affGte) {
      if ((GS.affection[memberId] || 0) < requires.affGte[memberId]) return false;
    }
  }
  return true;
}
```

### 10.4 与现有代码共存

- Day 1-2 先迁移到事件图谱试点
- 其他天保持现有 `getMandatoryTask` + AI 自由生成
- 试点稳定后再逐天迁移

---

## 十一、Phase 7：长期记忆与角色性格向量

### 7.1 长期记忆：事件日志 + 检索

```js
// src/event-log.js
import { GS } from './state.js';

export function logEvent(event) {
  if (!GS.eventLog) GS.eventLog = [];
  GS.eventLog.push({
    t: Date.now(),
    day: GS.day,
    phase: GS.phaseIndex,
    type: event.type,
    members: event.members,
    tags: event.tags,
    summary: event.summary
  });
  // 限制最多 500 条
  if (GS.eventLog.length > 500) GS.eventLog.shift();
}

// 检索相关记忆（简单版：关键词匹配）
export function retrieveRelevantMemories(query, k) {
  if (!GS.eventLog) return [];
  return GS.eventLog
    .map(function(e) {
      return { e: e, score: keywordOverlap(e.tags, query.tags) };
    })
    .filter(function(x) { return x.score > 0; })
    .sort(function(a, b) { return b.score - a.score; })
    .slice(0, k || 5)
    .map(function(x) { return x.e; });
}
```

### 7.2 角色性格向量

```js
// src/personality.js
import { MEMBERS } from './data.js';

export var PERSONALITY_VECTORS = {
  scoups: { tenderness: 7, playfulness: 5, possessiveness: 6, directness: 8 },
  jeonghan: { tenderness: 9, playfulness: 7, possessiveness: 5, directness: 4 },
  joshua: { tenderness: 8, playfulness: 6, possessiveness: 4, directness: 5 },
  // ... 13 个成员
};

export function getMemberPersonalityConstraint(memberId) {
  var v = PERSONALITY_VECTORS[memberId];
  var m = MEMBERS.find(function(x) { return x.id === memberId; });
  if (!v || !m) return '';
  return '[SYSTEM] ' + m.name + ' 性格向量：\n' +
    '温柔度=' + v.tenderness + '/10，玩心=' + v.playfulness + '/10，\n' +
    '占有欲=' + v.possessiveness + '/10，直接度=' + v.directness + '/10\n' +
    '请在剧情中体现这个性格倾向，不要 OOC。';
}
```

---

## 八、关键文件改造清单

### 8.1 新增文件

| 文件 | 阶段 | 用途 |
|---|---|---|
| `src/store.js` | Phase 1 | dispatch/reducer/subscribe |
| `src/ai-generator.js` | Phase 2 | 带重试的 AI 调用包装 |
| `src/prompts/` 目录 | Phase 3 | prompt 模块化 |
| `src/skeleton/` 目录 | Phase 4 | 游戏骨架 |
| `src/components/` 目录 | Phase 5 | UI 组件 |
| `src/event-log.js` | Phase 7 | 长期记忆 |
| `src/personality.js` | Phase 7 | 性格向量 |

### 8.2 修改文件

| 文件 | 阶段 | 改动内容 |
|---|---|---|
| `src/game-engine.js` | Phase 0 | 修复 pushCorrections（✅已完成） |
| `src/api.js` | Phase 0 | 修复 temperature（✅已完成） |
| `src/api.js` | Phase 2 | 加 sceneType 参数 + pickModel |
| `src/schema.js` | Phase 2 | 加 SCENE_SCHEMA_V2 + sanitizeSceneV2 |
| `src/validator.js` | Phase 2 | severity 分级 |
| `src/data.js` | Phase 2 | API_PROVIDERS 加 sceneModels |
| `src/prompts.js` | Phase 3 | 退化为 `prompts/legacy.js` → 逐步拆分 |
| `src/game-engine.js` | Phase 4 | 退化为薄编排层，UI 函数搬走 |
| `src/ui-renderer.js` | Phase 5 | 退化为组件协调器 |
| `src/init.js` | Phase 5 | 删除 `window.__renderAll`，改用 subscribe |
| `src/state.js` | Phase 1 | 保留，但新增字段初始值由 store 接管 |
| `src/modals.js` | Phase 5 | 拆分到 `components/` 下 |

### 8.3 删除/退役文件

| 文件 | 阶段 | 原因 |
|---|---|---|
| `src/app.js.backup` | 全程完成后 | 旧版单文件备份，不再需要 |
| `window.__renderAll` | Phase 5 | 循环依赖已解决 |

---

## 九、风险与回退策略

### 9.1 主要风险

| 风险 | 影响 | 缓解 |
|---|---|---|
| Phase 4 选项接管后剧情变僵 | 玩家体验下降 | 先在 Day 3 试点，保留 AI 生成选项的 fallback 开关 |
| 状态机迁移引入 bug | 游戏崩溃 | dispatch 内部仍 mutate GS，现有代码完全兼容 |
| Prompt 拆分后拼接错误 | AI 输出质量下降 | 每搬一个模块跑构建 + 手动测试 Day 1-3 |
| UI 组件化后样式错乱 | 界面崩坏 | 保留 style.css 不动，只拆 JS |
| 存档不兼容 | 老存档丢失 | migrateSave 继续兜底，不删字段 |

### 9.2 回退开关

在 `state.js` 加一个全局开关：

```js
// state.js defaultGameState 加
skeletonEnabled: false,  // Phase 4 的开关
aiOptionsEnabled: true,  // true=AI 生成选项，false=骨架生成选项
```

Phase 4 迁移时，先设 `aiOptionsEnabled: true`（AI 生成选项），逐 Day 切换到 `false`（骨架生成）。出问题随时切回。

### 9.3 分 Day 试点

Phase 4 的选项接管，建议按 Day 试点：

1. Day 3（普通日）→ 试点骨架生成选项
2. Day 4（约会日）→ 测试特殊场景选项
3. Day 7（提问箱）→ 测试固定按钮场景
4. Day 11（真心话）→ 测试无选项场景
5. 全量切换

---

## 十、验收标准

### Phase 0 验收

- [x] `pushCorrections` 不再栈溢出
- [x] `temperature` 参数生效（不同场景温度不同）

### Phase 1 验收

- [ ] 新建 `store.js`，包含 dispatch/reducer/subscribe
- [ ] 至少 6 个高频 action 迁移到 dispatch
- [ ] 现有游戏流程不受影响
- [ ] `npm run build` 通过
- [ ] 手动测试 Day 1-3 完整流程

### Phase 2 验收

- [ ] `schema.js` 有 `SCENE_SCHEMA_V2`
- [ ] `validator.js` 返回 severity 分级
- [ ] `ai-generator.js` 能自动重试 error 级问题
- [ ] `api.js` 支持 sceneType 参数和模型路由
- [ ] `npm run build` 通过

### Phase 3 验收

- [ ] `prompts/` 目录结构建立
- [ ] `buildSystemPrompt` 和 `buildUserMessage` 仍正常工作
- [ ] 每个子模块可独立测试
- [ ] `npm run build` 通过
- [ ] 手动测试 Day 1-3 完整流程

### Phase 4 验收（核心）

- [ ] `skeleton/` 目录结构建立
- [ ] 好感度由 `affection-engine.js` 结算
- [ ] 喝酒计数由 `drink-engine.js` 记录
- [ ] 选项由 `option-engine.js` 生成（至少 Day 3 试点）
- [ ] AI 输出不再包含 `options/drinks/affName` 字段
- [ ] `game-engine.js` 退化为薄编排层
- [ ] `npm run build` 通过
- [ ] 手动测试 Day 1-12 完整流程

### Phase 5 验收

- [ ] `components/` 目录结构建立
- [ ] `window.__renderAll` 删除
- [ ] 各组件通过 subscribe 驱动更新
- [ ] 事件委托替代逐个 addEventListener
- [ ] `npm run build` 通过
- [ ] 手动测试 Day 1-3 完整流程
- [ ] 移动端触摸无穿透

### 最终验收

- [ ] AI 只输出 `blocks + observers + smsDrafts`
- [ ] 剧情走向、好感度、喝酒、事件触发全部由骨架控制
- [ ] 代码可读性显著提升（无 600+ 行巨型文件）
- [ ] 存档兼容（老存档能继续玩）
- [ ] `npm run build` 通过
- [ ] 完整 12 天游戏流程跑通

---

## 附：时间线估算

| 阶段 | 工期 | 累计 |
|---|---|---|
| Phase 0 | 已完成 | 0 |
| Phase 1 | 1-2 天 | 2 天 |
| Phase 2 | 2-3 天 | 5 天 |
| Phase 3 | 3-4 天 | 9 天 |
| **Phase 4** | **5-7 天** | **16 天** |
| Phase 5 | 5-7 天 | 23 天 |
| Phase 6 | 1-2 周（可选） | 37 天 |
| Phase 7 | 1 周（可选） | 44 天 |

**核心目标（Phase 0-4）：约 16 个工作日**，可以达到"骨架管骨架、AI 管文案"的目标架构。

---

> **本文档生成于 2026-06-22，基于当前代码库状态。**
> **如代码有后续改动，需同步更新本文档的引用位置。**