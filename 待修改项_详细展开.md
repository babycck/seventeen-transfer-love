# SEVENTEEN 项目 — 待修改项详细展开

> 本文档是对 `TODO.md` 中所有未修改项的详细展开说明，供 Agent 实施时参考。
> 每项包含：目标文件、实现思路、关键代码位置、注意事项。

---

## 一、Prompt 与剧情生成规则

---

### 1. Day 1 傍晚读信字数缩减

- **文件：** `src/prompts.js` — Day 1 傍晚的 system prompt 或 `buildUserMessage` 中读信指令
- **内容：** 将字数要求从 `300-500字` 改为 `~200字`
- **做法：** 搜索 `300-500` 或 `读信` 相关字符串，找到 Day 1 evening 的 prompt 片段，修改字数限制
- **注意：** 只在 Day 1 evening 的读信剧情生效，不影响其他天的读信

---

### 2. Day 1 傍晚读信 prompt 精简

- **文件：** `src/prompts.js`
- **内容：** 当前读信流程有 7 个步骤（起床→早餐→...→读信），压缩为 2-3 句概括
- **做法：**
  - 找到 Day 1 evening 的剧情生成指令
  - 将枚举式的 7 步描述替换为简洁的自然语言描述
  - 末尾保留格式提醒（`只输出正文，不要选项、不要采访间……`）
- **效果示例：**
  - 原来：`1. 起床 2. 早餐 3. 读信……`
  - 改为：`Day 1 傍晚，女主收到前任的来信，读信后内心波动。描写读信过程及心情变化。`

---

### 3. 短信剧情加入采访间 + 字数缩减

- **文件：** `src/prompts.js` — `buildUserMessage('sms', ...)` 或短信相关 prompt
- **内容：**
  1. 短信剧情格式对齐自由行动标准：`正文 + 🎙女主采访间 + 🎤成员采访间 + 🎬导演OS + 💭观察员OS`
  2. 字数从 `~1000字` 缩减到 `~500字`
- **做法：**
  - 找到 SMS 生成 prompt，追加采访间格式要求
  - 修改字数描述：`生成一段约500字的短信互动剧情`
  - 示例格式注入：
    ```
    输出格式：
    [正文]
    
    🎙女主采访间
    （女主心理活动）
    
    🎤成员采访间
    （收到短信的成员评价）
    
    🎬导演OS
    （导演评论）
    
    💭观察员OS
    （观察员评论）
    ```

---

### 4. 短信内容保密规则

- **文件：** `src/prompts.js`
- **内容：** 心动短信的内容只有收发双方知道，其他成员只能观察表情反应，不能猜测/评论/引用具体内容
- **做法：**
  - 在 system prompt 的规则区追加：
    ```
    [INSTRUCTION] 心动短信保密规则：短信内容是私密的，只有发送方和接收方知道具体内容。
    其他成员只能看到"有人收到了短信"并观察该成员的表情反应，
    绝对不能猜测、评论、引用或暗示短信的具体内容。
    ```
  - 在 `buildUserMessage('consequence', ...)` 中（短信发送后的后续剧情）也要追加类似约束

---

### 5. 私密体质专属限定

- **文件：** `src/prompts.js`
- **内容：** 私密体质（如有）是女主的专属设定，禁止 AI 映射到成员身上
- **做法：**
  - 在 system prompt **三处**追加声明：
    1. 全局规则区
    2. `buildHeroineProfilePrompt()` 中
    3. `buildUserMessage` 中涉及体质描述的地方
  - 声明示例：
    ```
    [INSTRUCTION] 体质是女主的专属设定，绝对禁止映射、赋予或暗示到任何成员身上。
    成员没有体质概念，不会讨论体质，也不会提到"私密"、"魅惑"等体质相关词汇。
    ```
  - 在 `buildUserMessage` 中，当 `GS.heroineProfile.constitution` 有值时，明确标记 `【女主专属体质：xxx（仅供描写女主时参考，禁止投射到成员身上）】`

---

### 6. 进入下一天滚动到顶部

- **文件：** `src/game-engine.js` — `goToNextDay()`
- **内容：** 生成记忆压缩面板后，窗口滚动到顶部
- **做法：**
  - 找到 `goToNextDay()` 函数末尾（压缩面板生成后、`saveGame()` 或 `__renderAll()` 之前/之后）
  - 添加：`window.scrollTo(0, 0);`
  - 如果页面还没来得及渲染新内容，可以用 `setTimeout(window.scrollTo.bind(window, 0, 0), 100)`
- **涉及代码位置：** `game-engine.js` 中 `proceedToNextDay` 或 `goToNextDay` 函数

---

### 7. 压缩记忆删除对话归属要求

- **文件：** `src/memory.js` — `compressTodayToSummary()`
- **内容：** system prompt 第 2 条"保留所有关键对话（含完整'谁对谁说'归属）"删除
- **做法：**
  - 找到 `compressTodayToSummary()` 的 system prompt 字符串
  - 删除其中关于"对话归属"的要求
  - 保留关键事件、待兑现约定、已揭示信息三条
- **当前 prompt 参考（`memory.js:156-161`）：**
  ```
  '必须保留以下内容（缺一不可）：\n' +
  '1. 关键事件：今天发生了什么，按时间顺序\n' +
  '2. 待兑现约定：今天角色之间约定了什么——每条单独列出\n' +
  '3. 今日已揭示信息：女主和每位成员今天说过的偏好、禁忌、习惯、过敏原等——每条单独列出\n\n'
  ```

---

### 14. 深夜禁止 AI 自动发短信

- **文件：** `src/prompts.js`
- **内容：** 深夜时段的正文中，AI 绝对不能描写女主发送短信的动作
- **做法：**
  - 在 system prompt 规则区追加：
    ```
    [INSTRUCTION] 深夜时段（phaseIndex===3）正文中绝对禁止描写女主发送短信的动作。
    短信发送通过独立的短信系统完成，不在正文剧情中出现。
    但可以描写女主"想发短信"的念头或犹豫，不能写实际发送。
    ```

---

### 15. 普通选项剧情字数缩减

- **文件：** `src/prompts.js`
- **内容：** 普通选项（A/B/C）后续剧情字数从 `~1500字` 缩减到 `~1000字`
- **做法：**
  - 找到 `buildUserMessage('consequence', ...)` 中控制字数的 prompt
  - 将 `~1500字` 改为 `~1000字` 或 `约1000字`
  - 注意不要误改 phase narrative（每天开头的主线剧情）的字数

---

### 23. 约会内容强制保密规则

- **文件：** `src/prompts.js`
- **内容：** 约会具体细节只有女主和约会对象知道，其他成员只能看到"约会回来了"
- **做法：**
  - 在 system prompt 规则区追加：
    ```
    [INSTRUCTION] 约会内容保密规则：约会中的对话、互动、地点等具体细节只有女主和约会对象两人知道。
    其他成员在约会回来后，不能猜测、评论、引用或暗示约会内容。
    他们只能看到"约会结束了，人回来了"这个事实，最多描写表情/状态（如"看起来心情不错"）。
    ```
  - 在 `buildUserMessage` 中约会后的时段也需要追加类似约束

---

## 二、女主设定与系统配置

---

### 8. API 提供商重构

- **文件：** `src/ui-renderer.js`, `src/modals.js`, `src/api.js`, `src/state.js`, `src/data.js`, `index.html`
- **内容：** 双层下拉：提供商（DeepSeek/硅基流动/千问）+ 模型选择；删除 Kimi/Moonshot
- **做法：**
  1. **`data.js`** — 定义提供商和模型映射表：
     ```js
     var API_PROVIDERS = {
       deepseek: { name: 'DeepSeek', models: ['deepseek-chat', 'deepseek-reasoner'] },
       siliconflow: { name: '硅基流动', models: ['Qwen/Qwen2.5-7B-Instruct', 'deepseek-ai/DeepSeek-V3'] },
       qwen: { name: '千问', models: ['qwen-plus', 'qwen-turbo'] }
     };
     ```
  2. **`state.js`** — 新增 `apiProvider` 和 `apiModel` 字段 + `migrateSave` 兜底
  3. **`api.js`** — `callDeepSeek` 根据 `GS.apiProvider` 选择 baseURL 和 model
  4. **`ui-renderer.js`** — Step 1 增加"AI 提供商"下拉框 + "AI 模型"下拉框（联动，切换提供商时刷新模型列表）
  5. **`modals.js`** — API 设置弹窗同样增加双下拉框
  6. 删除所有 Kimi/Moonshot 相关的选项和配置
- **注意：** 已有代码中 `data.js` 已添加结构，`ui-renderer.js` Step 1 和 `modals.js` 弹窗已增加下拉框，但 `api.js` 的 baseURL 路由可能不完整，需要验证

---

### 9. 女主设定：生日取代星座

- **文件：** `src/ui-renderer.js`, `src/modals.js`, `src/state.js`, `src/prompts.js`
- **内容：** 移除星座芯片选择，增加「生日」输入框（月/日），星座自动计算并显示
- **做法：**
  1. **`state.js`** — `heroineProfile` 增加 `birthMonth`, `birthDay` 字段；`zodiac` 改为计算属性（不存，或存储并从生日推算）
  2. **`ui-renderer.js`** — Step 1 设定界面：删除星座芯片组，替换为两个下拉框或输入框（月份 1-12，日期 1-31）
  3. 根据月/日自动计算星座（写一个 `getZodiac(month, day)` 函数），在 UI 上显示计算结果
  4. **`prompts.js`** — 将注入的 `星座：xxx` 改为 `生日：X月X日（星座：xxx）`
- **星座映射表：**
  ```
  白羊座 3/21-4/19  金牛座 4/20-5/20  双子座 5/21-6/21
  巨蟹座 6/22-7/22  狮子座 7/23-8/22  处女座 8/23-9/22
  天秤座 9/23-10/23 天蝎座 10/24-11/22 射手座 11/23-12/21
  摩羯座 12/22-1/19 水瓶座 1/20-2/18  双鱼座 2/19-3/20
  ```

---

## 三、季节、天气、日期与节日系统

---

### 10. 季节与日期系统

- **文件：** `src/game-engine.js`, `src/state.js`, `src/data.js`, `src/ui-renderer.js`, `src/prompts.js`
- **内容：** 游戏开始时随机生成季节+月份（2月-12月）+ 连续12天日期；UI 显示「X月X日」
- **做法：**
  1. **`state.js`** — 新增 `season`, `gameMonth`, `gameDay` 字段
  2. **`game-engine.js`** — 在 `initGame()` 或游戏开始时：
     - 随机选择季节（春/夏/秋/冬）
     - 根据季节随机选择月份（春：2-4，夏：5-7，秋：8-10，冬：11-12）
     - 根据月份确定该月天数
     - 随机选择一个起始日，使得连续12天不跨月（或允许跨月但简化处理）
     - 赋值到 `GS.gameMonth` 和 `GS.gameDay`
  3. `goToNextDay()` 中每过一天 `GS.gameDay++`
  4. **`ui-renderer.js`** — 日期显示区域将 `GS.gameMonth` 和 `GS.gameDay` 格式化为 `X月X日`（隐藏年份）
  5. **`prompts.js`** — 在 system prompt 中注入 `【当前日期：{month}月{day}日，{season}】`
- **注意：** 季节影响天气（见 #11），也影响约会地点池（见 #44）

---

### 11. 每日天气系统

- **文件：** `src/game-engine.js`, `src/state.js`, `src/prompts.js`
- **内容：** 每天基于季节从天气池随机抽取；80% 概率关联前一天，20% 概率突变
- **做法：**
  1. **`state.js`** — 新增 `weather` 字段（字符串，如 `'晴'`、`'小雨'`）
  2. **`data.js`** — 定义季节-天气池映射：
     ```js
     var SEASON_WEATHER = {
       '春': ['晴', '多云', '小雨', '阵雨', '阴'],
       '夏': ['晴', '多云', '雷阵雨', '大雨', '晴'],
       '秋': ['晴', '多云', '小雨', '阴', '大风'],
       '冬': ['晴', '多云', '小雪', '阴', '大风']
     };
     ```
  3. **`game-engine.js`** — 在 `proceedToNextDay()` 中：
     - 80% 概率：从关联前一天天气的天气池抽取（如前一天是"小雨"，则从"小雨→多云/阴/晴"的过渡池抽取）
     - 20% 概率：从该季节天气池完全随机抽取
     - 第一天：从季节天气池完全随机
  4. **`prompts.js`** — 在 system prompt 注入 `【天气：{weather}】`
- **注意：** 天气影响约会地点（见 #44），影响送礼物剧情等

---

### 12. 节日系统

- **文件：** `src/game-engine.js`, `src/prompts.js`, `src/data.js`
- **内容：** 节日池：2/14 情人节、8/29 七夕、12/25 圣诞节 + 成员生日；若 12 天内覆盖则触发，无保底不偏移
- **做法：**
  1. **`data.js`** — 定义节日列表和成员生日列表：
     ```js
     var FESTIVALS = [
       { month: 2, day: 14, name: '情人节' },
       { month: 8, day: 29, name: '七夕' },
       { month: 12, day: 25, name: '圣诞节' }
     ];
     var MEMBER_BIRTHDAYS = [
       { id: 'scoups', month: 8, day: 8 },
       { id: 'jeonghan', month: 10, day: 4 },
       // ...
     ];
     ```
  2. **`game-engine.js`** — `proceedToNextDay()` 中检查当天是否匹配节日或成员生日：
     - 匹配则设置 `GS.todayFestival = { name: '情人节', isBirthday: false, memberId: null }` 或类似结构
     - 匹配成员生日则 `{ name: '珉奎生日', isBirthday: true, memberId: 'mingyu' }`
  3. **`prompts.js`** — 如果 `GS.todayFestival` 有值，在 system prompt 注入 `【今日特别日：{festival.name}】`
  4. 节日/生日当天 AI 生成剧情时应包含相关元素（如生日蛋糕、圣诞装饰、情人节巧克力等）
- **注意：** 纯随机无保底 — 如果游戏周期内没覆盖任何节日，就不触发。不要偏移日期来"凑"节日。

---

## 四、日程与强制任务调整

---

### 17. Day 3 说故事重复触发

- **文件：** `src/data.js` 或 `src/formatters.js` — `getMandatoryTask()`
- **内容：** Day 3 的 `afternoon`/`evening`/`night` 时段缺少描述，fallback 到 morning 导致全天重复说故事
- **做法：**
  - 找到 `getMandatoryTask()` 中 Day 3 的配置
  - 补齐 `afternoon`、`evening`、`night` 时段的描述，避免 fallback
  - 描述可以不同（如下午"继续闲聊"/晚上"一起做饭"等），确保每个时段有独特内容
  - 参考其他天的配置格式
- **代码位置：** `formatters.js` 中的 `getMandatoryTask` 函数

---

### 19. 约会日日程调整（方案A）

- **文件：** `src/data.js` 或 `src/formatters.js`, `src/game-engine.js`
- **内容：**
  - 第二次约会：Day 5 → Day 6
  - 第二批 X 记忆公开：Day 6 → Day 5
  - 集体外出（Day 7）不变
  - 第三次约会（Day 8）不变
- **做法：**
  - 找到日程表中 Day 5 和 Day 6 的配置
  - 将 Day 5 的"第二次约会"移到 Day 6
  - 将 Day 6 的"第二批 X 记忆公开"移到 Day 5
  - 检查是否有其他逻辑依赖这些日子（如 `jealousyMission`、`midnightCall` 等），确保不冲突

---

### 20. 约会日去掉「随机约会」选项

- **文件：** `src/ui-renderer.js` 或 `src/game-engine.js`
- **内容：** Day 4/6/8/9 上午只保留「▶ 进入约会场景」按钮，去掉「随机约会」选项
- **做法：**
  - 找到约会日上午的选项生成逻辑
  - 移除"随机约会"相关的选项/按钮
  - 只保留一个明确的「▶ 进入约会场景」按钮（或保留自由输入框）
  - 自由输入框保留（玩家可以输入想和谁约会等）

---

### 24. 约会骰子结果延迟写入

- **文件：** `src/game-engine.js` — `showDatingDiceModal()`
- **内容：** 投骰子后立即把 `result.partner` 写入 `GS.pendingDatingResult`，不等 `generatePhaseNarrative()` 结束
- **做法：**
  - 找到 `showDatingDiceModal()` 中骰子结果回调函数
  - 在骰子动画结束后、弹窗关闭前（或关闭时），立即执行：
    ```js
    GS.pendingDatingResult = { partner: result.partner, dateType: result.type };
    ```
  - 确保后续的 `generatePhaseNarrative()` 能读到这个值
  - 移除 `generatePhaseNarrative()` 中重复设置 `pendingDatingResult` 的逻辑（如果有）

---

### 25. `resetPhaseState()` 清空约会对象状态

- **文件：** `src/game-engine.js` — `resetPhaseState()`
- **内容：** `resetPhaseState()` 不应清空 `currentDatingPartner` 和 `pendingDatingResult`（全天级状态）
- **做法：**
  - 找到 `resetPhaseState()` 函数
  - 将 `currentDatingPartner` 和 `pendingDatingResult` 从清空列表中移除
  - 或者加判断：如果 `GS.pendingDatingResult` 有值，则保留
  - 原因是约会状态是全天有效的，跨时段（上午约会→下午后续剧情），不能被 `resetPhaseState` 清掉

---

## 五、好感度与互动系统

---

### 13. 好感度标记严格丢弃

- **文件：** `src/parser.js` 或 `src/validator.js`
- **内容：** 无原因（无上下文支持）的 `[好感度: 成员名 +N]` 标记直接忽略，不记录也不加好感度
- **做法：**
  - 在解析好感度标记时增加校验：
    1. 标记出现的剧情段落是否与该成员有关联
    2. 标记前是否有合理的触发事件（如"帮忙做了某事"、"说了暖心的话"等）
    3. 如果标记凭空出现（剧情中该成员完全没有出现），则忽略
  - 在 `validator.js` 的 `validateNarrative` 中增加针对好感度的 `corrections`
  - 或者在 `parseNarrative()` 中增加一个 `validateAffectionMarkers()` 步骤
- **简化实现：** 如果难以准确判断，可以改为：好感度标记只有在同一段剧情中明确提到该成员行为时才生效，否则标记被过滤掉

---

### 21. 好感度延迟结算机制

- **文件：** `src/parser.js`, `src/state.js`, `src/game-engine.js`
- **内容：** `parseNarrative()` 提取 `[好感度: ...]` 但不立即写入 state，暂存 `pendingAffChanges`；每次进入新剧情前结算上一段
- **做法：**
  1. **`state.js`** — 新增 `pendingAffChanges: []` 字段
  2. **`parser.js`** — `parseNarrative()` 中解析到好感度标记时，不调用 `updateAffection`，而是推入 `GS.pendingAffChanges`：
     ```js
     // 原：updateAffection(memberId, delta);
     // 改为：
     GS.pendingAffChanges.push({ memberId: memberId, delta: delta, reason: reason });
     ```
  3. **`game-engine.js`** — 在每次生成新剧情前（`generatePhaseNarrative`、选项后续、自由行动等），先调用一个 `flushPendingAffection()` 函数：
     ```js
     function flushPendingAffection() {
       if (GS.pendingAffChanges && GS.pendingAffChanges.length > 0) {
         GS.pendingAffChanges.forEach(function(change) {
           updateAffection(change.memberId, change.delta);
           addAffectionLog(change.memberId, change.delta, change.reason);
         });
         GS.pendingAffChanges = [];
       }
     }
     ```
  4. 重新生成时，旧 `pendingAffChanges` 被自然覆盖（不清空也行，因为 `generatePhaseNarrative` 开头会清）
- **注意：** 重新生成时前面 pending 的好感度不应生效，这是该机制的核心目的

---

### 32. 好感度难度提升（无上限版）

- **文件：** `src/affection.js`, `src/state.js`, `src/game-engine.js`, `src/sms.js`, `src/modals.js`
- **内容：** 三种机制：
  1. **同一行为重复做效果递减**：短信第二次 +5→+3→+1，送礼同理
  2. **负面行为扣分增加**：未收到短信 -1→-3，吃醋 -3→-5
  3. **好感度疲劳值**：连续对同一人行动效率下降
- **做法：**
  1. **`state.js`** — 新增 `actionCounts` 字段：
     ```js
     actionCounts: {
       sms: {},     // { memberId: count }
       gift: {},    // { memberId: count }
       flirt: {},   // { memberId: count } 等
     },
     fatigue: {}    // { memberId: count } 连续行动次数
     ```
  2. **`affection.js`** — 修改 `updateAffection` 或创建包装函数：
     - 每次加分前检查该成员该行为的累计次数
     - 用公式计算实际加成：`baseBonus - (count-1) * step`，最低保底 +1
     - 示例：短信第一次 +5，第二次 +3，第三次 +1，第四次起 +1
  3. 负面行为同理：累加次数，扣分递增
  4. 疲劳值：连续对同一人每次行动后 +1；每天重置或自然衰减
- **注意：** 每日重置，不影响跨天

---

### 33. 选项明确扣好感度

- **文件：** `src/game-engine.js`, `src/ui-renderer.js`
- **内容：** 某些选项会明确降低特定成员好感度，按钮旁显示风险提示
- **做法：**
  1. **`game-engine.js`** — 在选项定义中增加 `affRisks` 字段：
     ```js
     options: [
       { text: '去帮珉奎做饭', nextPhase: false, affRisks: [{ memberId: 'wonwoo', delta: -2, reason: '选择了帮珉奎而冷落了圆佑' }] },
       { text: '和圆佑聊天', nextPhase: false, affRisks: [{ memberId: 'mingyu', delta: -2, reason: '没去帮珉奎做饭' }] },
     ]
     ```
  2. **`ui-renderer.js`** — 渲染选项按钮时，检查是否有 `affRisks`：
     ```js
     if (opt.affRisks) {
       var riskText = opt.affRisks.map(function(r) { return r.memberId + ' ' + r.delta; }).join(' ');
       button.innerHTML += '<span class="aff-risk">⚠️ ' + riskText + '</span>';
     }
     ```
  - 或者直接在文字中标注：`去帮珉奎做饭（圆佑❤️-2）`
  3. **`style.css`** — `.aff-risk` 样式：小字、红色/橙色、悬浮显示详情

---

### 34. 成员间竞争（队内修罗场）

- **文件：** `src/prompts.js`, `src/game-engine.js`, `src/state.js`
- **内容：** A 看到女主和 B 互动后，不会夸 C，而是针对 B：讲坏话、暗示不适合、阴阳怪气、对比表现、私下"提醒"女主
- **做法：**
  1. **`prompts.js`** — 在 system prompt 追加角色行为规则：
     ```
     [INSTRUCTION] 成员竞争规则（修罗场）：当成员 A 看到女主和成员 B 有亲密/暧昧互动后，
     A 的后续行为应当针对 B（而非夸奖其他成员），例如：
     - 讲 B 的坏话或暗示 B 不值得信任
     - 暗示 B 不适合女主
     - 阴阳怪气地对比自己 vs B 的表现
     - 私下找女主"提醒"她注意 B
     触发后 A 与 B 之间产生明显张力，女主可能被夹在中间。
     ```
  2. **`state.js`** — 新增 `rivalryTriggers` 字段，记录已触发的竞争关系
  3. **`game-engine.js`** — 在好感度变动或特定互动后，评估是否触发竞争
  4. 触发后后续剧情注入该规则
- **注意：** 不要所有互动都触发，建议在女主和某成员连续 2+ 次亲密互动后触发

---

## 六、X 记忆物品系统重构

---

### 18. X 记忆物品在 X 档案中显示

- **文件：** `src/modals.js` — `showXArchiveModal()`
- **内容：** 每个 X 的故事前面追加显示 `GS.xItems[id]` 记忆物品列表
- **做法：**
  - 找到 `showXArchiveModal()` 中渲染 X 档案内容的部分
  - 在故事正文前插入记忆物品区：
    ```js
    if (GS.xItems[id]) {
      html += '<div class="x-items"><h4>📦 记忆物品</h4><p>' + escHtml(GS.xItems[id]) + '</p></div>';
    }
    ```
  - 只显示已公开的成员（根据 `GS.xRevealOrder` 或 `GS.day >= xRevealDay`）

---

### 26. X 记忆物品按需生成 2 个 + 50 容量缓存池

- **文件：** `src/x-archive.js`, `src/data.js`, `src/state.js`
- **内容：**
  - 首次某组合（女主+3成员+X）时，AI 生成 2 个完整记忆物品（名称+样式+故事约50字）
  - 缓存池上限 50 条
  - 已满 50 时从已有组合中随机调用
  - 相同组合复用
  - 第一次公开只显示名称+样式，第二次公开追加故事
- **做法：**
  1. **`state.js`** — 新增 `xItemCache` 字段（数组，上限 50）
  2. **`data.js`** — 定义缓存池常量
  3. **`x-archive.js`** — 修改记忆物品生成逻辑：
     - 生成前检查缓存是否已满 50
     - 未满：AI 生成 2 个新物品，存入缓存
     - 已满：从缓存随机取 2 个已有物品（可复用）
     - 相同组合的 key 复用缓存的物品
  4. X 记忆物品公开分两次：
     - 第一次公开：只显示名称+样式
     - 第二次公开：追加完整故事（约50字）

---

## 七、午夜电话亭重构

---

### 30. 午夜电话亭即时生成 + 正文不注入

- **文件：** `src/game-engine.js`, `src/prompts.js`
- **内容：**
  - 选择目标并输入内容后，立即调用 AI 生成约 100 字接听反应，在弹窗内展示
  - `prompts.js` 不再把电话亭信息注入正文剧情
- **做法：**
  1. **`game-engine.js`** — `showMidnightCallModal()` 中在用户输入内容并确认目标后：
     - 调用 `callDeepSeek` 生成约 100 字反应
     - 在弹窗内显示生成的剧情
     - 确认后关闭弹窗，记录最终状态到 `GS.midnightCall`
  2. **`prompts.js`** — 在 `buildUserMessage` 中移除 midnightCall 相关的注入逻辑
  3. 移除之前注入到正文中的 `【午夜电话亭】` 等标记
- **当前状态：** 即时生成已完成，但需要确认正文注入是否已移除

---

### 31. 午夜电话亭记录按钮

- **文件：** `src/ui-renderer.js`
- **内容：** Day 7 起 Header 增加 📞 按钮，点击弹窗显示当晚匿名电话详情
- **做法：**
  - `ui-renderer.js` 中 Header 渲染逻辑：当 `GS.day >= 7 && GS.midnightCall && GS.midnightCall.status === 'done'` 时
  - 显示一个 📞 按钮
  - 点击弹窗显示：
    - 目标成员
    - 电话内容
    - 接听反应
  - 数据来源：`GS.midnightCall`

---

## 八、UI 与交互优化

---

### 16. 选项与自由输入次数拆分

- **文件：** `src/game-engine.js`, `src/ui-renderer.js`
- **内容：** 每时段：选项（A/B/C）最多选 1 次，自由输入最多输入 1 次，合计上限 2 次
- **做法：**
  - **`state.js`** — 新增 `phaseOptionUsed` 和 `phaseFreeUsed` 字段
  - **`game-engine.js`** — 在选项选择和自由输入提交时分别计数：
    - 选 A/B/C → `phaseOptionUsed++`
    - 自由输入提交 → `phaseFreeUsed++`
  - **`ui-renderer.js`** — 渲染时检测：
    - `phaseOptionUsed >= 1` → 隐藏所有 A/B/C 按钮
    - `phaseFreeUsed >= 1` → 隐藏自由输入框
    - `phaseOptionUsed + phaseFreeUsed >= 2` → 显示"跳转到下一时段"提示
  - `resetPhaseState()` 中重置这两个计数

---

### 22. 保存意图后进入下一段自动清空输入框

- **文件：** `src/game-engine.js`, `src/ui-renderer.js`
- **内容：** 点击 💾 保存意图后，当用户进入下一段剧情时，渲染后自动清空 `GS.freeInput` 及输入框内容
- **做法：**
  - 找到保存意图的处理函数（`handleSaveIntent` 或类似）
  - 保存成功后设置一个标志：`GS._pendingFreeInputClear = true`
  - 在每次渲染剧情后（`__renderAll` 或 narrative 渲染逻辑中）检查该标志
  - 如果为 true：`GS.freeInput = ''` + 清空输入框 DOM 内容 + 标志置为 false

---

### 28. 礼物改造后显示「已改造」标签

- **文件：** `src/ui-renderer.js`, `src/modals.js`
- **内容：** 礼物 `isRemade=true` 时显示「✅ 已改造」标签，改造按钮仍隐藏
- **做法：**
  - 礼物列表中每个礼物渲染时检查 `gift.isRemade`
  - 如果是：显示 `✅ 已改造` 标签（灰色小字），不显示改造按钮
  - 改造时保留原名称不变，只改 `type` 和 `description`
  - 改造按钮（🔧）只在 `isRemade=false` 或 `isRemade` 未定义时显示

---

### 29. 送礼剧情缺少女主人设

- **文件：** `src/modals.js` — `sendGift()`
- **内容：** 送礼剧情 system prompt 注入女主名字和基本信息
- **做法：**
  - 找到 `sendGift()` 中调用 `callDeepSeek` 之前的 prompt 构建
  - 追加：
    ```js
    var hp = GS.heroineProfile;
    sysPrompt += ' 女主姓名：' + hp.name + '，年龄：' + hp.age + '岁，职业：' + hp.job + '，外貌：' + hp.appearance.join('、') + '。';
    ```
  - 这样 AI 生成的送礼剧情就不会编造女主名字
- **当前状态：** system prompt 中已有部分描述，但缺少 `hp.name`，需要补充

---

## 十、新增修改项（#35-54）

---

### 35. 匿名提问箱选项未渲染

- **文件：** `src/ui-renderer.js`
- **内容：** 缺少 `questionBox` 的选项区渲染，需增加「如实回答/拒绝回答并喝酒」两个按钮
- **做法：**
  - 在 option-area 渲染逻辑中增加对 `GS.questionBox && GS.questionBox.active` 的检测
  - 如果提问箱活跃，渲染两个固定按钮：
    ```html
    <button class="option-button" data-qb-action="answer">如实回答</button>
    <button class="option-button" data-qb-action="drink">拒绝回答并喝酒</button>
    ```
  - 点击后调用 `handleQuestionBoxChoice('answer')` 或 `handleQuestionBoxChoice('drink')`

---

### 36. 匿名提问箱选项改为固定按钮

- **文件：** `src/ui-renderer.js`, `src/game-engine.js`
- **内容：** 提问箱活跃时固定渲染两个按钮，不依赖 AI 返回的选项；`handleQuestionBoxChoice` 根据按钮索引判断，不再用 `indexOf('喝酒')`
- **做法：**
  - 渲染时直接硬编码两个按钮（不受 `GS.currentOptions` 影响）
  - `handleQuestionBoxChoice` 改为接收明确的 action 参数（`'answer'` / `'drink'`）
  - 移除原先通过 `opt.text.indexOf('喝酒')` 判断的代码

---

### 37. 匿名提问箱点取消后卡死

- **文件：** `src/game-engine.js` — `showQuestionBoxAnswerModal()`
- **内容：** 点取消返回空字符串时直接 `return`，导致提问箱 `active` 仍为 true 但按钮全 disabled
- **做法：**
  - 找到 `showQuestionBoxAnswerModal()` 中取消/关闭的处理
  - 改为关闭弹窗并恢复选项状态（重新启用按钮）
  - 或者允许重新选择（reject promise → 回到选择状态）
  - 核心逻辑：取消时不改变 `GS.questionBox.active` 状态，且重新启用选项按钮

---

### 38. 匿名提问箱缺少执行过程描写

- **文件：** `src/prompts.js`
- **内容：** prompt 追加要求必须描写女主回答/喝酒的动作过程
- **做法：**
  - 在提问箱相关的 prompt 中追加：
    ```
    [INSTRUCTION] 必须描写女主回答或喝酒的完整动作过程：
    - 如实回答：站起来、面对众人、念出回答、表情变化
    - 拒绝喝酒：站起来、拿起酒杯、一饮而尽、表情反应
    不能只写"她回答了"或"她喝了"就跳转到现场反应。
    ```

---

### 39. Day 7 提问箱喝酒分支特殊剧情

- **文件：** `src/game-engine.js`, `src/prompts.js`
- **内容：** 女主拒绝回答并喝酒（+2杯）后，触发"醉倒被送回房间"特殊剧情
- **做法：**
  - 在 `handleQuestionBoxChoice('drink')` 中，当女主累计杯数达到阈值时（如 drinkCount >= 2）
  - 触发特殊剧情描写（醉倒、被成员送回房间）
  - 流程照常，仍可进入深夜发短信
  - 在 prompt 中注入：`如果女主连续喝酒达到2杯以上，触发醉倒剧情描写`

---

### 40. 完整剧情文本导出

- **文件：** `src/ui-renderer.js`, `src/game-engine.js`
- **内容：** 增加「📥 导出记录」按钮，将所有 `dailyFullTexts` 合并导出为 `.txt` 下载
- **做法：**
  1. **`ui-renderer.js`** — 在界面合适位置（Header 或控制区）增加按钮：
     ```html
     <button id="exportBtn">📥 导出记录</button>
     ```
  2. 绑定点击事件，调用导出函数
  3. **导出函数逻辑：**
     - 遍历 `GS.dailyFullTexts`（或 `GS.todayFullText`+历史存档）
     - 合并所有剧情正文文本
     - 格式：`Day 1\n-----\n[正文]\n\nDay 2\n-----\n[正文]\n...`
     - 不包含好感度、SMS 历史等数据
     - 使用 Blob + URL.createObjectURL 创建下载
     ```js
     var text = '';
     for (var d = 1; d <= GS.day; d++) {
       if (GS.dailyFullTexts[d]) {
         text += 'Day ' + d + '\n' + '-----\n' + GS.dailyFullTexts[d].join('\n\n') + '\n\n';
       }
     }
     var blob = new Blob([text], { type: 'text/plain;charset=utf-8' });
     var a = document.createElement('a');
     a.href = URL.createObjectURL(blob);
     a.download = 'SEVENTEEN_剧情记录.txt';
     a.click();
     URL.revokeObjectURL(a.href);
     ```

---

### 41. Day 8 约会修罗场竞争机制

- **文件：** `src/game-engine.js`, `src/ui-renderer.js`
- **内容：** Day 8 约会 30% 概率触发：若骰子选中的成员好感度最低，其他两位成员有几率抗议/争取
- **做法：**
  1. 在 Day 8 上午骰子结束后检测触发条件：
     - 30% 概率
     - 被选中的成员好感度低于其他两位
  2. 如果触发，弹出新选项弹窗：
     ```
     「维持原结果：和XX约会」
     「换成B：和YY约会」
     「换成C：和ZZ约会」
     ```
  3. 无论选哪个，最终都进入约会场景（只是剧情走向不同）
  4. 记录选择到 `GS.day8RivalryChoice`
  5. prompt 根据选择注入对应的剧情走向

---

### 42. 深夜去掉选项按钮

- **文件：** `src/ui-renderer.js`
- **内容：** 深夜时段（phaseIndex === 3）去掉选项区 A/B/C 按钮
- **做法：**
  - 在选项区渲染逻辑中增加条件：
    ```js
    if (GS.phaseIndex === 3) {
      // 不渲染 A/B/C 选项按钮
    } else {
      // 正常渲染
    }
    ```
  - 保留「重新生成本段」「跳过进入下一时段」「发送心动短信」三个按钮
  - 保留自由输入框
  - 深夜时段的默认显示文案：`「深夜了，大家各自回房休息。你想做点什么？」`

---

### 43. Day 9 X 约会日改为双向读信

- **文件：** `src/prompts.js`, `src/game-engine.js`
- **内容：** 原单向（女主读给 X）改为双向：X 也读写给女主的信
- **做法：**
  1. **`prompts.js`** — 修改 Day 9 的剧情 prompt：
     - 描述改为双向读信场景
     - X 的信由 AI 根据 X 档案自动生成
     - 两封信各约 300 字
     - 读信顺序由 AI 自由安排
  2. 在 `buildUserMessage` 中，当 Day 9 且是读信时段时，注入：
     ```
     【双向读信：女主读给X + X读给女主，各约300字，顺序由你安排】
     ```
  3. 确保 X 的信内容与 X 档案一致（分手原因、回忆等）

---

### 44. 约会地点池与季节/天气联动

- **文件：** `src/data.js`, `src/prompts.js`, `src/game-engine.js`
- **内容：** 增加约会地点池，每次约会随机抽取注入 prompt；与季节、天气联动
- **做法：**
  1. **`data.js`** — 定义地点池（含季节/天气标签）：
     ```js
     var DATE_LOCATIONS = [
       { name: '樱花林', tags: ['自然'], season: ['春'], weather: ['晴', '多云'] },
       { name: '海边栈道', tags: ['自然'], season: ['春', '夏', '秋'], weather: ['晴', '多云'] },
       { name: '山顶观景台', tags: ['自然'], season: ['春', '秋'], weather: ['晴'] },
       { name: '湖边野餐', tags: ['自然'], season: ['春', '夏', '秋'], weather: ['晴', '多云'] },
       { name: '深夜便利店', tags: ['城市'], season: ['春', '夏', '秋', '冬'], weather: ['晴', '多云', '小雨', '小雪'] },
       { name: '旧书店', tags: ['城市'], season: ['春', '夏', '秋', '冬'], weather: ['晴', '多云', '小雨', '阴', '小雪'] },
       { name: '复古游戏厅', tags: ['城市'], season: ['春', '夏', '秋', '冬'], weather: ['晴', '多云', '小雨'] },
       { name: '天台篮球场', tags: ['城市'], season: ['春', '夏', '秋'], weather: ['晴', '多云'] },
       { name: '私人影院', tags: ['室内'], season: ['春', '夏', '秋', '冬'], weather: ['all'] },
       { name: '陶艺工坊', tags: ['室内'], season: ['春', '夏', '秋', '冬'], weather: ['all'] },
       { name: '猫咪咖啡馆', tags: ['室内'], season: ['春', '夏', '秋', '冬'], weather: ['all'] },
       { name: '录音棚', tags: ['室内'], season: ['春', '夏', '秋', '冬'], weather: ['all'] },
       { name: '废弃车站', tags: ['特殊'], season: ['秋', '冬'], weather: ['阴', '小雨', '小雪'] },
       { name: '凌晨的急诊室', tags: ['特殊'], season: ['春', '夏', '秋', '冬'], weather: ['all'] },
       { name: '24小时投币洗衣房', tags: ['特殊'], season: ['春', '夏', '秋', '冬'], weather: ['all'] },
     ];
     ```
  2. **`game-engine.js`** — 在约会日抽取地点：
     - 根据 `GS.season` 和 `GS.weather` 过滤可用地点
     - 从过滤后的池中随机抽一个
     - 存入 `GS.currentDateLocation`
  3. **`prompts.js`** — 在约会相关 prompt 注入：
     ```
     【约会地点：{GS.currentDateLocation.name} —— {氛围描述}】
     ```
  4. 每个地点附带氛围描述（随机变体或固定模板）

---

### 45. 短信草稿动态生成

- **文件：** `src/game-engine.js`, `src/prompts.js`
- **内容：** 短信草稿不再使用固定模板，要求 AI 基于当天剧情内容动态生成
- **做法：**
  - 在 `buildUserMessage` 中或发送短信的 prompt 中追加：
    ```
    [INSTRUCTION] 请基于今天的剧情内容，生成3条与今天事件/对话/氛围相关的短信草稿建议。
    草稿应包含具体的细节引用（如今天去了海边→"今天的海风很舒服"），不能是通用模板。
    ```
  - 或者在 `buildUserMessage('sms', ...)` 中注入今天的关键事件摘要
  - 移除原有的固定短信模板列表

---

### 46. Day 11 选项限制修正

- **文件：** `src/game-engine.js` 或 `src/ui-renderer.js`
- **内容：** `isDay11` → `isTruthRound`，只有真心话环节不限制选项，上午最终约会仍限制
- **做法：**
  - 找到判断 Day 11 选项限制的代码
  - 将：
    ```js
    if (GS.day === 11) { /* 不限制选项 */ }
    ```
    改为：
    ```js
    var isTruthRound = GS.day === 11 && GS.truthState && GS.truthState.active;
    if (isTruthRound) { /* 不限制选项 */ }
    ```
  - Day 11 上午的最终约会时段（`truthState` 未激活）仍然保持正常选项限制

---

### 47. Day 11 真心话游戏化重构

- **文件：** `src/game-engine.js`, `src/ui-renderer.js`, `src/data.js`, `src/prompts.js`
- **内容：** 真心话重构为抽卡+自动判定+女主回合输入框的完整游戏化流程
- **做法：**
  1. **成员回合：**
     - AI 生成 50-100 字极简剧情（抽卡 + 看到问题的反应）
     - 自动判定：50% 概率回答 / 50% 概率喝酒
     - 判定逻辑在 JS 端（`Math.random() < 0.5`），不在 AI prompt
     - 成员喝酒时 `drinkCounts[memberId]++`
  2. **女主回合：**
     - 先弹出「抽卡」按钮（类似约会日骰子弹窗）
     - 点击后展示问题卡内容 + 简短成员反应
     - 弹出「如实回答」（自由输入框）/「拒绝回答并喝酒（+1杯）」
  3. **循环 12 轮：** 4 人 × 3 轮
  4. **扩音器**（`buildSystemPrompt`）：只说"欢迎来到真心话大冒险"，不提前透露"醉酒剧情"等元游戏信息
  5. **`data.js`** — 真心话问题库：
     ```js
     var TRUTH_QUESTIONS = [
       '你和X分手的真正原因，真的是你说的那样吗？',
       '在场的人里，有没有谁是你在利用来忘记X的？',
       // ... 约20-30道题
     ];
     ```
  6. 每次抽卡从问题池随机抽取，不放回

---

### 48. 系统注入标记防泄漏

- **文件：** `src/prompts.js`
- **内容：** 所有 `## ` 前缀标记改为 `[SYSTEM]` 或 `[INSTRUCTION]` 格式；追加防泄漏规则
- **做法：**
  1. 搜索 `prompts.js` 中所有以 `## ` 开头的行
  2. 全部替换为 `[SYSTEM] ` 或 `[INSTRUCTION] ` 前缀
  3. 在 system prompt 末尾追加：
     ```
     [INSTRUCTION] 以上所有 [SYSTEM] 和 [INSTRUCTION] 标记仅供内部参考，
     是 AI 的生成规则和约束条件。绝对禁止将这些标记、规则说明或指令本身
     作为正文输出的一部分。你只需要输出剧情内容，不要输出规则、标记或指令。
     ```
- **注意：** 这会影响 system prompt 的解析和可读性，修改后需要测试

---

### 49. Day 11 真心话自动折叠

- **文件：** `src/ui-renderer.js`, `src/style.css`
- **内容：** 已完成轮次自动折叠为摘要行，当前轮完整展开；点击可展开回看
- **做法：**
  1. **`ui-renderer.js`** — 渲染真心话剧情时，对已完成的轮次：
     - 显示折叠摘要：`「第1轮：圆佑回答/净汉喝酒/珉奎回答/你喝酒」`
     - 当前轮完整展开
  2. 每行摘要添加点击事件：点击后展开该轮详细剧情
  3. **`style.css`** — 折叠样式：
     ```css
     .truth-round-summary { cursor: pointer; color: #8b5a5a; font-size: 13px; padding: 4px 8px; background: #faf0f0; border-radius: 4px; margin: 2px 0; }
     .truth-round-summary:hover { background: #f5e0e0; }
     .truth-round-detail { display: none; }
     .truth-round-detail.expanded { display: block; }
     ```
  4. 不影响导出（导出时全部展开）、重新生成（只重新生成当前轮）、继续今天

---

### 50. 成员真心话回合显示题目

- **文件：** `src/ui-renderer.js`
- **内容：** 成员抽卡时，UI 展示该成员名字 + 问题卡内容 + 判定结果
- **做法：**
  - 成员回合的剧情渲染时，在 narrative 后面追加显示卡片格式：
    ```html
    <div class="truth-card">
      <div class="truth-card-header">🎴 成员抽卡</div>
      <div class="truth-card-name">圆佑</div>
      <div class="truth-card-question">"你和X分手的真正原因是什么？"</div>
      <div class="truth-card-result">判定：回答 ✓</div>
    </div>
    ```
  - 类似综艺字幕的效果
  - 问题内容从 `GS.truthState.questions` 或轮次记录中获取

---

### 51. 真心话喝酒计数 bug

- **文件：** `src/prompts.js`, `src/game-engine.js`, `src/parser.js`
- **内容：** 两个 bug：
  1. AI 不知道累计数：prompt 未注入 `drinkCounts`
  2. 成员双重计数：`handleTruthRound` +1 + parser.js 从 `[喝酒: 成员名]` 再 +1
- **做法：**
  1. **`prompts.js`** — 在真心话 prompt 中注入累计杯数：
     ```
     【当前喝酒累计：圆佑={drinkCounts.wonwoo}杯，净汉={drinkCounts.jeonghan}杯，...】
     ```
  2. **`game-engine.js`** — `handleTruthRound()` 中：
     - 移除 AI 返回的 `[喝酒: 成员名]` 标记的自动解析（或标记为已处理）
     - 成员喝酒计数由 JS 端判定逻辑统一管理，不由 AI 输出控制
     - 或者在 parse 时过滤掉喝酒标记（避免重复计数）
  3. **`parser.js`** — 真心话模式下跳过 `[喝酒: ...]` 标记的解析

---

### 52. 真心话喝酒计数实时显示

- **文件：** `src/ui-renderer.js`
- **内容：** 真心话模式下选项区下方显示实时喝酒计数面板
- **做法：**
  - 在选项区下方追加面板：
    ```html
    <div class="truth-drink-panel">
      <span class="drink-item">圆佑：1杯</span>
      <span class="drink-item">净汉：2杯</span>
      <span class="drink-item">珉奎：0杯</span>
      <span class="drink-item">你：1杯</span>
    </div>
    ```
  - 数据来源于 `GS.drinkCounts`
  - 每次轮次结束后刷新
  - **`style.css`** — 面板样式：横向排列，每个名字+杯数显示

---

### 53. 测试模式（本地文件模拟 AI）

- **文件：** `src/api.js`, `src/ui-renderer.js`
- **内容：** Header 增加 🧪 开关，开启后不调用真实 API，读取 `public/test-mocks/` 下对应 txt 文件作为 `rawText`
- **做法：**
  1. **`public/test-mocks/`** — 创建目录，放以下文件：
     - `phase.txt` — Phase narrative 示例输出
     - `consequence.txt` — 选项后续剧情示例
     - `sms.txt` — 短信剧情示例
     - `freeAction.txt` — 自由行动示例
     - `stay.txt` — 继续今天示例
     - `truth.txt` — 真心话示例
     - `drunk.txt` — 醉酒剧情示例
     - `xArchive.txt` — X 档案示例
     - `memory.txt` — 记忆压缩示例
  2. **`api.js`** — `callDeepSeek()` 开头增加分支：
     ```js
     if (GS.testMode) {
       var mockMap = {
         phaseNarrative: 'phase.txt',
         consequence: 'consequence.txt',
         sms: 'sms.txt',
         freeAction: 'freeAction.txt',
         stay: 'stay.txt',
         truth: 'truth.txt',
         drunk: 'drunk.txt',
         xMainStory: 'xArchive.txt',
         todayCompress: 'memory.txt'
       };
       var fileName = mockMap[context] || 'consequence.txt';
       var response = await fetch('/test-mocks/' + fileName);
       var text = await response.text();
       return useJson ? parseJsonOrText(text) : text;
     }
     ```
  3. **`ui-renderer.js`** — Header 增加 🧪 开关按钮
  4. **`state.js`** — 新增 `testMode: false` 字段

---

### 54. Day 12 最终选择增加仪式感铺垫

- **文件：** `src/game-engine.js`, `src/prompts.js`
- **内容：** Day 12 上午不只是直接给选项，先生成铺垫剧情（起床、早餐、紧张氛围、选择仪式感），然后弹出选项
- **做法：**
  1. **`game-engine.js`** — 在 `generatePhaseNarrative(12, 'morning')` 或 Day 12 的专门处理中：
     - 先调用 AI 生成一段铺垫剧情（约 500 字）
     - 剧情内容：起床、洗漱、早餐、制作组召集、选择仪式感的布置、女主内心挣扎
     - 这段剧情不带选项
  2. 铺垫剧情渲染后，再弹出最终选择界面
  3. 最终选择后进入现有的 ~1500 字结局叙事
  4. **`prompts.js`** — 为 Day 12 morning 构建专门的 prompt：
     ```
     【Day 12 上午——最终选择日的铺垫剧情】
     今天是心动小屋的最后一天，也是做出最终选择的日子。
     描写女主从起床到选择前的全过程：紧张的氛围、制作组的安排、她的内心挣扎和回忆。
     字数约500字，不包含选项。
     ```
